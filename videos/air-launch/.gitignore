# Studio scratch and render working directories — regenerated on demand
snapshots/
renders/work-*/
renders/.*hf-transaction*/

# The finished film is 79 MB: too heavy for the repo. Render it with `npm run render`,
# or take the copy delivered with the build. Stills in renders/stills/ are committed.
renders/*.mp4
