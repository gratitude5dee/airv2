# Wave 3: redesign phase 4 — UI debt burn-down

Status: done — D2 SSE cleanup (chat + bots EventSources tracked in refs, closed in effect cleanup); D3 ads zombie poll (generation counters invalidate in-flight loads/polls on unmount; analytics tab only mounts while its subtab is active); D11 stable message ids (`nextMessageId`/`replaceLast` in app/home/lib.ts — chat, bot, room, transcript, trace-receipt lists all key by id, no index keys); D9 `role="log"` + `aria-live="polite"` on the main chat, bot chat, and room logs; D10 `useDialogFocus` (app/home/use-dialog.ts: focus trap, Esc closes, focus restore) on the bots drawer, new-bot sheet, vault modal, needs detail sheet, and store detail sheet; D6 stale-while-revalidate (`useStaleWhileRevalidate` + module cache in app/home/use-swr.ts for unmounting panels; activation-refresh with retained state for mounted-but-hidden panels: history, skills, people, wallet, connectors); D8 stick-to-bottom via `isNearBottom` + onScroll tracking — auto-scroll only when already near the bottom. P1 sweep: D7 catches on naked fetches across extracted panels; D14 shared `isComputerTool` in lib.ts (page + browser-panel dupes removed); D16 power failure resets optimistic state to stopped; D17 copy/reveal timer cleanup (page, wallet, vault); D18 vestigial `active` props dropped from apps/calendar/vault panels (parents render conditionally); D20 dead `usd` token field removed from wallet types (ads `usd()` formatter is live and untouched). Unit tests in app/home/lib.test.ts cover isComputerTool/isNearBottom/nextMessageId/replaceLast/SWR cache. D15 ref-below-effect: no remaining instances found in the new files (already clean on main).

Phase 4 of docs/review-2026-08/03-redesign-spec.md §9; debt register §10.

See `docs/review-2026-08/01-engineering-review.md` for full detail.
