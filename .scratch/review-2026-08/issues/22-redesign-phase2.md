# Redesign Phase 2: Pixel OS skin

Status: done (spec §1/§4/§5/§7/§9) — Pixel OS tokens (`--outline`, `--shadow-hard`, `--chrome-top`, `.chrome`/`.tilebox` utilities) in globals.css; dither-kit PixelIcon (8×8 SVG glyph set, currentColor) + DitherMarquee (Bayer/PRNG selection marquee); nav + app tiles on PixelIcons with DitherAvatar fallback per slug; DitherButton adopted for primary actions; /home App Store screen (featured DitherGradient banner + single gloss band, category pills, 4-up tile grid with gate badges, detail sheet, Open→in-chat, install via POST /api/mini/install, earnings chip, search on /api/store/search); LaunchButton 402 → app pay page; Installed + right-rail tiles on shared AppTile chrome; Settings assembly per §7 (Profile + Plugin Sign-In side by side, CONNECTORS/SKILLS tabs); Context screen (memory/traces/onairos cards over existing owner APIs, new GET /api/me/traces list); vault emoji glyphs → PixelIcons (D19).
