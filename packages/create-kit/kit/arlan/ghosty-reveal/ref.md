<!-- GENERATED by packages/create-kit/scripts/harvest.ts — do not edit. Edit prompts/src/*, scripts/lib/catalog.ts or a component's ref.md source and re-run harvest. Reference for arlan/ghosty-reveal. -->

# Ghosty Reveal (`arlan/ghosty-reveal`)

Mask-image wipe that reveals or hides a block using an image mask you supply.

- **Weight**: 0.9 KiB JS + 0.1 KiB CSS (own, gzip); 0.9 KiB with vendor deps. **Lite**: true. **Touch**: true. **Reduced motion**: static.
- **License**: MIT, tier A, from `arlan.me/vault@2026-09-04` by Arlan Marat.
- **Deps**: none beyond React.

## Usage

```tsx
import { GhostyReveal } from "./kit/arlan/ghosty-reveal";

<GhostyReveal maskSrc={…} maskSrcH={…} scale={…} />
```

## Props

| Prop | Type |
| --- | --- |
| `maskSrc` | `string` |
| `maskSrcH` | `string?` |
| `scale` | `number?` |
| `duration` | `number?` |
| `easing` | `string?` |
| `direction` | `GhostDirection?` |
| `play` | `boolean?` |
| `threshold` | `number?` |
| `onHidden` | `() => void?` |

## Compose

1. Inside a shell `.panel` as the screen's one hero element, with the headline in `--font-body` and labels in `.kicker`.
2. As a row in a list/card (`.item`, `.card`) where it is decoration for the text next to it — then it must be `aria-hidden`.

## Failure modes

- Under `prefers-reduced-motion` the final frame renders immediately; make sure that frame is complete.

## Air tokens

Colours, radii and fonts come from `kit/air/theme.css` custom properties (`--ink`, `--accent`, `--panel-bg`, `--ring`, `--radius-*`, `--font-*`); pass them through `className`/`style`, never literals.
