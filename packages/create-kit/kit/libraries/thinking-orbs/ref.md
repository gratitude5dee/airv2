<!-- GENERATED by packages/create-kit/scripts/harvest.ts — do not edit. Edit prompts/src/*, scripts/lib/catalog.ts or a component's ref.md source and re-run harvest. Reference for libraries/thinking-orbs. -->

# Thinking Orbs (`libraries/thinking-orbs`)

Nine named agent states (working, searching, solving…) on a 2D canvas. Best 'the agent is doing X' indicator.

- **Weight**: 0.6 KiB JS + 0.1 KiB CSS (own, gzip); 6.4 KiB with vendor deps. **Lite**: true. **Touch**: true. **Reduced motion**: static.
- **License**: MIT, tier A, from `Jakubantalik/Libraries.dev@aea4c00db4b7` by Jakub Antalik.
- **Deps**: `thinking-orbs`.

## Usage

```tsx
import { ThinkingOrbs } from "./kit/libraries/thinking-orbs";

<ThinkingOrbs paused={…} state={…} size={…} />
```

## Props

| Prop | Type |
| --- | --- |
| `paused` | `boolean` |
| `state` | `'working'\|'searching'\|'solving'\|'listening'\|'connecting'\|'weaving'\|'composing'\|'breathing'\|'shaping'` |
| `size` | `20 \| 64` |
| `speed` | `number` |
| `theme` | `'auto'\|'dark'\|'light'` |

## Compose

1. Inside a shell `.panel` as the screen's one hero element, with the headline in `--font-body` and labels in `.kicker`.
2. As a row in a list/card (`.item`, `.card`) where it is decoration for the text next to it — then it must be `aria-hidden`.

## Failure modes

- Under `prefers-reduced-motion` the final frame renders immediately; make sure that frame is complete.

## Air tokens

Colours, radii and fonts come from `kit/air/theme.css` custom properties (`--ink`, `--accent`, `--panel-bg`, `--ring`, `--radius-*`, `--font-*`); pass them through `className`/`style`, never literals.
