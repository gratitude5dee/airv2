<!-- GENERATED by packages/create-kit/scripts/harvest.ts — do not edit. Edit prompts/src/*, scripts/lib/catalog.ts or a component's ref.md source and re-run harvest. Reference for libraries/border-beam. -->

# Border Beam (`libraries/border-beam`)

Animated beam around one card that deserves attention (the pending approval, the live item).

- **Weight**: 0.5 KiB JS + 0.1 KiB CSS (own, gzip); 10.6 KiB with vendor deps. **Lite**: true. **Touch**: true. **Reduced motion**: static.
- **License**: MIT, tier A, from `Jakubantalik/Libraries.dev@aea4c00db4b7` by Jakub Antalik.
- **Deps**: `border-beam`.

## Usage

```tsx
import { BorderBeam } from "./kit/libraries/border-beam";

<BorderBeam size={…} colorVariant={…} theme={…} />
```

## Props

| Prop | Type |
| --- | --- |
| `size` | `'sm'\|'md'\|'line'\|'pulse-outside'\|'pulse-inner'` |
| `colorVariant` | `'colorful'\|'mono'\|'ocean'\|'sunset'` |
| `theme` | `'dark'\|'light'\|'auto'` |
| `strength` | `number` |
| `duration` | `number` |
| `active` | `boolean` |
| `borderRadius` | `number` |

## Compose

1. Inside a shell `.panel` as the screen's one hero element, with the headline in `--font-body` and labels in `.kicker`.
2. As a row in a list/card (`.item`, `.card`) where it is decoration for the text next to it — then it must be `aria-hidden`.

## Failure modes

- Under `prefers-reduced-motion` the final frame renders immediately; make sure that frame is complete.

## Air tokens

Colours, radii and fonts come from `kit/air/theme.css` custom properties (`--ink`, `--accent`, `--panel-bg`, `--ring`, `--radius-*`, `--font-*`); pass them through `className`/`style`, never literals.
