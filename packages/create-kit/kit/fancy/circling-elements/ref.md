<!-- GENERATED by packages/create-kit/scripts/harvest.ts — do not edit. Edit prompts/src/*, scripts/lib/catalog.ts or a component's ref.md source and re-run harvest. Reference for fancy/circling-elements. -->

# Circling Elements (`fancy/circling-elements`)

Small items orbiting a center: avatars around a host, badges around a logo.

- **Weight**: 0.6 KiB JS + 0.1 KiB CSS (own, gzip); 49.5 KiB with vendor deps. **Lite**: true. **Touch**: true. **Reduced motion**: static.
- **License**: MIT, tier A, from `danielpetho/fancy@f9f62c61207b` by daniel petho.
- **Deps**: `clsx`, `tailwind-merge`.

## Usage

```tsx
import { CirclingElements } from "./kit/fancy/circling-elements";

<CirclingElements radius={…} duration={…} easing={…} />
```

## Props

| Prop | Type |
| --- | --- |
| `radius` | `number?` |
| `duration` | `number?` |
| `easing` | `string?` |
| `direction` | `"normal" \| "reverse"?` |
| `pauseOnHover` | `boolean?` |

## Compose

1. Inside a shell `.panel` as the screen's one hero element, with the headline in `--font-body` and labels in `.kicker`.
2. As a row in a list/card (`.item`, `.card`) where it is decoration for the text next to it — then it must be `aria-hidden`.

## Failure modes

- Under `prefers-reduced-motion` the final frame renders immediately; make sure that frame is complete.

## Air tokens

Colours, radii and fonts come from `kit/air/theme.css` custom properties (`--ink`, `--accent`, `--panel-bg`, `--ring`, `--radius-*`, `--font-*`); pass them through `className`/`style`, never literals.
