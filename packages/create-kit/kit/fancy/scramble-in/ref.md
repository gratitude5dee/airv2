<!-- GENERATED by packages/create-kit/scripts/harvest.ts — do not edit. Edit prompts/src/*, scripts/lib/catalog.ts or a component's ref.md source and re-run harvest. Reference for fancy/scramble-in. -->

# Scramble In (`fancy/scramble-in`)

Reveal a short label by unscrambling it once on mount or on view.

- **Weight**: 0.7 KiB JS + 0.1 KiB CSS (own, gzip); 0.7 KiB with vendor deps. **Lite**: true. **Touch**: true. **Reduced motion**: static.
- **License**: MIT, tier A, from `danielpetho/fancy@f9f62c61207b` by daniel petho.
- **Deps**: none beyond React.

## Usage

```tsx
import { ScrambleIn } from "./kit/fancy/scramble-in";

<ScrambleIn text={…} scrambleSpeed={…} scrambledLetterCount={…} />
```

## Props

| Prop | Type |
| --- | --- |
| `text` | `string` |
| `scrambleSpeed` | `number?` |
| `scrambledLetterCount` | `number?` |
| `characters` | `string?` |
| `scrambledClassName` | `string?` |
| `autoStart` | `boolean?` |
| `onStart` | `() => void?` |
| `onComplete` | `() => void?` |

## Compose

1. Inside a shell `.panel` as the screen's one hero element, with the headline in `--font-body` and labels in `.kicker`.
2. As a row in a list/card (`.item`, `.card`) where it is decoration for the text next to it — then it must be `aria-hidden`.

## Failure modes

- Under `prefers-reduced-motion` the final frame renders immediately; make sure that frame is complete.

## Air tokens

Colours, radii and fonts come from `kit/air/theme.css` custom properties (`--ink`, `--accent`, `--panel-bg`, `--ring`, `--radius-*`, `--font-*`); pass them through `className`/`style`, never literals.
