<!-- GENERATED by packages/create-kit/scripts/harvest.ts — do not edit. Edit prompts/src/*, scripts/lib/catalog.ts or a component's ref.md source and re-run harvest. Reference for fancy/pixelate-svg-filter. -->

# Pixelate SVG Filter (`fancy/pixelate-svg-filter`)

Provides an SVG pixelation filter by id; apply to an image that resolves on load.

- **Weight**: 0.6 KiB JS + 0.1 KiB CSS (own, gzip); 0.6 KiB with vendor deps. **Lite**: true. **Touch**: true. **Reduced motion**: n/a.
- **License**: MIT, tier A, from `danielpetho/fancy@f9f62c61207b` by daniel petho.
- **Deps**: none beyond React.

## Usage

```tsx
import { PixelateSVGFilter } from "./kit/fancy/pixelate-svg-filter";

<PixelateSVGFilter id={…} size={…} crossLayers={…} />
```

## Props

| Prop | Type |
| --- | --- |
| `id` | `string` |
| `size` | `number?` |
| `crossLayers` | `boolean?` |

## Compose

1. Inside a shell `.panel` as the screen's one hero element, with the headline in `--font-body` and labels in `.kicker`.
2. As a row in a list/card (`.item`, `.card`) where it is decoration for the text next to it — then it must be `aria-hidden`.

## Failure modes

- None recorded.

## Air tokens

Colours, radii and fonts come from `kit/air/theme.css` custom properties (`--ink`, `--accent`, `--panel-bg`, `--ring`, `--radius-*`, `--font-*`); pass them through `className`/`style`, never literals.
