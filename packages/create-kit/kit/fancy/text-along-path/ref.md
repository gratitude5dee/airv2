<!-- GENERATED by packages/create-kit/scripts/harvest.ts — do not edit. Edit prompts/src/*, scripts/lib/catalog.ts or a component's ref.md source and re-run harvest. Reference for fancy/text-along-path. -->

# Text Along Path (`fancy/text-along-path`)

Text set along an SVG path (circles, arcs). Decorative headers only.

- **Weight**: 0.9 KiB JS + 0.1 KiB CSS (own, gzip); 8.9 KiB with vendor deps. **Lite**: true. **Touch**: true. **Reduced motion**: static.
- **License**: MIT, tier A, from `danielpetho/fancy@f9f62c61207b` by daniel petho.
- **Deps**: `motion`.

## Usage

```tsx
import { TextAlongPath } from "./kit/fancy/text-along-path";

<TextAlongPath path={…} pathId={…} pathClassName={…} />
```

## Props

| Prop | Type |
| --- | --- |
| `path` | `string` |
| `pathId` | `string?` |
| `pathClassName` | `string?` |
| `preserveAspectRatio` | `PreserveAspectRatio?` |
| `showPath` | `boolean?` |
| `width` | `string \| number?` |
| `height` | `string \| number?` |
| `viewBox` | `string?` |
| `svgClassName` | `string?` |
| `text` | `string` |
| `textClassName` | `string?` |
| `textAnchor` | `"start" \| "middle" \| "end"?` |
| `animationType` | `"auto" \| "scroll"?` |
| `duration` | `number?` |
| `repeatCount` | `number \| "indefinite"?` |
| `easingFunction` | `object?` |
| `scrollContainer` | `RefObject<HTMLElement \| null>?` |
| `scrollOffset` | `UseScrollOptions["offset"]?` |
| `scrollTransformValues` | `[number, number]?` |

## Compose

1. Inside a shell `.panel` as the screen's one hero element, with the headline in `--font-body` and labels in `.kicker`.
2. As a row in a list/card (`.item`, `.card`) where it is decoration for the text next to it — then it must be `aria-hidden`.

## Failure modes

- Under `prefers-reduced-motion` the final frame renders immediately; make sure that frame is complete.
- Pulls `motion` (shared with other motion components; counted once per app).

## Air tokens

Colours, radii and fonts come from `kit/air/theme.css` custom properties (`--ink`, `--accent`, `--panel-bg`, `--ring`, `--radius-*`, `--font-*`); pass them through `className`/`style`, never literals.
