<!-- GENERATED by packages/create-kit/scripts/harvest.ts — do not edit. Edit prompts/src/*, scripts/lib/catalog.ts or a component's ref.md source and re-run harvest. Reference for fancy/underline-to-background. -->

# Underline to Background (`fancy/underline-to-background`)

Link underline that swells into a highlight on hover; good for a single CTA link.

- **Weight**: 0.8 KiB JS + 0.1 KiB CSS (own, gzip); 49.6 KiB with vendor deps. **Lite**: true. **Touch**: true. **Reduced motion**: reduced.
- **License**: MIT, tier A, from `danielpetho/fancy@f9f62c61207b` by daniel petho.
- **Deps**: `motion`.

## Usage

```tsx
import { UnderlineToBackground } from "./kit/fancy/underline-to-background";

<UnderlineToBackground as={…} transition={…} targetTextColor={…} />
```

## Props

| Prop | Type |
| --- | --- |
| `as` | `ElementType?` |
| `transition` | `ValueAnimationTransition?` |
| `targetTextColor` | `string` |
| `underlineHeightRatio` | `number?` |
| `underlinePaddingRatio` | `number?` |

## Compose

1. Inside a shell `.panel` as the screen's one hero element, with the headline in `--font-body` and labels in `.kicker`.
2. As a row in a list/card (`.item`, `.card`) where it is decoration for the text next to it — then it must be `aria-hidden`.

## Failure modes

- Pulls `motion` (shared with other motion components; counted once per app).

## Air tokens

Colours, radii and fonts come from `kit/air/theme.css` custom properties (`--ink`, `--accent`, `--panel-bg`, `--ring`, `--radius-*`, `--font-*`); pass them through `className`/`style`, never literals.
