<!-- GENERATED by packages/create-kit/scripts/harvest.ts — do not edit. Edit prompts/src/*, scripts/lib/catalog.ts or a component's ref.md source and re-run harvest. Reference for fancy/letter-3d-swap. -->

# Letter 3D Swap (`fancy/letter-3d-swap`)

Letters flip on a 3D axis on hover. Hover-only; heavier than the 2D swaps.

- **Weight**: 1.5 KiB JS + 0.1 KiB CSS (own, gzip); 32.9 KiB with vendor deps. **Lite**: true. **Touch**: false. **Reduced motion**: none.
- **License**: MIT, tier A, from `danielpetho/fancy@f9f62c61207b` by daniel petho.
- **Deps**: `motion`.

## Usage

```tsx
import { Letter3DSwap } from "./kit/fancy/letter-3d-swap";

<Letter3DSwap as={…} mainClassName={…} frontFaceClassName={…} />
```

## Props

| Prop | Type |
| --- | --- |
| `as` | `ElementType?` |
| `mainClassName` | `string?` |
| `frontFaceClassName` | `string?` |
| `secondFaceClassName` | `string?` |
| `staggerDuration` | `number?` |
| `staggerFrom` | `"first" \| "last" \| "center" \| number \| "random"?` |
| `transition` | `ValueAnimationTransition \| AnimationOptions?` |
| `rotateDirection` | `"top" \| "right" \| "bottom" \| "left"?` |

## Compose

1. Inside a shell `.panel` as the screen's one hero element, with the headline in `--font-body` and labels in `.kicker`.
2. As a row in a list/card (`.item`, `.card`) where it is decoration for the text next to it — then it must be `aria-hidden`.

## Failure modes

- Hover/pointer-driven: provide the same information without the interaction on touch.
- Pulls `motion` (shared with other motion components; counted once per app).

## Air tokens

Colours, radii and fonts come from `kit/air/theme.css` custom properties (`--ink`, `--accent`, `--panel-bg`, `--ring`, `--radius-*`, `--font-*`); pass them through `className`/`style`, never literals.
