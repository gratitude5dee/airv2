<!-- GENERATED by packages/create-kit/scripts/harvest.ts — do not edit. Edit prompts/src/*, scripts/lib/catalog.ts or a component's ref.md source and re-run harvest. Reference for fancy/letter-swap-pingpong-anim. -->

# Letter Swap (ping-pong) (`fancy/letter-swap-pingpong-anim`)

Like Letter Swap (forward) but rolls back on hover-out. Hover-only.

- **Weight**: 0.7 KiB JS + 0.1 KiB CSS (own, gzip); 43.3 KiB with vendor deps. **Lite**: true. **Touch**: false. **Reduced motion**: none.
- **License**: MIT, tier A, from `danielpetho/fancy@f9f62c61207b` by daniel petho.
- **Deps**: `motion`.

## Usage

```tsx
import { LetterSwapPingPong } from "./kit/fancy/letter-swap-pingpong-anim";

<LetterSwapPingPong label={…} reverse={…} transition={…} />
```

## Props

| Prop | Type |
| --- | --- |
| `label` | `string` |
| `reverse` | `boolean?` |
| `transition` | `AnimationOptions?` |
| `staggerDuration` | `number?` |
| `staggerFrom` | `"first" \| "last" \| "center" \| number?` |
| `onClick` | `() => void?` |

## Compose

1. Inside a shell `.panel` as the screen's one hero element, with the headline in `--font-body` and labels in `.kicker`.
2. As a row in a list/card (`.item`, `.card`) where it is decoration for the text next to it — then it must be `aria-hidden`.

## Failure modes

- Hover/pointer-driven: provide the same information without the interaction on touch.
- Pulls `motion` (shared with other motion components; counted once per app).

## Air tokens

Colours, radii and fonts come from `kit/air/theme.css` custom properties (`--ink`, `--accent`, `--panel-bg`, `--ring`, `--radius-*`, `--font-*`); pass them through `className`/`style`, never literals.
