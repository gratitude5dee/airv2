<!-- GENERATED by packages/create-kit/scripts/harvest.ts — do not edit. Edit prompts/src/*, scripts/lib/catalog.ts or a component's ref.md source and re-run harvest. Reference for beautiful/glide-menu. -->

# Glide Menu (atom) (`beautiful/glide-menu`)

Sliding highlight menu used inside approval-card, records-table, search.

- **Weight**: 0.6 KiB JS + 0.1 KiB CSS (own, gzip); 0.6 KiB with vendor deps. **Lite**: true. **Touch**: true. **Reduced motion**: reduced.
- **License**: MIT, tier A, from `beautifului.dev@2026-09-04` by Shane Levine.
- **Deps**: none beyond React.

## Usage

```tsx
import { GlideMenuAtom } from "./kit/beautiful/glide-menu";

<GlideMenuAtom highlightClassName={…} rowSelector={…} />
```

## Props

| Prop | Type |
| --- | --- |
| `highlightClassName` | `string?` |
| `rowSelector` | `string?` |

## Compose

1. Inside a shell `.panel` as the screen's one hero element, with the headline in `--font-body` and labels in `.kicker`.
2. As a row in a list/card (`.item`, `.card`) where it is decoration for the text next to it — then it must be `aria-hidden`.

## Failure modes

- None recorded.

## Air tokens

Beautiful UI tokens are bridged in `kit/beautiful/foundation/foundation.css`: `--page`→`--canvas`, `--surface`→`--panel-bg`, `--inset`→`--well-bg`, `--ink-2`→`--ink-muted`, `--line`→`--ring`, `--accent-ink`→`--accent`. Import `kit/beautiful/foundation` once per app.
