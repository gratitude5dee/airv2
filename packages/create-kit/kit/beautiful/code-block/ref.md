<!-- GENERATED by packages/create-kit/scripts/harvest.ts — do not edit. Edit prompts/src/*, scripts/lib/catalog.ts or a component's ref.md source and re-run harvest. Reference for beautiful/code-block. -->

# Code Block (`beautiful/code-block`)

Code block with line numbers and copy. aicss/code-block is the lighter default.

- **Weight**: 2.3 KiB JS + 0.1 KiB CSS (own, gzip); 2.3 KiB with vendor deps. **Lite**: true. **Touch**: true. **Reduced motion**: n/a.
- **License**: MIT, tier A, from `beautifului.dev@2026-09-04` by Shane Levine.
- **Deps**: none beyond React.

## Usage

```tsx
import { CodeBlock } from "./kit/beautiful/code-block";

<CodeBlock variant={…} lines={…} code={…} />
```

## Props

| Prop | Type |
| --- | --- |
| `variant` | `string?` |
| `lines` | `string[]?` |
| `code` | `string?` |
| `diff` | `DiffRow[]?` |
| `filename` | `string?` |
| `labels` | `Partial<CodeBlockLabels>?` |
| `onCopy` | `(text: string) => void?` |

## Compose

1. Inside a shell `.panel` as the screen's one hero element, with the headline in `--font-body` and labels in `.kicker`.
2. As a row in a list/card (`.item`, `.card`) where it is decoration for the text next to it — then it must be `aria-hidden`.

## Failure modes

- None recorded.

## Air tokens

Beautiful UI tokens are bridged in `kit/beautiful/foundation/foundation.css`: `--page`→`--canvas`, `--surface`→`--panel-bg`, `--inset`→`--well-bg`, `--ink-2`→`--ink-muted`, `--line`→`--ring`, `--accent-ink`→`--accent`. Import `kit/beautiful/foundation` once per app.
