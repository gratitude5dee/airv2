<!-- GENERATED by packages/create-kit/scripts/harvest.ts — do not edit. Edit prompts/src/*, scripts/lib/catalog.ts or a component's ref.md source and re-run harvest. Reference for aicss/approval-card. -->

# Approval Card (`aicss/approval-card`)

Approve/deny a proposed action with a short summary. Default for the approval recipe.

- **Weight**: 4.5 KiB JS + 2.9 KiB CSS (own, gzip); 5.7 KiB with vendor deps. **Lite**: true. **Touch**: true. **Reduced motion**: n/a.
- **License**: MIT, tier A, from `kvnkld/aicss@4556a918fd8c` by Kevin Kelder (kvnkld).
- **Deps**: `lucide-react`.

## Usage

```tsx
import { ApprovalCard } from "./kit/aicss/approval-card";

<ApprovalCard />
```

## Props

_No props beyond `className`/`children`._

## Compose

1. Inside a shell `.panel` as the screen's one hero element, with the headline in `--font-body` and labels in `.kicker`.
2. As a row in a list/card (`.item`, `.card`) where it is decoration for the text next to it — then it must be `aria-hidden`.

## Failure modes

- None recorded.

## Air tokens

AI CSS components read `--background`, `--foreground`, `--muted`, `--border`, `--primary` via the `@theme inline` bridge in `kit/air/theme.css`; no per-component mapping needed.
