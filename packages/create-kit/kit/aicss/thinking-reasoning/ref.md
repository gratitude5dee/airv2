<!-- GENERATED by packages/create-kit/scripts/harvest.ts — do not edit. Edit prompts/src/*, scripts/lib/catalog.ts or a component's ref.md source and re-run harvest. Reference for aicss/thinking-reasoning. -->

# Thinking Reasoning (`aicss/thinking-reasoning`)

Collapsible reasoning trace under a thinking indicator.

- **Weight**: 1.6 KiB JS + 1 KiB CSS (own, gzip); 1.6 KiB with vendor deps. **Lite**: true. **Touch**: true. **Reduced motion**: reduced.
- **License**: MIT, tier A, from `kvnkld/aicss@4556a918fd8c` by Kevin Kelder (kvnkld).
- **Deps**: none beyond React.

## Usage

```tsx
import { ThinkingReasoning } from "./kit/aicss/thinking-reasoning";

<ThinkingReasoning />
```

## Props

_No props beyond `className`/`children`._

## Compose

1. Inside a shell `.panel` as the screen's one hero element, with the headline in `--font-body` and labels in `.kicker`.
2. As a row in a list/card (`.item`, `.card`) where it is decoration for the text next to it — then it must be `aria-hidden`.

## Failure modes

- None recorded.

## Air tokens

AI CSS components read `--background`, `--foreground`, `--muted`, `--border`, `--primary` via the `@theme inline` bridge in `kit/air/theme.css`; no per-component mapping needed.
