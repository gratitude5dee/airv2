# arlan.me vault — index as crawled 2026-09-17

Studies listed by `https://arlan.me/sitemap.xml` and their public pages (18), with the Kit decision for each. Supersedes `vault-index.md` (2026-09-04) where both list a study. Descriptions are the author's own one-liners from the study pages; the full crawl is `../catalogues/component-prompts-2026-09-17.csv`.

| Study | URL slug | Kit decision | Renderer · lite · touch | Note |
| --- | --- | --- | --- | --- |
| Amo hover button | `amo` | excluded | video · lite · hover | Kit status: excluded, third-party trade dress (amo). Two rules broken: trade dress, and hover-reveal (doctrine: hover may enhance, never reveal). The clip itself would have to live in the bundle; the description says you generate your own, so no remote-asset risk in principle, but a Builder copying the study would point at amo's hosted clip. If a Builder wants the idea, an original is a bundled looping video in a .pill that plays on pointerdown/focus with a poster frame under reduced motion; that is a pattern, not a harvest. |
| Apple's corners | `squircle` | harvested → `@kit/arlan/squircle` | svg · lite · touch | Harvested as arlan/squircle (1.6kb, lite, touch, motion n/a). Canonical slug squircle matches the recorded status. Note clip-path clips box-shadow and focus rings, so the Kit version is for the shape only; the ring must go on a wrapper. |
| Arcade pixel | `arcade-pixel` | excluded | webgl · non-lite · touch | Kit status: excluded, WebGL (non-lite). Rule broken: lite forbids WebGL; backgrounds and GPU text are non-lite by default. The look itself is cheap to fake as an original if a game-2d title screen wants it: draw the word on a tiny Canvas 2D and upscale with image-rendering: pixelated (or CSS scale on a low-res canvas), which stays lite and DPR 1. That would be a Builder original, not this study. |
| Chromatic glow | `chroma-glow` | excluded | webgl · non-lite · touch | Kit status: excluded, WebGL (non-lite). Rule broken: lite forbids WebGL. A Builder needing a soft chromatic edge on one hero word can layer three text-shadows (or drop-shadow filters) in red, green and blue offset by a pixel or two with a wide blur; that is a two-line original that degrades to a plain glow under pixel theme. The description is one sentence, so renderer is inferred from the recorded exclusion. |
| Dia Browser's gradient | `dia-gradient` | excluded | css · non-lite · touch | Kit status: excluded, third-party trade dress (Dia Browser). Rule broken: trade dress; secondarily a full-screen blurred background, non-lite. A generic bottom glow in Air tokens (var(--accent) bars, no Dia palette) is already covered in spirit by fancy/animated-gradient-with-svg, which renders flat under lite and pixel theme. |
| Fade motion | `fade-motion` | excluded | webgl · non-lite · touch | Kit status: excluded, WebGL (non-lite). Rule broken: lite forbids WebGL. Do not attempt a DOM port: two hundred stacked text layers per word is a compositing bill the 390px webview cannot pay. If a headline needs a motion-blur entrance, fancy/vertical-cut-reveal or arlan/typer is the Kit answer. |
| Figma vector editor | `vector-editor` | excluded | svg · lite · touch | Kit status: excluded, third-party trade dress (Figma). Rule broken: trade dress. Slug discrepancy: the canonical URL is /vault/vector-editor and the index card is titled 'Figma vector editor', but kit.sources.json and exclusions.md record the study under the slug 'figma'; treat them as the same study. A drag-to-edit shape tool for a 'tool' template would be a Builder original in Air tokens with 44px handles, not this look. |
| Ghosty reveal | `ghosty-reveal` | harvested → `@kit/arlan/ghosty-reveal` | css · lite · touch | Harvested as arlan/ghosty-reveal (1.0kb, lite, touch, motion static). The soft mask image must ship inside the bundle (a small PNG or an inline SVG turbulence mask); a remote mask URL would fail the CSP ceiling. Counts as the screen's hero motion when used on the hero image. |
| Holo | `holo` | harvested → `@kit/arlan/holo` | css · lite · touch | Harvested as arlan/holo (6.0kb, lite, touch, motion static, 'one per screen; heavy blend modes'). The study frames itself as an Airbnb identity card rebuilt; the Kit component is the generic foil card, so a Builder must not restore Airbnb layout or marks. DeviceOrientation needs a user gesture on iOS; the Kit version falls back to drag. |
| Kinetic typography | `kinetic-typography` | build (original) / harvest candidate | dom · lite · touch | Kit status: not reviewed for Kit 2026.09. tier-a-harvest-candidate: the source is MIT ('MIT -> free to copy') and could be harvested as @kit/arlan/kinetic-typography at the next harvest; until then a Builder can write an original (N x M overflow-hidden tiles, each holding an offset copy of the glyph in var(--font-body), translateY by sin(t + row + col) via rAF or CSS animation-delay). Must be the screen's one hero motion, finite or aria-hidden, and a plain glyph under reduced motion. Renderer inferred from the description (could equally be Canvas 2D slices), hence medium confidence. Not in this crawl: the three owner-supplied arlan typographic pieces (shutter-type, swing-type, rush-type); they are not among these 18 studies and get no entries here. |
| Liquid UI | `liquid-ui` | harvested → `@kit/arlan/liquid-ui` | svg · lite · touch | Harvested as arlan/liquid-ui (2.9kb, lite, touch, motion static, 'decorative group backgrounds only'). The Kit component is the silhouette engine; the draggable demo cards are the study's playground, not part of the component. libraries/liquid-gooey is the lighter answer when the merge is a menu or tab thumb rather than cards. |
| Midjourney Medical's ASCII | `midjourney` | excluded | dom · lite · touch | Kit status: excluded, third-party trade dress (Midjourney). Rule broken: trade dress. The underlying idea (characters converge into a word on load) is covered by fancy/scramble-in for short labels, and a Builder can write a converge-on-mount original for one hero word; the study's value is the Midjourney wordmark choreography, which must not be reproduced. |
| Pixel brushes | `pixel-brushes` | build (original) / harvest candidate | canvas2d · lite · touch | Kit status: not reviewed for Kit 2026.09. Decision: build-original rather than harvest. Stamping a shape every N px along an interpolated pointer path with spacing, size jitter and scatter parameters is a few dozen lines of Canvas 2D; a Builder writes it under the contract without vendored code, so no tier-a-harvest-candidate flag. Contract notes for the Builder: canvas at DPR 1 under lite, touch-action: none, 44px tool controls, and if the drawing must persist store stroke point lists (not bitmaps) behind useAirState within the 256 KiB resource limit; no localStorage. User-driven marks are not animation, so reduced motion is n/a. |
| Ransom note | `ransom-note` | excluded | dom · lite · touch | Kit status: gap, depends on site-hosted cut-out letter imagery and an unpublished manifest that was not captured (recorded in kit.sources.json, never fabricated). Rule broken: needs remote assets; the CSP ceiling forbids any host reference, and bundling a full cut-out alphabet with variants would be several hundred KiB of images. If a Builder wants the mood, an original in Air tokens is possible without imagery: each letter in a tinted chip with random rotate/scale, alternating var(--font-body) and var(--font-ui), which is a style, not this study. |
| Realistic emboss | `emboss` | excluded | webgl · non-lite · touch | Kit status: excluded, WebGL (non-lite). Rule broken: lite forbids WebGL. The description is one sentence, so renderer follows the recorded exclusion. An embossed label for a 'page' or 'store' header is a cheap original: two text-shadows (light offset up-left, dark offset down-right) on ink-coloured text, or an inline SVG feDiffuseLighting filter; both are lite and match arlan/color-depth's skeuomorphic buttons. |
| Symbols effect | `sandbox` | excluded | webgl · non-lite · touch | Kit status: not reviewed for Kit 2026.09. Decision: not-for-miniapp, WebGL/GPU makes it non-lite and the study's point is live video through the shader, which lite cannot serve and reduced motion would have to freeze to a poster. Slug discrepancy: the canonical URL is /vault/sandbox, the index card is titled 'Symbols effect', and kit.sources.json records it as 'sandbox (Symbols effect; index slug symbols)'; treat all three as this one study. A still-image variant (four brightness bands posterised and stamped with glyphs on Canvas 2D at DPR 1) would be a Builder original for a gallery hero, not a harvest of this GPU code. Any bundled media must be in-bundle; no remote video. |
| The art of color depth | `color-depth` | harvested → `@kit/arlan/color-depth` | css · lite · touch | Harvested as arlan/color-depth (3.4kb, lite, touch, motion n/a; pure CSS classes, SKILL.md ships as ref). Materials must sit on Air tokens so they read under both atmosphere and pixel themes; the glass material should not add backdrop-filter under lite. Canonical slug color-depth matches the recorded status. |
| The typer | `typer` | harvested → `@kit/arlan/typer` | dom · lite · touch | Harvested as arlan/typer (2.7kb, lite, touch, motion static; vanilla class plus React wrapper). Alternative to fancy/typewriter when the out-animation is needed; adjacent letters in the same state merge into one rounded bar, so it needs var(--font-ui) sizing that keeps the bars legible at 390px. Counts as the screen's hero motion. Canonical slug typer matches the recorded status. |

Footer on the index and every study page: "MIT → free to copy" (see `license-page.md`).

Not in this crawl: the three owner-supplied typographic pieces (`shutter-type`, `swing-type`, `rush-type`, briefs under `prompts/src/briefs/`) — they are not listed by the sitemap as of 2026-09-17; verify their vault pages and license footer at harvest.
