<!-- GENERATED by packages/create-kit/scripts/harvest.ts — do not edit. Edit prompts/src/*, scripts/lib/catalog.ts or a component's ref.md source and re-run harvest. Design doc (goal-create-v11 §12.3). -->



# Air Create — Design doc (Kit 2026.09)

Read top to bottom once, then open `ref.md` only for the components you pick. 79 components; 76 are `lite`.


## 1. Doctrine

### The contract

A mini-app is one static bundle behind the Air CSP ceiling. Rules, not preferences:

- `index.html` at the bundle root; everything it loads is in the bundle. No host is ever referenced: no CDN script, no web font, no remote image, no `<iframe>`, no `<meta http-equiv>`, no analytics. The Build Service rejects a bundle that names a host.
- No client storage. `localStorage`, `sessionStorage`, `indexedDB`, cookies and service workers are forbidden; every app shares one origin, so anything persisted is readable by every other app. State lives behind `useAirState()` (the Apps API, 256 KiB per resource; owners write, guests read).
- No `eval(`, no `new Function(`, no inline event handlers, no `javascript:` URLs. `"use client"` where a component touches the DOM; nothing reads `window` at module scope.
- Messages viewport: `<meta name="viewport" content="width=device-width,initial-scale=1,viewport-fit=cover">`, single column ≤ 36rem, `min-height: 100svh`, `env(safe-area-inset-*)` padding, ≥ 44px (2.75rem) touch targets, 16px inputs so iOS does not zoom.
- Lite (`surface.lite`, `<html data-lite="1">`, `useLite()`): no `backdrop-filter`, no fixed backgrounds, DPR 1, no WebGL, no component marked `lite="false"`. Compact card (390×360) first; expanded (390×760) second; nothing depends on a desktop width.
- `prefers-reduced-motion: reduce` yields a static, complete frame. `useReducedMotion()` is the switch; every component in this Kit declares what it does under it (`reducedMotion` in the catalog).

### Air's visual system is data

Use tokens, never literals: `var(--canvas)`, `var(--ink)`, `var(--ink-muted)`, `var(--accent)`, `var(--on-accent)`, `var(--on-ink)`, `var(--panel-bg)`, `var(--well-bg)`, `var(--ring)`, `var(--shadow)`, `var(--blur)`, `var(--font-body)`, `var(--font-ui)`, `var(--radius-panel)`, `var(--radius-well)`, `var(--radius-pill)`, `var(--text-shadow)`, `var(--slide-in)`. `kit/air/theme.css` defines them for both themes; `kit/air/shell.css` gives the structural vocabulary (`.frame`, `header.bar`, `main.app`, `.panel`, `.notice`, `.row`, `.item`, `.card`, `.tile`, `.grid`, `.chip`, `.addrow`, `.tablewrap`, `.hero`, `.navlink`). Build screens from that vocabulary first and reach for a catalog component only where the recipe names one.

`atmosphere` is the default (Newsreader display over Azeret Mono labels, warm dark canvas, soft blur). `pixel` is the fallback (Inter/system, flat neutrals, no shader, no blur) and is what lite surfaces get. A component that only looks right under one theme is wrong.

### Motion

One hero motion per screen. Everything else settles in ≤ 300ms with an ease-out. Nothing moves on scroll inside a webview; no parallax, no scroll-linked reveals. Under reduced motion the hero is a static frame that already says everything. Loops are finite or explicitly `aria-hidden` decoration. Never animate `width`/`height`/`top`/`left`; use `transform` and `opacity`.

### Typography, density, copy

Display in `--font-body`, labels/buttons/counters in `--font-ui` with `.kicker` tracking. One display size per screen, body 16px, labels 12–13px uppercase. Touch first: single column, 44px rows, no hover-only affordance (hover may enhance, never reveal). Copy is short, present tense, no exclamation marks, no emoji in UI chrome. The owner's name and the app's purpose appear once, in the header pill.

### Data and identity

The app knows the viewer only as owner or guest, and learns which by trying to save: `useAirState().canWrite` is `null` until a write is attempted, `false` after a 403. Show guests the same screen, read-only, without a broken control. No secrets in `src/`; Functions secrets live in the Secrets tab and are read server-side only.

## 2. Recipes

Twelve shapes cover almost every request. Start from the closest one and remove, never add, motion.

### Status / thinking view

The owner's agent is working on something and the app shows its progress. Components: `aicss/thinking-state` (the phase list), `libraries/thinking-orbs` (the hero, `AirThinkingOrb`), `aicss/streaming-text` or `fancy/typewriter` for the one live line.

```tsx
<main className="app">
  <section className="panel hero">
    <AirThinkingOrb state={status} />          {/* one hero motion */}
    <p className="kicker">Working</p>
    <Typewriter text={currentStep} />          {/* the live line */}
  </section>
  <ThinkingState steps={steps} />              {/* settles; no per-row animation */}
</main>
```

State comes from `useAirState<{ status; steps }>("progress")`, written by the owner's agent, read by everyone. Under reduced motion the orb is a still disc and the typewriter renders the full line.

### Approval card

One decision, two buttons, a receipt. Components: `beautiful/approval-card` (or `aicss/approval-card` for the compact variant), `beautiful/button`, `libraries/border-beam` only while the decision is pending.

```tsx
const { state, update, canWrite } = useAirState<{ decision: "pending" | "approved" | "declined" }>("approval");
<AirBorderBeam active={state.decision === "pending"}>
  <ApprovalCard
    title="Send the invoice to Nadia?"
    detail="$1,240 · net 30"
    onApprove={() => update((s) => ({ ...s, decision: "approved" }))}
    onDecline={() => update((s) => ({ ...s, decision: "declined" }))}
    disabled={canWrite === false}
  />
</AirBorderBeam>
```

Guests see the same card with the buttons disabled and the notice "Guests are read-only." The card is forwardable, so it never contains the thing being approved in full: one line of detail, and the receipt after the decision.

### List with an add row

The oldest mini-app shape: a `.panel` of `.item` rows and an `.addrow` at the bottom. No catalog component is required; `aicss/todo-list` is the ready-made version, `beautiful/task-rows` when rows carry status pills.

```tsx
const { state, update } = useAirState<{ items: { id: string; text: string; done: boolean }[] }>("list", { items: [] });
<section className="panel">
  {state.items.map((it) => (
    <label key={it.id} className="item row">
      <input type="checkbox" checked={it.done} onChange={() => toggle(it.id)} />
      <span className={it.done ? "muted" : ""}>{it.text}</span>
    </label>
  ))}
  <form className="addrow" onSubmit={add}>
    <input placeholder="Add…" />
    <button className="chip">Add</button>
  </form>
</section>
```

Rows are 44px, inputs 16px. Reorder by explicit up/down chips, not drag. Nothing animates except the new row's ≤ 300ms fade.

### Countdown / ticker hero

A number that matters, large. Components: `fancy/basic-number-ticker` (the hero), `fancy/text-rotate` or `fancy/vertical-cut-reveal` for the label, `fancy/simple-marquee` for a one-line ticker under it.

```tsx
<section className="panel hero">
  <p className="kicker">Doors open in</p>
  <NumberTicker from={0} to={hours} autoStart />
  <VerticalCutReveal>hours</VerticalCutReveal>
</section>
<SimpleMarquee baseVelocity={2}><span className="chip">Sat 8pm</span> …</SimpleMarquee>
```

The ticker is the only hero motion; the marquee stops under reduced motion and lite (`useReducedMotion()` → render a static row of chips). Time comes from the client clock; never fetch time from a host.

### RSVP / guest list (Functions)

Many people write, so state cannot be the owner's 256 KiB resource. This shape needs a Functions backend: `functions/index.ts` with `hono` + `zod`, one D1 table, `POST /api/rsvp` and `GET /api/guests`. Front end: `beautiful/records-table` or a `.panel` list of `.item` rows, `beautiful/entity-chip` for each guest, `beautiful/button` to submit.

```ts
// functions/index.ts
import { Hono } from "hono";
import { z } from "zod";
import { air } from "@air/functions";
const app = new Hono();
const Rsvp = z.object({ name: z.string().min(1).max(60), going: z.boolean() });
app.post("/api/rsvp", async (c) => {
  const body = Rsvp.parse(await c.req.json());
  await air.db(c).prepare("insert into rsvp(principal,name,going) values(?,?,?) on conflict(principal) do update set name=excluded.name, going=excluded.going")
    .bind(air.user(c.req.raw).principal, body.name, body.going ? 1 : 0).run();
  return c.json({ ok: true });
});
export default app;
```

The guest never sees another guest's principal; names only. The owner sees counts and the list. Rate limits and identity are the Dispatcher's; the app trusts `X-Air-Role` via `air.user()` and nothing else.

### Gallery

Images from the owner's media prefix (`/api/media/*` on the app origin or `https://media.wzrd.tech` via `img-src`), never anywhere else. Components: `.grid` of `.tile` from the shell, `fancy/stacking-cards` for a stacked hero, `fancy/simple-carousel` when the set is ≤ 8, `arlan/squircle` for the tile shape.

```tsx
<section className="grid">
  {photos.map((p) => (
    <figure key={p.src} className="tile squircle">
      <img src={p.src} alt={p.alt} loading="lazy" decoding="async" />
      <figcaption className="muted">{p.caption}</figcaption>
    </figure>
  ))}
</section>
```

Each image ≤ 2 MiB, sized for 390px × DPR 3 at most. No lightbox motion beyond a 200ms fade; no pinch-zoom implementation (the webview does it). Under lite the carousel becomes the grid.

### Form with confirmation

Collect a few fields, show a receipt. Components: shell `.panel` + `.row` inputs, `beautiful/button`, `beautiful/value-pill` on the receipt, `fancy/scramble-in` or `arlan/typer` for the "Sent" line.

```tsx
const [sent, setSent] = useState<null | Form>(null);
return sent ? (
  <section className="panel notice">
    <TyperText text="Sent." play="in" />
    <div className="row"><ValuePill label="Name" value={sent.name} /><ValuePill label="Date" value={sent.date} /></div>
  </section>
) : (
  <form className="panel" onSubmit={submit}>
    <label className="row"><span className="kicker">Name</span><input required maxLength={80} /></label>
    <label className="row"><span className="kicker">Date</span><input type="date" required /></label>
    <Button type="submit" variant="primary">Send</Button>
  </form>
);
```

Where does it go? Either `useAirState().save()` (the owner reads it in the Files tab) or a Functions `POST` (many submitters). Never email, never a webhook to a host. Validate with `zod` on both sides.

### Chat with the owner's agent

A thread with the owner's Hermes through Functions → `POST https://air.internal/v1/chat/completions`. Components: `beautiful/chat-composer` (input), `beautiful/stream-text` or `aicss/text-response` (assistant bubbles), `aicss/code-block` for code, `beautiful/tool-chips` when a turn used tools, `libraries/thinking-orbs` while waiting.

```tsx
const { messages, send, pending } = useThread();     // your hook over fetch("/api/chat", { method: "POST", body })
<main className="app">
  <section className="panel thread">
    {messages.map((m) => m.role === "assistant" ? <StreamText key={m.id} text={m.text} /> : <p key={m.id} className="item">{m.text}</p>)}
    {pending && <AirThinkingOrb state="thinking" size={20} />}
  </section>
  <ChatComposer onSubmit={send} disabled={pending} placeholder="Ask…" />
</main>
```

```ts
// functions/index.ts
app.post("/api/chat", async (c) => {
  const { messages } = z.object({ messages: z.array(Msg).max(40) }).parse(await c.req.json());
  const res = await air.ai.chat(c, { model: "fast", messages, stream: true });
  return new Response(res.body, { headers: { "content-type": "text/event-stream" } });
});
```

`model` is one of `fast | balanced | deep`, never a vendor name. The daily cap is the owner's; when the gateway returns 429 the app says "The agent is resting today." Streaming renders progressively; under reduced motion the bubble fills without the caret.

### Landing funnel (`landing` template)

One promise, one proof, one tap. Components: `arlan/swing-type` (the hero type, one lite motion), `fancy/vertical-cut-reveal` for the headline under it, `fancy/simple-marquee` for a strip of proof chips, `arlan/squircle` around the single CTA card. The full-surface variant may swap the hero for `arlan/rush-type` (non-lite, WebGL1) — then `useLite()` must fall back to `arlan/swing-type`, never to nothing.

```tsx
const lite = useLite();
<main className="app">
  <section className="panel hero" aria-label="hero">
    {lite ? <SwingTypeCard /> : <RushTypeCard />}      {/* one hero motion */}
    <p className="kicker">October tour</p>
    <h1 data-test="headline"><VerticalCutReveal splitBy="words">Twelve cities, one van</VerticalCutReveal></h1>
  </section>
  <Squircle radius={24} smoothing={0.8} className="cta">
    <a data-test="cta" className="cta-link" href="https://dice.fm/">Get tickets</a>
  </Squircle>
  <SimpleMarquee baseVelocity={1.5}><span className="chip">Sold out · Berlin</span> …</SimpleMarquee>
</main>
```

The CTA is a real `<a href>` (the runner asserts `expectHref`), never a button that navigates in script. Under reduced motion the swing type draws its final frame and the marquee becomes a static row of chips; under lite there is one hero and no marquee velocity. No form, no counter: a funnel that asks for a name is the `tool` shape.

### Product page (`store` template)

A few things to look at, a price on each, one button per thing. Components: `.grid` of `.tile` from the shell, `arlan/squircle` for the featured tile, `beautiful/value-pill` for the price, `fancy/basic-number-ticker` for the bag count (the one hero motion), `fancy/simple-carousel` when the set is ≤ 8 photos.

```tsx
const { state, update } = useAirState<{ bag: string[] }>({ bag: [] });   // resource: bag (owner-writable)
<section className="panel hero">
  <p className="kicker">In the bag</p>
  <NumberTicker from={0} target={state.bag.length} className="count" />
</section>
<section className="grid">
  {products.map((p) => (
    <article key={p.id} className="tile" data-test={`product-${p.id}`}>
      <img src={p.image} alt={p.alt} loading="lazy" />
      <h3>{p.name}</h3>
      <ValuePill tone="accent">{p.price}</ValuePill>
      <button data-test={`buy-${p.id}`} onClick={() => update((s) => ({ bag: [...s.bag, p.id] }))}>Add</button>
    </article>
  ))}
</section>
```

Buy is a stub: it changes the bag and says "Checkout is coming soon"; payments are P2 (§13) and nothing in the app talks to a processor. Images come from the owner's media prefix only. Under reduced motion the ticker shows the final number; under lite the carousel is the grid.

### 2D canvas game (`game-2d` template)

One `<canvas>` sized to the panel at `devicePixelRatio`, a `requestAnimationFrame` loop that reads a `ref`, tap to act. Components: `arlan/typer` for the title line, `fancy/basic-number-ticker` for the score readout, `beautiful/value-pill` for the best score; the game itself is plain Canvas 2D and needs no catalog component. The loop is the screen's one motion.

```tsx
const { state, update, canWrite } = useAirState<{ best: number }>({ best: 0 });   // resource: best
const [score, setScore] = useState(0);
const [running, setRunning] = useState(false);
useEffect(() => {
  if (!running) return;
  let raf = 0, last = performance.now();
  const tick = (now: number) => { step(now - last); last = now; draw(); raf = requestAnimationFrame(tick); };
  raf = requestAnimationFrame(tick);
  return () => cancelAnimationFrame(raf);
}, [running]);
<section className="panel hero">
  <h1><TyperText text="Skyline dash" play="in" /></h1>
  <canvas ref={canvasRef} data-test="stage" onPointerDown={jump} aria-label="game" />
  <div className="row"><span data-test="score"><NumberTicker from={0} target={score} /></span><ValuePill>best {state.best}</ValuePill></div>
  <button data-test="start" onClick={() => setRunning(true)}>Play</button>
</section>
```

The frame budget is 16ms on a phone: draw with rects and arcs, no filters, no per-frame allocations, no `getImageData`. Pause when `document.hidden`. Under reduced motion the canvas still advances (the game is the motion) but every ambient effect — parallax, screen shake, particles — is off; under lite there is one canvas and no backdrop. The high score is the owner's `useAirState` document; guests play and see the board but never write.

### 3D game (`game-3d` template, `three`)

A scene, a camera, a tap. This shape is not `lite`: WebGL and a renderer are the point. Components: `fancy/scramble-in` for the title, `fancy/basic-number-ticker` for the score; the scene comes from `three` once it is vendored. **`three` is not in `vendor/sbom.json` yet (MC5):** `import * as THREE from "three"` is a hard `foreign-import` finding today, so the recipe and the scaffold ship a Canvas 2D poster — a projected wireframe drawn by hand — under lite, and that poster is the whole game until the tarball lands. Keep the `three` code behind one `Scene` component so swapping the poster for the renderer is one file.

```tsx
const lite = useLite();
const reduced = useReducedMotion();
<section className="panel hero">
  <h1 data-test="title"><ScrambleIn text="Orbit" autoStart /></h1>
  {lite || reduced ? <Poster data-test="poster" /> : <Scene onScore={setScore} />}   {/* Poster: Canvas 2D still frame */}
  <p className="kicker">Score</p>
  <NumberTicker from={0} target={score} className="count" data-test="score" />
  <button data-test="start" onClick={start}>Start</button>
</section>
```

`air.json` says `surface.lite: false`; the weight (~150 KiB gz for the `three` core) fits the 1 MiB hard budget, not the 300 KiB lite budget. The `libraries/metal-fx` rule applies: under lite, reduced motion or no WebGL the app renders one complete still frame — never a black canvas, never a spinner. Pause the loop when `document.hidden`; dispose geometries on unmount; no postprocessing, no shadows, one light.

### Templates

Runnable scaffolds the Planner copies and edits (`templates/<id>/`: `air.json`, `src/main.tsx`, `src/app.css`, `goal.template.md`, `README.md`). Each builds with zero hard findings; every one but `game-3d` fits the lite budget. Start from the template, keep its locked tests, and change copy before structure.

| Template | What it is | Kit components | Tests | Lite |
| --- | --- | --- | --- | --- |
| `game-2d` | A one-tap Canvas 2D runner: a canvas, a score in tenths of a second, an owner-kept best via useAirState, Play/Jump buttons. | `arlan/typer`, `beautiful/foundation`, `beautiful/value-pill`, `fancy/basic-number-ticker` | 3 (2 locked) | yes |
| `game-3d` | A tap-timing game around a spinning cube: a Canvas 2D stand-in scene today (three is not vendored yet), a still poster under lite and reduced motion, score and status chips. | `fancy/scramble-in`, `fancy/basic-number-ticker` | 3 (2 locked) | no |
| `landing` | A one-screen landing funnel: swing-type hero, headline, one CTA link, a strip of proof chips. | `arlan/swing-type`, `arlan/squircle`, `fancy/vertical-cut-reveal`, `fancy/simple-marquee` | 3 (2 locked) | yes |
| `page` | A single reading page: shutter-type title card, a lede with one highlighted phrase, a few sections and a list of link rows. | `arlan/shutter-type`, `fancy/text-highlighter` | 3 (2 locked) | yes |
| `store` | A product page: a bag counter, one featured squircle tile and a grid of tiles with a price pill and an Add button each; Buy is a stub that files nothing (payments are P2). | `arlan/squircle`, `beautiful/foundation`, `beautiful/value-pill`, `fancy/basic-number-ticker` | 3 (2 locked) | yes |
| `tool` | An owner utility: a checklist in the owner's useAirState document with an add row, checkboxes, a clear-done chip and a count pill; guests read and are told "Guests are read-only." on a write. | `arlan/typer`, `beautiful/foundation`, `beautiful/value-pill` | 3 (2 locked) | yes |

## 3. Catalog

One line per component: `path` is the reference to open, `lite` whether it may be used on a lite surface, `weight` gzipped KiB it adds (JS+CSS, own code), `tier` the license tier (only A ships in source), `touch` whether it works without hover, `motion` what happens under `prefers-reduced-motion` (static: a complete still frame; reduced: shorter; none: no motion to begin with; n/a: not animated).

### Text and typography

<components>
<aicss/code-block path="kit/aicss/code-block/ref.md" lite="true" weight="1.6kb" tier="A" touch="true" motion="n/a">Code Block — Monospace block with a copy affordance; no highlighter dependency. Tags: code, chat, text</aicss/code-block>
<aicss/streaming-text path="kit/aicss/streaming-text/ref.md" lite="true" weight="0.8kb" tier="A" touch="true" motion="static">Streaming Text — Token-by-token reveal of an incoming reply. Tags: ai, chat, text, streaming</aicss/streaming-text>
<aicss/text-response path="kit/aicss/text-response/ref.md" lite="true" weight="0.6kb" tier="A" touch="true" motion="n/a">Text Response — A finished agent reply with light markdown-ish structure. Tags: ai, chat, text</aicss/text-response>
<arlan/rush-type path="kit/arlan/rush-type/ref.md" lite="false" weight="6.6kb" tier="A" touch="true" motion="static">Rush Type — One lowercase word rests, then blasts twelve times taller and tears into green and violet streaks from speed alone, resolving to plain white at rest; the next word falls out of the blur. Non-lite: WebGL1. Tags: text, motion, hero, webgl</arlan/rush-type>
<arlan/shutter-type path="kit/arlan/shutter-type/ref.md" lite="true" weight="3.8kb" tier="A" touch="true" motion="static">Shutter Type — Two lines of type seen through a rolling shutter: stripes lag the ink so a word tears into bands only while it moves and is whole at rest. One hero motion for a title card; the phrases are props. Tags: text, motion, hero, canvas</arlan/shutter-type>
<arlan/swing-type path="kit/arlan/swing-type/ref.md" lite="true" weight="3.1kb" tier="A" touch="true" motion="static">Swing Type — Enormous coloured letters on a pendulum, each turning edge-on as it passes so only two or three read at once and the word never assembles; the word changes by turning. A hero for a title screen, never body copy. Tags: text, motion, hero, canvas</arlan/swing-type>
<arlan/typer path="kit/arlan/typer/ref.md" lite="true" weight="2.7kb" tier="A" touch="true" motion="static">Typer — A headline that types in instead of fading: a wave runs along the line and each letter flickers through pill, highlight and outline, then lands as text. Pick it over fancy/typewriter when the line must also type away. Tags: text, motion, hero, status</arlan/typer>
<beautiful/code-block path="kit/beautiful/code-block/ref.md" lite="true" weight="2.4kb" tier="A" touch="true" motion="n/a">Code Block — Code block with line numbers and copy. aicss/code-block is the lighter default. Tags: code, text</beautiful/code-block>
<beautiful/selection-actions path="kit/beautiful/selection-actions/ref.md" lite="true" weight="4.2kb" tier="A" touch="true" motion="reduced">Selection Actions — Floating action bar for a text selection (rewrite, shorten, ask). Tags: actions, menu, text, ai</beautiful/selection-actions>
<beautiful/stream-text path="kit/beautiful/stream-text/ref.md" lite="true" weight="0.6kb" tier="A" touch="true" motion="static">Stream Text (atom) — Word-stream reveal used by selection-actions. Tags: text, streaming, atom, helper</beautiful/stream-text>
<beautiful/streaming-text path="kit/beautiful/streaming-text/ref.md" lite="true" weight="2.6kb" tier="A" touch="true" motion="static">Streaming Text — Streaming reply with inline source chips. For plain streaming use aicss/streaming-text. Tags: ai, chat, text, streaming, sources</beautiful/streaming-text>
<fancy/breathing-text path="kit/fancy/breathing-text/ref.md" lite="true" weight="0.8kb" tier="A" touch="true" motion="static">Breathing Text — A single word whose weight/width breathes slowly. Ambient only; never for status. Tags: text, motion, ambient</fancy/breathing-text>
<fancy/letter-3d-swap path="kit/fancy/letter-3d-swap/ref.md" lite="true" weight="1.6kb" tier="A" touch="false" motion="none">Letter 3D Swap — Letters flip on a 3D axis on hover. Hover-only; heavier than the 2D swaps. Tags: text, motion, hover, 3d</fancy/letter-3d-swap>
<fancy/letter-swap-forward-anim path="kit/fancy/letter-swap-forward-anim/ref.md" lite="true" weight="0.7kb" tier="A" touch="false" motion="none">Letter Swap (forward) — Nav/button label whose letters roll upward on hover. Hover-only. Tags: text, motion, hover, nav</fancy/letter-swap-forward-anim>
<fancy/letter-swap-pingpong-anim path="kit/fancy/letter-swap-pingpong-anim/ref.md" lite="true" weight="0.8kb" tier="A" touch="false" motion="none">Letter Swap (ping-pong) — Like Letter Swap (forward) but rolls back on hover-out. Hover-only. Tags: text, motion, hover, nav</fancy/letter-swap-pingpong-anim>
<fancy/media-between-text path="kit/fancy/media-between-text/ref.md" lite="true" weight="1.0kb" tier="A" touch="true" motion="static">Media Between Text — Headline where an image expands between two words on hover/view. Tags: text, media, motion, hero</fancy/media-between-text>
<fancy/random-letter-swap-forward-anim path="kit/fancy/random-letter-swap-forward-anim/ref.md" lite="true" weight="0.9kb" tier="A" touch="false" motion="none">Random Letter Swap (forward) — Letter Swap with randomized per-letter direction. Hover-only. Tags: text, motion, hover, nav</fancy/random-letter-swap-forward-anim>
<fancy/random-letter-swap-pingpong-anim path="kit/fancy/random-letter-swap-pingpong-anim/ref.md" lite="true" weight="0.9kb" tier="A" touch="false" motion="none">Random Letter Swap (ping-pong) — Randomized Letter Swap that rolls back on hover-out. Hover-only. Tags: text, motion, hover, nav</fancy/random-letter-swap-pingpong-anim>
<fancy/scramble-hover path="kit/fancy/scramble-hover/ref.md" lite="true" weight="1.2kb" tier="A" touch="false" motion="none">Scramble Hover — A nav label that scrambles on hover/focus. Hover-only flourish; no touch equivalent. Tags: text, motion, hover</fancy/scramble-hover>
<fancy/scramble-in path="kit/fancy/scramble-in/ref.md" lite="true" weight="0.8kb" tier="A" touch="true" motion="static">Scramble In — Reveal a short label by unscrambling it once on mount or on view. Tags: text, motion, reveal</fancy/scramble-in>
<fancy/text-along-path path="kit/fancy/text-along-path/ref.md" lite="true" weight="1.0kb" tier="A" touch="true" motion="static">Text Along Path — Text set along an SVG path (circles, arcs). Decorative headers only. Tags: text, svg, motion, hero</fancy/text-along-path>
<fancy/text-highlighter path="kit/fancy/text-highlighter/ref.md" lite="true" weight="1.0kb" tier="A" touch="true" motion="static">Text Highlighter — Marker-style highlight that sweeps across a phrase when it scrolls into view. Tags: text, motion, emphasis</fancy/text-highlighter>
<fancy/text-rotate path="kit/fancy/text-rotate/ref.md" lite="true" weight="1.5kb" tier="A" touch="true" motion="static">Text Rotate — One slot in a headline that cycles through a short list of words. Tags: text, motion, hero</fancy/text-rotate>
<fancy/typewriter path="kit/fancy/typewriter/ref.md" lite="true" weight="0.9kb" tier="A" touch="true" motion="static">Typewriter — A headline or status line that should feel typed by someone; not for body copy. Tags: text, motion, hero, status</fancy/typewriter>
<fancy/underline-center path="kit/fancy/underline-center/ref.md" lite="true" weight="0.9kb" tier="A" touch="true" motion="reduced">Underline (center) — Inline link whose underline grows from the center on hover/focus. Tags: text, link, hover, motion</fancy/underline-center>
<fancy/underline-comes-in-goes-out path="kit/fancy/underline-comes-in-goes-out/ref.md" lite="true" weight="0.9kb" tier="A" touch="true" motion="reduced">Underline (comes in, goes out) — Inline link underline that enters from one side and exits the other. Tags: text, link, hover, motion</fancy/underline-comes-in-goes-out>
<fancy/underline-goes-out-comes-in path="kit/fancy/underline-goes-out-comes-in/ref.md" lite="true" weight="0.9kb" tier="A" touch="true" motion="reduced">Underline (goes out, comes in) — Inline link underline that leaves before re-entering on hover. Tags: text, link, hover, motion</fancy/underline-goes-out-comes-in>
<fancy/underline-to-background path="kit/fancy/underline-to-background/ref.md" lite="true" weight="0.9kb" tier="A" touch="true" motion="reduced">Underline to Background — Link underline that swells into a highlight on hover; good for a single CTA link. Tags: text, link, hover, motion</fancy/underline-to-background>
<fancy/vertical-cut-reveal path="kit/fancy/vertical-cut-reveal/ref.md" lite="true" weight="1.4kb" tier="A" touch="true" motion="static">Vertical Cut Reveal — Reveal a headline line by line (or word by word) with a clipped upward slide. Tags: text, motion, reveal, hero</fancy/vertical-cut-reveal>
</components>

### Status, thinking, streaming

<components>
<aicss/orbs path="kit/aicss/orbs/ref.md" lite="true" weight="5.6kb" tier="A" touch="true" motion="static">Orb — Ambient CSS orb as the agent's presence. Use thinking-orbs when you need named states. Tags: ai, status, avatar, ambient</aicss/orbs>
<aicss/thinking-reasoning path="kit/aicss/thinking-reasoning/ref.md" lite="true" weight="2.6kb" tier="A" touch="true" motion="reduced">Thinking Reasoning — Collapsible reasoning trace under a thinking indicator. Tags: ai, status, disclosure, thinking</aicss/thinking-reasoning>
<aicss/thinking-state path="kit/aicss/thinking-state/ref.md" lite="true" weight="0.5kb" tier="A" touch="true" motion="reduced">Thinking State — The agent is working and you have a verb for it. Default for the status/thinking recipe. Tags: ai, status, loading, thinking</aicss/thinking-state>
<aicss/todo-list path="kit/aicss/todo-list/ref.md" lite="true" weight="3.2kb" tier="A" touch="true" motion="reduced">Todo List — Agent task checklist with pending/active/done rows. Tags: list, tasks, status</aicss/todo-list>
<beautiful/loading-state path="kit/beautiful/loading-state/ref.md" lite="true" weight="1.3kb" tier="A" touch="true" motion="static">Loading State — Full-panel waiting state with a message; pass your own media or none. Tags: loading, status, ai</beautiful/loading-state>
<beautiful/shimmer path="kit/beautiful/shimmer/ref.md" lite="true" weight="0.4kb" tier="A" touch="true" motion="static">Shimmer (atom) — Text shimmer used by selection-actions. Tags: loading, atom, helper</beautiful/shimmer>
<beautiful/task-rows path="kit/beautiful/task-rows/ref.md" lite="true" weight="2.2kb" tier="A" touch="true" motion="reduced">Task Rows — Task rows with progress and owners; heavier than aicss/todo-list. Tags: list, tasks, status</beautiful/task-rows>
<beautiful/thinking path="kit/beautiful/thinking/ref.md" lite="true" weight="2.6kb" tier="A" touch="true" motion="reduced">Thinking — Thinking indicator that lists steps and sources as they arrive. Tags: ai, status, thinking, sources</beautiful/thinking>
<beautiful/tool-chips path="kit/beautiful/tool-chips/ref.md" lite="true" weight="3.1kb" tier="A" touch="true" motion="reduced">Tool Chips — Row of tool-call chips with running/done states. Tags: ai, status, chips, tools</beautiful/tool-chips>
<fancy/basic-number-ticker path="kit/fancy/basic-number-ticker/ref.md" lite="true" weight="0.7kb" tier="A" touch="true" motion="static">Number Ticker — A number that counts to its value: RSVP totals, countdowns, prices. Tags: number, motion, hero, status, countdown</fancy/basic-number-ticker>
<libraries/thinking-orbs path="kit/libraries/thinking-orbs/ref.md" lite="true" weight="0.7kb" tier="A" touch="true" motion="static">Thinking Orbs — Nine named agent states (working, searching, solving…) on a 2D canvas. Best 'the agent is doing X' indicator. Tags: ai, status, thinking, canvas</libraries/thinking-orbs>
</components>

### Inputs and actions

<components>
<aicss/agent-input path="kit/aicss/agent-input/ref.md" lite="true" weight="9.5kb" tier="A" touch="true" motion="n/a">Agent Input — Prompt composer with attachments and send. Default composer for the chat recipe. Tags: input, chat, composer</aicss/agent-input>
<aicss/approval-card path="kit/aicss/approval-card/ref.md" lite="true" weight="7.4kb" tier="A" touch="true" motion="n/a">Approval Card — Approve/deny a proposed action with a short summary. Default for the approval recipe. Tags: approval, decision, card, actions</aicss/approval-card>
<arlan/color-depth path="kit/arlan/color-depth/ref.md" lite="true" weight="3.4kb" tier="A" touch="true" motion="n/a">Color Depth — Buttons that feel like real objects: glossy plastic, brushed metal, glass, a soft cushion, neon, a pressed-in key. Pick one material per screen for the primary action; glass needs backdrop-filter, so never under lite. Tags: button, style, material, action</arlan/color-depth>
<arlan/squircle path="kit/arlan/squircle/ref.md" lite="true" weight="1.6kb" tier="A" touch="true" motion="n/a">Squircle — Apple's corner: the curve eases into the flat edge with no kink, so a button or card reads smooth all the way round. Pick it for the one CTA or hero card whose outline matters; rows and chips keep the Air pill radius. Tags: shape, button, card, style</arlan/squircle>
<beautiful/approval-card path="kit/beautiful/approval-card/ref.md" lite="true" weight="4.1kb" tier="A" touch="true" motion="reduced">Approval Card — Rich approval with a glide menu of alternatives. aicss/approval-card is the lighter default. Tags: approval, decision, card, actions</beautiful/approval-card>
<beautiful/button path="kit/beautiful/button/ref.md" lite="true" weight="1.0kb" tier="A" touch="true" motion="n/a">Button (atom) — Beautiful UI button used by its cards and tables. Prefer Air's .row.actions buttons for app chrome. Tags: button, atom, helper</beautiful/button>
<beautiful/chat-composer path="kit/beautiful/chat-composer/ref.md" lite="true" weight="2.1kb" tier="A" touch="true" motion="n/a">Chat Composer — Multi-line composer with mode pills. aicss/agent-input is the lighter default. Tags: input, chat, composer</beautiful/chat-composer>
<beautiful/search path="kit/beautiful/search/ref.md" lite="true" weight="1.7kb" tier="A" touch="true" motion="reduced">Search — Search field with a filtered result list and glide highlight. Tags: search, list, input</beautiful/search>
</components>

### Lists, tables, cards

<components>
<aicss/data-table path="kit/aicss/data-table/ref.md" lite="true" weight="2.6kb" tier="A" touch="true" motion="n/a">Data Table — Small read-only data table inside a reply. For sorting/filtering use beautiful/filter-table. Tags: table, data</aicss/data-table>
<arlan/holo path="kit/arlan/holo/ref.md" lite="true" weight="6.0kb" tier="A" touch="true" motion="static">Holo Card — An ID card on holographic foil: tilt it or turn the phone and the foil catches the light, marks rise on the turned side and the photo flips colour. Pick it for one membership, ticket or pass card, the screen's hero. Tags: card, motion, hero, material</arlan/holo>
<arlan/liquid-ui path="kit/arlan/liquid-ui/ref.md" lite="true" weight="2.9kb" tier="A" touch="true" motion="static">Liquid UI — Two cards fuse and the corner between them bends inward, as if poured together; one knob runs from crisp joints to gooey blobs. Pick it for a 2-4 card cluster read as one poured shape; menus take libraries/liquid-gooey. Tags: motion, card, layout, decor</arlan/liquid-ui>
<beautiful/diff-table path="kit/beautiful/diff-table/ref.md" lite="true" weight="2.9kb" tier="A" touch="true" motion="n/a">Diff Table — Before/after rows with accept controls. Tags: table, diff, review</beautiful/diff-table>
<beautiful/entity-chip path="kit/beautiful/entity-chip/ref.md" lite="true" weight="0.5kb" tier="A" touch="true" motion="n/a">Entity Chip (atom) — Small entity chip used by recommendation-card. Tags: chip, atom, helper</beautiful/entity-chip>
<beautiful/filter-table path="kit/beautiful/filter-table/ref.md" lite="true" weight="1.5kb" tier="A" touch="true" motion="n/a">Filter Table — Table with filter chips and sorting. Tags: table, data, filter</beautiful/filter-table>
<beautiful/fine-tune-card path="kit/beautiful/fine-tune-card/ref.md" lite="true" weight="3.1kb" tier="A" touch="true" motion="n/a">Fine-tune Card — Card of sliders/toggles to tune a result before confirming. Tags: card, settings, sliders</beautiful/fine-tune-card>
<beautiful/recommendation-card path="kit/beautiful/recommendation-card/ref.md" lite="true" weight="2.5kb" tier="A" touch="true" motion="n/a">Recommendation Card — One recommendation with entity chips, a value and accept/skip. Tags: card, decision, actions</beautiful/recommendation-card>
<beautiful/records-table path="kit/beautiful/records-table/ref.md" lite="true" weight="14kb" tier="A" touch="true" motion="n/a">Records Table — Spreadsheet-like records grid with sticky first column and column selection. Heavy; one per app. Tags: table, data, records</beautiful/records-table>
<beautiful/value-pill path="kit/beautiful/value-pill/ref.md" lite="true" weight="0.6kb" tier="A" touch="true" motion="n/a">Value Pill (atom) — Numeric pill used by recommendation-card. Tags: pill, atom, helper</beautiful/value-pill>
<libraries/border-beam path="kit/libraries/border-beam/ref.md" lite="true" weight="0.6kb" tier="A" touch="true" motion="static">Border Beam — Animated beam around one card that deserves attention (the pending approval, the live item). Tags: decor, motion, card, focus</libraries/border-beam>
</components>

### Layout, media, motion blocks

<components>
<air path="kit/air/ref.md" lite="true" weight="5.9kb" tier="A" touch="true" motion="n/a">Air shell, theme and hooks — Always: theme.css + shell.css give every screen its tokens and vocabulary; index.ts gives useLite(), useReducedMotion(), useTheme(), useAirState(). Tags: air, layout, hooks, state, theme</air>
<arlan/ghosty-reveal path="kit/arlan/ghosty-reveal/ref.md" lite="true" weight="1.0kb" tier="A" touch="true" motion="static">Ghosty Reveal — A photo bleeds in through a soft, cloudy edge, as if forming out of fog. Pick it for the one hero image or a confirmation reveal; the owner bundles the cloud mask, and text takes fancy/vertical-cut-reveal. Tags: reveal, media, motion, hero, gallery</arlan/ghosty-reveal>
<fancy/animated-gradient-with-svg path="kit/fancy/animated-gradient-with-svg/ref.md" lite="true" weight="0.9kb" tier="A" touch="true" motion="static">Animated Gradient (SVG) — Soft moving color blobs behind a panel. Uses blur; pixel theme and lite render it flat. Tags: background, motion, ambient</fancy/animated-gradient-with-svg>
<fancy/circling-elements path="kit/fancy/circling-elements/ref.md" lite="true" weight="0.7kb" tier="A" touch="true" motion="static">Circling Elements — Small items orbiting a center: avatars around a host, badges around a logo. Tags: motion, ambient, decor, gallery</fancy/circling-elements>
<fancy/css-box path="kit/fancy/css-box/ref.md" lite="true" weight="1.3kb" tier="A" touch="true" motion="static">CSS Box — A CSS 3D cube with a face per side; rotate to show six images or words. Tags: 3d, motion, hero, gallery</fancy/css-box>
<fancy/float path="kit/fancy/float/ref.md" lite="true" weight="0.7kb" tier="A" touch="true" motion="static">Float — Gentle idle float for a hero image or badge. One per screen. Tags: motion, ambient, decor</fancy/float>
<fancy/screensaver path="kit/fancy/screensaver/ref.md" lite="true" weight="0.9kb" tier="A" touch="true" motion="static">Screensaver — DVD-logo bounce for a badge inside a container. Ambient only. Tags: motion, ambient, decor</fancy/screensaver>
<fancy/simple-carousel path="kit/fancy/simple-carousel/ref.md" lite="true" weight="1.4kb" tier="A" touch="true" motion="reduced">Simple Carousel — Swipeable horizontal carousel with snap; the gallery recipe default. Tags: layout, gallery, touch</fancy/simple-carousel>
<fancy/simple-marquee path="kit/fancy/simple-marquee/ref.md" lite="true" weight="1.4kb" tier="A" touch="true" motion="static">Simple Marquee — Endless horizontal strip of logos, photos or words. Pauses on reduced motion. Tags: layout, motion, gallery, ambient</fancy/simple-marquee>
<fancy/stacking-cards path="kit/fancy/stacking-cards/ref.md" lite="true" weight="0.8kb" tier="A" touch="true" motion="reduced">Stacking Cards — Sections that stack as the page scrolls. Needs at least three cards to read. Tags: layout, scroll, cards</fancy/stacking-cards>
</components>

### Filters, backgrounds, materials

<components>
<beautiful/foundation path="kit/beautiful/foundation/ref.md" lite="true" weight="2.5kb" tier="A" touch="true" motion="n/a">Beautiful UI foundation — Shared CSS for every beautiful/* component: token bridge onto Air, keyframes, primitive classes. Import once. Tags: style, tokens, helper</beautiful/foundation>
<fancy/gooey-svg-filter path="kit/fancy/gooey-svg-filter/ref.md" lite="true" weight="0.5kb" tier="A" touch="true" motion="n/a">Gooey SVG Filter — Provides an SVG goo filter by id for blobby merges. Pair with liquid-gooey only when that package is too heavy. Tags: filter, svg, helper</fancy/gooey-svg-filter>
<fancy/pixel-trail path="kit/fancy/pixel-trail/ref.md" lite="true" weight="1.1kb" tier="A" touch="true" motion="none">Pixel Trail — Pixels light up under the pointer/finger. Decorative background for one screen. Tags: background, pointer, motion</fancy/pixel-trail>
<fancy/pixelate-svg-filter path="kit/fancy/pixelate-svg-filter/ref.md" lite="true" weight="0.7kb" tier="A" touch="true" motion="n/a">Pixelate SVG Filter — Provides an SVG pixelation filter by id; apply to an image that resolves on load. Tags: filter, svg, helper</fancy/pixelate-svg-filter>
<libraries/metal-fx path="kit/libraries/metal-fx/ref.md" lite="false" weight="0.8kb" tier="A" touch="true" motion="static">Metal FX — Liquid-metal surface behind a hero word. Non-lite only; renders a flat plate when WebGL is off. Tags: shader, webgl, decor, hero</libraries/metal-fx>
</components>

### Foundations and helpers

<components>
<beautiful/context-cards path="kit/beautiful/context-cards/ref.md" lite="true" weight="1.4kb" tier="A" touch="true" motion="reduced">Context Cards — Small stack of context/source cards the agent used. Tags: cards, context, sources</beautiful/context-cards>
<beautiful/flowchart path="kit/beautiful/flowchart/ref.md" lite="true" weight="3.8kb" tier="A" touch="true" motion="reduced">Flowchart — Simple node/edge flowchart for a plan or pipeline. Tags: diagram, svg, plan</beautiful/flowchart>
<beautiful/glide-menu path="kit/beautiful/glide-menu/ref.md" lite="true" weight="0.7kb" tier="A" touch="true" motion="reduced">Glide Menu (atom) — Sliding highlight menu used inside approval-card, records-table, search. Tags: menu, atom, helper</beautiful/glide-menu>
<fancy/elastic-line path="kit/fancy/elastic-line/ref.md" lite="false" weight="1.3kb" tier="A" touch="false" motion="static">Elastic Line — A divider line that stretches toward the pointer and snaps back. Non-lite (physics). Tags: physics, pointer, motion, divider</fancy/elastic-line>
<libraries/liquid-gooey path="kit/libraries/liquid-gooey/ref.md" lite="true" weight="0.6kb" tier="A" touch="true" motion="reduced">Liquid Gooey — Items that merge like liquid: a plus-menu that splits into droplets, a tab thumb that trails. Tags: motion, menu, decor, svg</libraries/liquid-gooey>
</components>

### Sources

| Source | Name | License | Tier | Pin |
| --- | --- | --- | --- | --- |
| `fancy` | Fancy Components (fancycomponents.dev) | MIT | A | danielpetho/fancy@f9f62c61207b |
| `aicss` | AI CSS (free tier) (aicss.dev) | MIT | A | kvnkld/aicss@4556a918fd8c |
| `beautiful` | Beautiful UI (beautifului.dev) | MIT | A | captured 2026-09-04 |
| `libraries` | libraries.dev (libraries.dev) | MIT | A | Jakubantalik/Libraries.dev@aea4c00db4b7 |
| `arlan` | arlan.me vault (arlan.me/vault) | MIT | A | captured 2026-09-04 |

## 4. Effects vocabulary

Owners name looks, not components: "make the title glitch", "an aurora behind it", "a card that tilts", "a trail behind my finger". This section maps 193 named effects from two public catalogues — the React Bits public index (`reactbits.dev/llms.txt`, MIT + Commons Clause, Tier B) and the arlan.me vault (MIT, Tier A, including the three studies harvested from owner briefs) — onto what the Kit does for each. The data is `prompts/src/effects.json` (annotated for the Kit on 2026-09-17, each entry audited by an independent verifier; evidence in `evidence/catalogues/`); this section is rendered from it by `scripts/lib/effects.ts`, never hand-edited.

Read a line as: **Name** · how · flags — what it is and, where the verb needs it, what to do. `how` is one of:

- `@kit/<id>` — a Tier A component already gives this look (or its touch-native counterpart, and the line says so). Import it; do not rebuild it. The flags are that component's measured lite / touch / reduced-motion behaviour from §3.
- `@kit/restricted/<name>` — one of the thirteen React Bits backgrounds compiled from the private Tier B artifact. Non-lite; the surface must not be `lite`, and you supply the still frame under reduced motion.
- `pack` — worth adding to the Tier B artifact (a shader, physics or 3D piece that is hard to rebuild well) but not in it yet. Do not build it; use the packed background the line names, or decline when it names none.
- `build` — implement an original from the one-line look and the `Build:` sentence that follows it. Never copy upstream source; the line is the brief. The flags describe the original you will write (renderer, lite, touch, still frame), not the catalogued source.
- `no` — not for a mini-app; the line names the rule it breaks and, when a Kit component gives the same read on a phone, the component to use instead; `Instead: none` means there is no substitute, and you say so to the owner.

Text and card effects split between `@kit` and `build`; backgrounds are where `@kit/restricted` and `pack` live; cursor-driven and scroll-driven effects are `no`. Tags feed the same groups as the catalog; "fits" names the recipes and templates the effect suits.

### Text

32 effects: 10 already in the Kit, 0 packed Tier B, 0 to pack, 14 to build, 8 not for a mini-app.

- **Circular Text** · `@kit/fancy/text-along-path` · lite ✓ · touch ✓ · motion static — Short label set around a circle with a slow spin; a decorative badge or stamp beside a hero, aria-hidden. Tags: text, decor, motion. Fits: landing, page.
- **Count Up** · `@kit/fancy/basic-number-ticker` · lite ✓ · touch ✓ · motion static — A number that counts up to its value with separators and decimals: totals, prices, guest counts. Tags: text, data, status, motion. Fits: countdown-ticker-hero, rsvp-guest-list, store, landing, tool.
- **Curved Loop** · `@kit/fancy/text-along-path` · lite ✓ · touch ✓ · motion static — Phrase that loops endlessly along a curve, a decorative band under a hero; the Kit's text-along-path runs the loop on its own, and the drag-to-scrub is dropped. Tags: text, marquee, decor, motion. Fits: landing, page.
- **Decrypted Text** · `@kit/fancy/scramble-in` · lite ✓ · touch ✓ · motion static — Label that cycles random glyphs before resolving to real text, once on mount or on view; short labels only, a paragraph scrambling is noise. Tags: text, motion, hero. Fits: landing, game-2d.
- **Rotating Text** · `@kit/fancy/text-rotate` · lite ✓ · touch ✓ · motion static — Slot in a headline that cycles through a short list of phrases with a flip transition; a finite list, one per screen. Tags: text, motion, hero. Fits: landing-funnel, landing.
- **Shiny Text** · `@kit/beautiful/shimmer` · lite ✓ · touch ✓ · motion static — A metallic sheen sweeps across a word or label; a thinking line or one hero word, never body copy or more than one element per screen. Tags: text, loading, motion, style. Fits: status-thinking, landing-funnel, landing, tool, page.
- **Shuffle** · `@kit/fancy/scramble-in` · lite ✓ · touch ✓ · motion static — Characters shuffle before settling into a short label, once on mount: a kicker, a price, a status word; the Kit's scramble-in resolves random glyphs rather than sliding character strips. Tags: text, motion, hero. Fits: landing-funnel, countdown-ticker-hero, landing, page.
- **Split Text** · `@kit/fancy/vertical-cut-reveal` · lite ✓ · touch ✓ · motion static — A headline whose words or letters enter with a stagger, once on mount as the screen's hero motion; word-level stagger is enough at 390px. Tags: text, motion, hero. Fits: landing-funnel, form-confirmation, landing, store, page.
- **Text Loop** · `@kit/fancy/text-along-path` · lite ✓ · touch ✓ · motion static — Text that flows endlessly along an SVG arc or circle as a decorative header; the Kit's text-along-path runs the marquee on its own, kept inside the 390px column. Tags: text, marquee, motion, decor. Fits: landing-funnel, gallery, landing, store, page.
- **Text Type** · `@kit/fancy/typewriter` · lite ✓ · touch ✓ · motion static — A typewriter that types a headline or status line with a blinking caret and adjustable cadence; arlan/typer when the line must also type away, never body copy. Tags: text, motion, status, hero. Fits: status-thinking, chat-with-agent, landing-funnel, landing, tool, page.
- **ASCII Text** · `build` · canvas2d · lite ✓ · touch ✓ · motion static — Hero word drawn as a field of animated ASCII glyphs for a retro terminal feel; a game or lo-fi title, one per screen, still glyphs under reduced motion. Build: Draw the word to an offscreen 2D canvas, sample luminance per cell and map it to a glyph ramp drawn in the bundled monospace, offsetting rows with a slow sine wave. Keep the real word in the DOM and the glyph canvas aria-hidden. Tags: text, hero, decor, motion, game. Fits: game-2d, landing.
- **Blur Text** · `build` · css · lite ✓ · touch ✓ · motion static — Headline or confirmation line that resolves from soft blur to crisp, word by word; a one-shot mount reveal for the hero, never for body copy. Build: Give each word span one keyframe from blur(8px) plus a small translateY to none, staggered by about 70ms, run once on mount. Keep it to a single line of words. Tags: text, motion, hero. Fits: form-confirmation, landing-funnel, landing, page, tool.
- **Depth Text** · `build` · css · lite ✓ · touch ✓ · motion static — Extruded, layered type that reads as a solid block lifted off the page; a static hero headline style with a short settle on mount, the pointer parallax dropped. Build: Build the extrusion from six to ten stacked text-shadow offsets stepping one pixel each in a darker ink, and on mount slide the stack from zero to full depth in under 300ms with an ease-out. Tags: text, style, hero. Fits: landing-funnel, landing, page.
- **Echo Text** · `build` · css · lite ✓ · touch ✓ · motion static — Ghosted copies converge and settle into one hero word; a single mount entrance for a title, static word under reduced motion. Build: Render three or four aria-hidden copies of the word offset by translate and faded, and collapse them onto the real word in under 600ms with a small stagger. Run it once on mount. Tags: text, motion, hero. Fits: landing-funnel, landing.
- **Falling Text** · `build` · css · lite ✓ · touch ✓ · motion static — Letters drop and bounce into a headline; a playful entrance for a game or party app, one per screen, letters already landed under reduced motion. Build: Split the headline into letter spans and drop each from above with a translateY keyframe on an overshoot ease, staggered by a few tens of ms so the line lands within a second. Skip the piling and dragging. Tags: text, motion, hero, game. Fits: game-2d, landing.
- **Fold Text** · `build` · css · lite ✓ · touch ✓ · motion static — Lines unfold into place like creased paper; a one-shot hero entrance for a two or three line title, flat lines under reduced motion. Build: Put perspective on the parent and give each line one keyframe from rotateX(-90deg) to 0 with transform-origin alternating top and bottom, staggered per line and finished in under 600ms. Tags: text, motion, hero. Fits: landing-funnel, landing, page.
- **Fuzzy Text** · `build` · canvas2d · lite ✓ · touch ✓ · motion static — Word that vibrates with a fuzzy edge on a small canvas; a decorative game title or lo-fi hero, aria-hidden, crisp text under reduced motion. Build: Draw the word once to an offscreen canvas, then each frame copy its horizontal row slices back with a small random x jitter; let a tap spike the jitter briefly. Keep the real text in the DOM and the canvas aria-hidden. Tags: text, motion, decor, game. Fits: game-2d.
- **Glitch Text** · `build` · css · lite ✓ · touch ✓ · motion static — RGB-split jitter over a hero word or game title; a short finite glitch burst, never on status or body text, still under reduced motion. Build: Layer two pseudo-element copies of the word with clip-path slices, a translateX offset and coloured text-shadow, and run a short keyframe burst once on mount or every few seconds. Keep the copies aria-hidden. Tags: text, motion, decor, game. Fits: game-2d, landing.
- **Gradient Text** · `build` · css · lite ✓ · touch ✓ · motion static — Headline with a colour gradient sweeping across the glyphs; one hero word or CTA per screen, gradient held still under reduced motion. Build: Paint a wide gradient of var(--accent) stops behind the headline with background-clip: text and slide background-position across it in one looping keyframe, so it survives both themes. Tags: text, style, hero, motion. Fits: landing-funnel, product-page, landing, store.
- **Masked Heading** · `build` · css · lite ✓ · touch ✓ · motion static — Large headline with a drifting gradient or a bundled image showing through the glyphs, revealed word by word; a landing hero, flat gradient under lite or reduced motion. Build: Put @kit/fancy/animated-gradient-with-svg behind the headline, clip it into the glyphs with background-clip: text, and stagger the words in with @kit/fancy/vertical-cut-reveal. Take any image from the bundle, never a URL. Tags: text, hero, motion, media. Fits: landing-funnel, landing.
- **Particle Text** · `build` · canvas2d · lite ✓ · touch ✓ · motion static — Hero word that assembles from drifting particles and scatters again on tap; a Canvas 2D moment for a reveal or game title, assembled text under reduced motion. Build: Sample glyph pixels of the word from an offscreen canvas into target points and ease a capped set of particles toward them each frame; a tap scatters them and they reform. Keep the real word in the DOM and the canvas aria-hidden. Tags: text, motion, hero, game. Fits: landing-funnel, landing, game-2d.
- **Split Flap Text** · `build` · css · lite ✓ · touch ✓ · motion static — A departure board that clacks through phrases tile by tile; the hero for a countdown, a guest count or an event status when fancy/text-rotate feels too soft. Build: Set fixed-width tiles in var(--font-ui), each a top and bottom half that flips with rotateX in under 300ms, staggered so a phrase settles in about a second. Cap at roughly 12 tiles and swap glyphs instantly under reduced motion. Tags: text, motion, hero, status. Fits: countdown-ticker-hero, rsvp-guest-list, landing-funnel, landing, tool, page.
- **Stroke Text** · `build` · svg · lite ✓ · touch ✓ · motion static — Outlined letters draw themselves on and then fill; a one-shot hero for a confirmation or a launch headline on a screen with no other motion. Build: Set the words as inline SVG text in var(--font-body) stroked with var(--ink), animate stroke-dasharray and dashoffset over about 1.2s, then fade fill-opacity to 1. Never outline glyphs to paths; one to three words. Tags: text, motion, hero. Fits: form-confirmation, landing-funnel, landing, page.
- **True Focus** · `build` · css · lite ✓ · touch ✓ · motion static — A focus frame steps across a short phrase, sharpening one word at a time; a finite hero for a three or four word promise on a landing screen, never for copy people must read. Build: Wrap each word in a span, blur(4px) the inactive words (opacity .5 under lite) and move four corner brackets between word rects with transform, about 700ms per step, parking on the last word so the run is finite. Tags: text, motion, hero, filter. Fits: landing-funnel, landing, page.
- **Scrambled Text** · `no` — Text that distorts and scrambles around the cursor as it passes over the letters, settling back once it leaves; `no`: pointer-driven with no tap equivalent (no pointer on touch). Instead: `@kit/fancy/scramble-in`.
- **Scroll Float** · `no` — Text that drifts upward and parallax-shifts as the page scrolls; `no`: moves on scroll (nothing moves on scroll inside the webview). Instead: `@kit/fancy/float`.
- **Scroll Reveal** · `no` — Words unblur and rise into place as they scroll into view; `no`: moves on scroll (nothing moves on scroll inside the webview). Instead: `@kit/fancy/vertical-cut-reveal`.
- **Scroll Velocity** · `no` — A word marquee whose speed and skew follow how fast the page is scrolling; `no`: moves on scroll (nothing moves on scroll inside the webview). Instead: `@kit/fancy/simple-marquee`.
- **Text Cursor** · `no` — Copies of a word spawn and trail behind the cursor as it moves, fading out behind it; `no`: pointer-driven with no tap equivalent (no pointer on touch). Instead: none.
- **Text Pressure** · `no` — Letters swell and squeeze under the pointer through a variable font's weight and width axes; `no`: pointer-driven with no tap equivalent (no pointer on touch). Instead: `@kit/fancy/breathing-text`.
- **Variable Proximity** · `no` — Letter weight and width bend toward the pointer through a variable font and relax as it moves away; `no`: pointer-driven with no tap equivalent (no pointer on touch). Instead: `@kit/fancy/breathing-text`.
- **Warp Text** · `no` — A shader pass bends and refracts a headline around the pointer like glass; arlan/rush-type is the WebGL text hero the Kit allows, non-lite; `no`: pointer-driven with no tap equivalent (no pointer on touch). Instead: `@kit/arlan/rush-type`.

### Motion, pointer and tap feedback

38 effects: 8 already in the Kit, 0 packed Tier B, 1 to pack, 12 to build, 17 not for a mini-app.

- **Animated Content** · `@kit/air` · lite ✓ · touch ✓ · motion n/a — A block that fades and slides in from a chosen side on mount; Air's --slide-in is that settle motion for a panel's first paint. Mount only, never tied to scroll. Tags: motion, layout, hero. Fits: form-confirmation, approval-card, landing, page, tool.
- **Cursor Grid** · `@kit/fancy/pixel-trail` · lite ✓ · touch ✓ · motion none — A canvas grid whose cells glow under the pointer or finger and pulse on tap; the Kit's pixel-trail is the finger-driven version and drops only the hover falloff at rest. Tags: cursor, background, decor, motion. Fits: landing.
- **Fade Content** · `@kit/air` · lite ✓ · touch ✓ · motion n/a — A block that fades or slides in once; use Air's --slide-in for the first paint on mount, never on scroll into view. Tags: motion, layout. Fits: form-confirmation, approval-card, landing, page, tool.
- **Logo Loop** · `@kit/fancy/simple-marquee` · lite ✓ · touch ✓ · motion static — An endless strip of logos with a seamless repeat; the logos ship in the bundle and the pause on hover is an extra, not the way in. Tags: marquee, gallery, ambient, layout. Fits: gallery, landing-funnel, landing, store.
- **Meta Balls** · `@kit/arlan/liquid-ui` · lite ✓ · touch ✓ · motion static — Soft blobs that merge and split as they drift; the Kit's liquid-ui gives the melting-card version of this without WebGL, and gooey-svg-filter goos any drifting circles. Tags: ambient, decor, webgl, motion, background. Fits: landing.
- **Metallic Paint** · `@kit/libraries/metal-fx` · lite ✗ · touch ✓ · motion static — Liquid chrome shimmer filling a logo or hero word; it draws a flat plate when WebGL, lite or reduced motion says no. Tags: material, webgl, hero, decor. Fits: countdown-ticker-hero, landing-funnel, landing, store.
- **Orbit Images** · `@kit/fancy/circling-elements` · lite ✓ · touch ✓ · motion static — Images that travel around a circle or a custom path; the Kit's circling-elements orbits avatars or badges around a center, and the images ship in the bundle. Tags: gallery, media, ambient, decor, motion. Fits: rsvp-guest-list, gallery, landing, page.
- **Pixel Trail** · `@kit/fancy/pixel-trail` · lite ✓ · touch ✓ · motion none — Squares light up and fade behind the pointer for a retro digital feel; the Kit's pixel-trail is the finger-driven version on a 2D canvas and stays lite. Tags: cursor, background, motion, decor. Fits: landing, game-2d.
- **Laser Flow** · `pack` · three · lite ✗ — A laser beam pours light onto a surface behind a hero title; one per screen as the hero motion of a launch or game landing, a flat gradient plate under lite and reduced motion. Until packed: use `@kit/restricted/light-rays`. Tags: background, webgl, hero, ambient. Fits: countdown-ticker-hero, landing-funnel, landing, game-3d.
- **Click Spark** · `build` · canvas2d · lite ✓ · touch ✓ · motion none — A short burst of sparks where the finger taps; use it once for the confirming tap (RSVP sent, item added), never as ambient decoration. Build: On pointerdown draw a dozen radial strokes on a Canvas 2D overlay that translate outward and fade over 400ms, with the canvas pointer-events none over the tap target and DPR capped at 1 under lite. Skip spawning under reduced motion. Tags: action, button, motion, decor, game. Fits: rsvp-guest-list, form-confirmation, approval-card, list-add-row, game-2d, store, landing.
- **Cubes** · `build` · css · lite ✓ · touch ✓ · motion static — A small cluster of CSS cubes turning slowly as a hero object for a landing, at a fixed isometric angle under reduced motion. Build: Lay a grid of at most 3x3 @kit/fancy/css-box cubes sharing one CSS keyframe rotation, auto-rotating with hover tilt as an optional extra; prefer a single css-box when the faces carry content. Show the cluster at a fixed isometric angle under reduced motion. Tags: hero, decor, motion, ambient. Fits: landing-funnel, landing, page.
- **Electric Border** · `build` · svg · lite ✓ · touch ✓ · motion static — A crackling electric outline around one card; use it for the single live or pending item, a steady glow under lite or reduced motion. Build: Apply an inline SVG turbulence and displacement filter to a border layer, cycling the seed on a short loop, with a box-shadow glow in the accent colour. Freeze the seed to a steady glow under lite and reduced motion. Tags: decor, card, motion, filter, style. Fits: approval-card, countdown-ticker-hero, status-thinking, game-2d, landing, store.
- **Glare Hover** · `build` · css · lite ✓ · touch ✓ · motion static — A light glare sweeping once across a card; it plays on press or on mount, since hover never reveals on touch. Build: Translate a pseudo-element linear-gradient across the card with overflow hidden, fired on :active or once on mount, with hover as an extra. Under reduced motion show the card without the sweep. Tags: card, style, motion, decor, media. Fits: gallery, product-page, store, landing.
- **Gradual Blur** · `build` · css · lite ✓ · touch ✓ · motion static — Content that comes into focus from soft to sharp once, as a hero reveal on mount or tap; sharp from the start under reduced motion. Build: Set filter blur(12px) on the element and transition it to 0 over 300ms on mount or tap, never backdrop-filter and never tied to scroll. Start sharp under reduced motion. Tags: filter, motion, hero. Fits: form-confirmation, landing-funnel, landing, page.
- **Magic Rings** · `build` · svg · lite ✓ · touch ✓ · motion static — Concentric glowing rings that ripple outward from a tap as a hero flourish on a landing; the rings sit still under reduced motion. Build: Draw three to five concentric SVG circles and, on tap, run a stroke pulse of scale and opacity that settles in 300ms. Hold the rings still under reduced motion. Tags: decor, ambient, hero, motion. Fits: landing.
- **Noise** · `build` · svg · lite ✓ · touch ✓ · motion static — A film-grain overlay that gives a flat canvas some texture; a still grain tile everywhere, animated only in atmosphere as decoration. Build: Tile an inline SVG turbulence grain at low opacity over the canvas as a static texture; animate its seed only in atmosphere as aria-hidden decoration and hold it still under lite and reduced motion. Tags: ambient, material, background, decor. Fits: landing, game-2d, page.
- **Pixel Swap** · `build` · css · lite ✓ · touch ✓ · motion static — A grid of pixel blocks covers a card, the content swaps underneath and the blocks dissolve; a tap-to-flip card or a confirmation swap, one per screen. Build: Overlay a CSS grid of opaque squares whose opacity staggers with a per-cell delay so the cover assembles, the content swaps underneath and the squares dissolve, all on tap and settling in 300ms. Cut straight to the swapped content under reduced motion. Tags: card, motion, media, decor. Fits: gallery, form-confirmation, landing, game-2d, store.
- **Pixel Transition** · `build` · css · lite ✓ · touch ✓ · motion static — A pixel-grid dissolve swaps one face of a card for another on tap: a photo to its caption, a price to its detail. One per screen; both faces ship in the bundle. Build: Overlay a CSS grid of squares with a per-cell animation delay set from a custom property and toggle it on tap so one face dissolves into the other; hover may preview but never be the only trigger. Both faces ship in the bundle. Tags: card, media, motion, decor. Fits: gallery, product-page, landing, store, page.
- **Star Border** · `build` · css · lite ✓ · touch ✓ · motion static — A sparkle border orbits one button or card to mark the action that matters; one per screen, and it or border-beam, not both. Build: Orbit two radial-gradient sparkle blobs inside an overflow-hidden pill layered behind the button's border with one CSS keyframe; pause the loop and leave the border lit under reduced motion. Tags: button, decor, motion, style. Fits: approval-card, countdown-ticker-hero, landing-funnel, landing, store, tool.
- **Sticker Peel** · `build` · css · lite ✓ · touch ✓ · motion static — A sticker whose corner lifts and peels when you tap or drag it: a 'going' badge on an RSVP, a 'confirmed' seal on a receipt. One per screen; the image lives in the bundle. Build: Rotate the corner in CSS 3D, clip the lifted flap with clip-path and move a shadow with the same transform, driven by tap and pointer drag. The sticker is a bundled PNG or SVG the owner supplies, never a remote image. Tags: media, decor, motion, card. Fits: rsvp-guest-list, form-confirmation, gallery, landing, store, page.
- **Strands** · `build` · canvas2d · lite ✓ · touch ✓ · motion static — Glowing strands ripple and weave behind a landing hero; a few translucent canvas curves give the look lite, and packed Beams or Silk only when a recipe wants the shader look. Build: Stroke four to six sine-offset bezier curves on a Canvas 2D at DPR 1, two or three passes at widening line width and falling alpha with the lighter composite for glow and no shadow blur. Render one frame and stop under reduced motion. Tags: background, ambient, webgl, hero. Fits: landing-funnel, countdown-ticker-hero, landing.
- **Antigravity** · `no` — A 3D particle field that scatters away from the mouse cursor as it moves and drifts back when it leaves; `no`: pointer-driven with no tap equivalent (no pointer on touch). Instead: none.
- **Blob Cursor** · `no` — A gooey blob that trails the mouse pointer with inertia and stretches as it catches up; `no`: pointer-driven with no tap equivalent (no pointer on touch). Instead: none.
- **Crosshair** · `no` — A custom crosshair cursor that tracks the mouse and opens or tints when it passes over a link; `no`: pointer-driven with no tap equivalent (no pointer on touch). Instead: none.
- **Elastic Mesh** · `no` — A full-surface spring mesh that stretches under the pointer and wobbles back into place; `no`: pointer-driven with no tap equivalent (no pointer on touch). Instead: none.
- **Ghost Cursor** · `no` — A translucent, smoky trail that follows the mouse cursor and thins out behind it; `no`: pointer-driven with no tap equivalent (no pointer on touch). Instead: `@kit/fancy/pixel-trail`.
- **Glow Cursor** · `no` — A soft light trail with a tapered tail and a slow pulse drawn behind the mouse pointer; `no`: pointer-driven with no tap equivalent (no pointer on touch). Instead: `@kit/fancy/pixel-trail`.
- **Halftone Reveal** · `no` — A print-style halftone dot screen over an image that resolves into the sharp photo around the pointer; `no`: pointer-driven with no tap equivalent (no pointer on touch). Instead: none.
- **Image Trail** · `no` — Photos spawn along the pointer's path and fade away behind it; `no`: pointer-driven with no tap equivalent (no pointer on touch). Instead: none.
- **Magnet** · `no` — A button that leans toward the approaching cursor and springs back when it leaves; `no`: pointer-driven with no tap equivalent (no pointer on touch). Instead: none.
- **Magnet Lines** · `no` — A grid of short lines that all swing to point at the cursor as it moves; `no`: pointer-driven with no tap equivalent (no pointer on touch). Instead: `@kit/fancy/pixel-trail`.
- **Ribbons** · `no` — Long physics ribbons stream and whip behind the pointer and vanish when it stops; `no`: pointer-driven with no tap equivalent (no pointer on touch). Instead: none.
- **Ripple Distortion** · `no` — Water-like ripples warp an image under the pointer and leave a slowly fading wake; `no`: pointer-driven with no tap equivalent (no pointer on touch). Instead: none.
- **Scroll Expand** · `no` — A rounded media frame grows to full bleed as the page scrolls through it; `no`: moves on scroll (nothing moves on scroll inside the webview). Instead: `@kit/air`.
- **Shape Blur** · `no` — A blurred geometric outline that morphs toward the pointer and fades when it leaves; `no`: pointer-driven with no tap equivalent (no pointer on touch). Instead: `@kit/fancy/animated-gradient-with-svg`.
- **Splash Cursor** · `no` — A liquid splash bursts under the cursor and curls into rippling waves across the whole page; `no`: pointer-driven with no tap equivalent (no pointer on touch). Instead: none.
- **Swarm Cursor** · `no` — A flock of particles chases the pointer, jostles for room and drifts apart when it stops; `no`: pointer-driven with no tap equivalent (no pointer on touch). Instead: none.
- **Target Cursor** · `no` — Four corner brackets replace the mouse cursor and snap onto whatever it hovers; `no`: pointer-driven with no tap equivalent (no pointer on touch). Instead: none.

### Cards, lists, galleries, navigation

45 effects: 14 already in the Kit, 0 packed Tier B, 1 to pack, 18 to build, 12 not for a mini-app.

- **Border Glow** · `@kit/libraries/border-beam` · lite ✓ · touch ✓ · motion static — A glowing gradient border that chases the cursor and brightens near the edges; the Kit's border-beam is the self-running substitute that marks one card without a pointer. Tags: decor, card, style. Fits: approval-card, landing, store.
- **Carousel** · `@kit/fancy/simple-carousel` · lite ✓ · touch ✓ · motion reduced — A swipeable strip of slides that snap into place and can loop; the gallery default for photos or products. Tags: carousel, gallery, layout, media. Fits: gallery, product-page, store, landing, page.
- **Circular Gallery** · `@kit/fancy/simple-carousel` · lite ✓ · touch ✓ · motion reduced — A ring of bent images you drag to spin; the Kit's swipe carousel is the flat, lite substitute for the WebGL ring. Tags: gallery, media, webgl, carousel. Fits: gallery, store, landing.
- **Counter** · `@kit/fancy/basic-number-ticker` · lite ✓ · touch ✓ · motion static — Digits that roll to a value like an odometer; the Kit's ticker counts to the value instead of rolling columns, for RSVP totals, prices and countdowns. Tags: data, text, motion, status. Fits: countdown-ticker-hero, rsvp-guest-list, landing, store, tool.
- **Dome Gallery** · `@kit/fancy/simple-carousel` · lite ✓ · touch ✓ · motion reduced — Photos wrapped on a draggable 3D dome as a full-screen showpiece; the Kit's swipe carousel is the compact substitute, css-box if six faces of 3D matter. Tags: gallery, media, hero, motion. Fits: gallery, page, landing.
- **Flowing Menu** · `@kit/beautiful/glide-menu` · lite ✓ · touch ✓ · motion reduced — A highlight that glides between tabs or menu rows as a tap changes the active item; segmented controls and small nav, the source's hover-entry marquee band dropped. Tags: menu, nav, motion. Fits: approval-card, tool, page.
- **Glass Icons** · `@kit/arlan/color-depth` · lite ✓ · touch ✓ · motion n/a — Frosted-glass icon tiles for a feature row or action grid; color-depth's glass material in atmosphere, its glossy or cushion material under lite, glass is never lite. Tags: button, style, material. Fits: landing-funnel, landing, page.
- **Glass Surface** · `@kit/air` · lite ✓ · touch ✓ · motion n/a — A refractive glass panel with edge lighting; Air's .panel with var(--blur) is the glass in atmosphere and renders flat under pixel and lite. Tags: material, style, layout, filter. Fits: landing-funnel, product-page, landing, page, store.
- **Gooey Nav** · `@kit/libraries/liquid-gooey` · lite ✓ · touch ✓ · motion reduced — A tab thumb that stretches like liquid and splits into droplets as it moves to the tapped item; one nav per screen, the thumb jumps under reduced motion. Tags: nav, menu, motion, filter. Fits: product-page, tool, page, store.
- **Infinite Menu** · `@kit/fancy/simple-carousel` · lite ✓ · touch ✓ · motion reduced — A menu of image tiles on a sphere you drag or swipe to spin endlessly; the Kit's wrap-around swipe carousel is the phone substitute for the WebGL sphere. Tags: menu, nav, carousel, webgl. Fits: gallery, page, store.
- **Infinite Spiral** · `@kit/fancy/simple-carousel` · lite ✓ · touch ✓ · motion reduced — Images on an endlessly turning 3D helix as a hero gallery; the Kit's swipe carousel is the compact substitute, css-box if the 3D flavour matters. Tags: gallery, media, hero, motion. Fits: gallery, landing, page.
- **Pill Nav** · `@kit/beautiful/glide-menu` · lite ✓ · touch ✓ · motion reduced — A row of pills with a highlight that slides to the active one; the section switcher for a tool or page app with two to four sections. Tags: nav, menu, pill, motion. Fits: tool, page, store, landing.
- **Profile Card** · `@kit/arlan/holo` · lite ✓ · touch ✓ · motion static — A portrait card with a glare that tilts in 3D toward the pointer; holo's orientation tilt is the phone substitute, turn or tilt the phone, portrait and handle set inside it. Tags: card, motion, material, hero. Fits: landing-funnel, landing, page.
- **Tilted Card** · `@kit/arlan/holo` · lite ✓ · touch ✓ · motion static — A card that tilts in perspective toward the pointer; holo's orientation tilt is the phone substitute, tilt or turn the phone, with the foil layers left off for a plain tilt. Tags: card, motion, media, hero. Fits: product-page, landing-funnel, landing, store, page.
- **Morph Slider** · `pack` · webgl · lite ✗ — A hero image slider that melts one photo into the next through a displacement shader; one per landing or product hero, a crossfade or still under lite. Until packed: decline; no packed substitute. Tags: carousel, gallery, media, webgl, hero. Fits: gallery, landing-funnel, product-page, landing, store.
- **Animated List** · `build` · css · lite ✓ · touch ✓ · motion static — List rows that enter with a short stagger when the list first appears; the hero motion for a guest list or task list, every row present under reduced motion. Build: Set a per-row animation-delay on Air .row or .item with one transform and opacity keyframe fired once on mount, the whole list settling within 300ms; no scroll listener or intersection observer. Tags: list, motion. Fits: rsvp-guest-list, list-add-row, tool, page, landing.
- **Bounce Cards** · `build` · css · lite ✓ · touch ✓ · motion static — A fan of cards that springs into place on mount; the hero motion for a gallery or product spread, already fanned under reduced motion. Build: Animate three to five bundled image cards with one transform keyframe on an overshoot cubic-bezier and a per-card delay, landing in a fan that fits 390px. Tags: card, motion, gallery, hero. Fits: gallery, landing, store.
- **Bubble Menu** · `build` · css · lite ✓ · touch ✓ · motion static — A round toggle that expands into a staggered ring of action bubbles; a compact plus-menu of three to five actions, opened by tap. Build: Scale and translate each 44px bubble out from the toggle with CSS transitions and a rising transition-delay, and reverse them on close; the whole ring settles within 300ms. Tags: menu, nav, action, motion. Fits: tool, page, landing.
- **Card Nav** · `build` · css · lite ✓ · touch ✓ · motion static — A header that opens into grouped cards of links; the nav for a landing with more than four links, one disclosure under the header bar. Build: Build a single disclosure under header.bar: a tap slides two or three Air .panel link groups down with transform and opacity in 300ms, rows 44px; never animate height. Tags: nav, menu, layout. Fits: landing-funnel, landing, page.
- **Card Swap** · `build` · css · lite ✓ · touch ✓ · motion static — A small stack of cards where the front card drops to the back on a timer or tap; a testimonial or feature rotator, paused on the front card under reduced motion. Build: Offset three cards with CSS 3D transforms and rotate their z-order on a finite interval or a tap, each swap a 300ms transform; mark the loop aria-hidden and keep the content readable as a list. Tags: card, carousel, motion, hero. Fits: gallery, landing-funnel, landing, store.
- **Depth Carousel** · `build` · css · lite ✓ · touch ✓ · motion reduced — A coverflow rail where cards recede into perspective as you drag; a product spread with depth, the active card in front and full width at 390px. Build: Start from @kit/fancy/simple-carousel's snap rail and give each slot a perspective transform, rotateY, translateZ and opacity by distance from the active slot; auto-advance stops under reduced motion. Tags: carousel, card, gallery, motion. Fits: gallery, product-page, store, landing.
- **Drift Wall** · `build` · css · lite ✓ · touch ✓ · motion static — An endless perspective wall of tiles drifting past as ambient decor behind a hero; the tiles keep drifting on their own, one wall per screen. Build: Wrap @kit/fancy/simple-marquee in a container with perspective and a rotateX/rotateY tilt so the strip reads as a wall; stack two or three strips at offset speeds as aria-hidden decoration. Tags: marquee, ambient, decor, motion, background. Fits: landing-funnel, gallery, landing.
- **Elastic Slider** · `build` · css · lite ✓ · touch ✓ · motion reduced — A range slider whose thumb overshoots and springs back on release; one tunable value (tip, quantity, budget) where the flourish confirms the drop. Build: Build on a native 16px range input with a 44px hit area inside @kit/beautiful/fine-tune-card and add the overshoot as a transform spring on pointerup that settles within 300ms; under reduced motion the thumb lands. Tags: input, motion, data. Fits: form-confirmation, tool, store.
- **Folder** · `build` · css · lite ✓ · touch ✓ · motion reduced — A folder icon that opens on tap to fan out two or three cards; a playful disclosure for a small photo set or the items an agent gathered. Build: Draw a 44px folder in var(--accent) with a flap and papers as plain elements, translating and rotating the papers open with transform over 300ms ease-out; under reduced motion it opens with no transition. Tags: card, motion, decor, layout. Fits: gallery, page, landing.
- **Magic Bento** · `build` · css · lite ✓ · touch ✓ · motion static — A bento grid of tiles with tap-to-expand; the cursor spotlight, glow and magnetism that need a mouse are dropped, a still grid under reduced motion. Build: Lay the tiles out with Air .grid and .tile, expand a tapped tile with transform and opacity in 300ms, and put @kit/libraries/border-beam on the one tile that deserves attention. Tags: layout, card, motion. Fits: landing-funnel, product-page, landing, store.
- **Masonry** · `build` · css · lite ✓ · touch ✓ · motion static — A two-column grid of mixed-height photo tiles that settle into place once on load; the gallery grid when images vary in shape, never a layout for text. Build: Use CSS columns or a two-column grid of bundled images with one transform and opacity entrance on mount, capped at two columns at 390px; no reflow animation and nothing tied to scroll. Tags: layout, gallery, media, list. Fits: gallery, product-page, store, page, landing.
- **Option Wheel** · `build` · css · lite ✓ · touch ✓ · motion static — A curved drum picker you drag to choose one value from a short list; use it when the pick is the screen's hero, otherwise a native select. Build: Place the options on a drum with per-item rotateX, translateZ and opacity by distance from the selection, driven by drag deltas under touch-action: none with a snap on release; pair with a hidden native select. Tags: input, list, motion. Fits: rsvp-guest-list, form-confirmation, tool, store.
- **Pixel Card** · `build` · canvas2d · lite ✓ · touch ✓ · motion static — A card whose surface fills with animated pixel squares as it appears; a once-on-mount or tap reveal for one featured card, never hover. Build: Draw a grid of squares on a Canvas 2D layer over the card and fade or scale them in with a per-cell delay, run once on mount or tap, then leave the canvas inert and aria-hidden. Tags: card, decor, motion, media. Fits: product-page, landing, page.
- **Scroll Stack** · `build` · css · lite ✓ · touch ✓ · motion static — Cards that pin under one another and pile up as the page scrolls; a landing section of three or more cards, nothing scaled or blurred by scroll position. Build: Give each card position: sticky with a top offset that grows a few pixels per card so they pile as the page scrolls; drive no property from scroll position, no scale, blur or smooth-scroll. Tags: card, layout. Fits: landing-funnel, landing, page.
- **Specular Button** · `build` · css · lite ✓ · touch ✓ · motion static — A glass button with a bright rim light sweeping its edge; the one primary action on a screen, the light sweeping on its own without a cursor. Build: Compose @kit/libraries/border-beam over @kit/arlan/color-depth as a 44px button, metal or glossy under lite and glass only in atmosphere; the beam is the screen's one hero motion. Tags: button, material, motion, action. Fits: approval-card, landing-funnel, landing, store.
- **Stack** · `build` · css · lite ✓ · touch ✓ · motion static — A deck of cards you swipe to send the top one to the back; a gallery or guest-photo hero when a flat carousel is not enough, reordering without a fling under reduced motion. Build: Offset three to five cards a few pixels with a slight rotation, drag the top card with pointer events under touch-action: pan-y and fling it to the back past a threshold; keep any autoplay finite or aria-hidden. Tags: card, carousel, gallery, motion, media. Fits: gallery, landing, store, page.
- **Staggered Menu** · `build` · css · lite ✓ · touch ✓ · motion static — An overlay menu whose items slide in one after another on open; the nav for a multi-section page or tool app, opened from the header bar. Build: Slide a panel in from header.bar and give each 44px row a transform and opacity keyframe with a rising animation-delay so the whole open settles in 300ms; respect env(safe-area-inset-*). Tags: menu, nav, motion, layout. Fits: page, tool, landing.
- **Stepper** · `build` · css · lite ✓ · touch ✓ · motion static — A multi-step form with a progress indicator and animated step changes; RSVP, checkout and any form longer than one compact card, up to four steps. Build: Render a row of done/active/pending dots and one visible step panel that slides between steps in 300ms with transform, 16px inputs and 44px buttons; keep progress in useAirState so guests read it. Tags: input, status, layout, motion, nav. Fits: form-confirmation, rsvp-guest-list, landing-funnel, product-page, tool, store, landing.
- **Accordion Gallery** · `no` — Panels that widen under the pointer to reveal parallax imagery and captions; a horizontal accordion of photos; `no`: pointer-driven with no tap equivalent (no pointer on touch). Instead: `@kit/fancy/simple-carousel`.
- **Chroma Grid** · `no` — Grayscale tiles that regain their colour under a cursor spotlight; a responsive card grid seen through a moving window; `no`: pointer-driven with no tap equivalent (no pointer on touch). Instead: none.
- **Curved Input** · `no` — An input bar bent along an arc, its text, caret and send button all following the curve; `no`: no phone-sized use inside Messages. Instead: `@kit/fancy/text-along-path`.
- **Decay Card** · `no` — A card whose image warps and dissolves as the pointer moves across it, with a parallax shift; `no`: pointer-driven with no tap equivalent (no pointer on touch). Instead: none.
- **Dock** · `no` — An icon bar whose items magnify as the cursor nears them, in the manner of a desktop dock; `no`: pointer-driven with no tap equivalent (no pointer on touch). Instead: none.
- **Fluid Glass** · `no` — A glass lens that refracts the page beneath it as it follows the pointer, liquid distortion rippling through the blob; `no`: a physics or 3D playground far over the budgets. Instead: `@kit/air`.
- **Flying Posters** · `no` — Posters on a ribbon that spin past endlessly as the page scrolls, bending with a shader; `no`: moves on scroll (nothing moves on scroll inside the webview). Instead: `@kit/fancy/simple-carousel`.
- **Lanyard** · `no` — A badge card swinging on a physics rope you can grab and fling, with real inertia and a woven strap; `no`: a physics or 3D playground far over the budgets. Instead: `@kit/arlan/holo`.
- **Line Sidebar** · `no` — A sidebar list whose rows shift and brighten as the cursor nears them; `no`: pointer-driven with no tap equivalent (no pointer on touch). Instead: none.
- **Model Viewer** · `no` — An orbitable viewer for a 3D model file with lighting presets, one-finger rotate and two-finger zoom; `no`: needs a remote asset the CSP forbids. Instead: none.
- **Reflective Card** · `no` — A card that mirrors the viewer through the webcam and glares as the cursor moves; `no`: no phone-sized use inside Messages. Instead: none.
- **Spotlight Card** · `no` — A card lit by a soft gradient spotlight that follows the cursor across its surface; `no`: pointer-driven with no tap equivalent (no pointer on touch). Instead: `@kit/libraries/border-beam`.

### Backgrounds and fields

57 effects: 8 already in the Kit, 13 packed Tier B, 6 to pack, 24 to build, 6 not for a mini-app.

- **Aurora** · `@kit/fancy/animated-gradient-with-svg` · lite ✓ · touch ✓ · motion static — Slow ribbons of teal and violet flowing behind a headline; the Kit's blurred gradient blobs in aurora tones stand in for the shader. Tags: background, ambient, hero, motion. Fits: landing-funnel, countdown-ticker-hero, rsvp-guest-list, landing, page.
- **Color Bends** · `@kit/fancy/animated-gradient-with-svg` · lite ✓ · touch ✓ · motion static — Bands of vivid colour bending and flowing behind a panel; the Kit's moving gradient blobs give the same wash without a shader. Tags: background, ambient, hero, motion. Fits: landing-funnel, form-confirmation, landing, page.
- **Dark Veil** · `@kit/fancy/animated-gradient-with-svg` · lite ✓ · touch ✓ · motion static — A barely moving dark haze behind a chat or tool screen; the atmosphere canvas plus the Kit's gradient blobs in two near-canvas tones gives the same veil. Tags: background, ambient, motion. Fits: chat-with-agent, status-thinking, page, tool, landing.
- **Dot Field** · `@kit/fancy/pixel-trail` · lite ✓ · touch ✓ · motion none — A dot grid that bulges and sparkles under the cursor; the Kit's pixel-trail is the finger-driven version, and a still dot grid is one CSS radial-gradient. Tags: background, cursor, decor, motion. Fits: landing, game-2d.
- **Dot Grid** · `@kit/fancy/pixel-trail` · lite ✓ · touch ✓ · motion none — A dot grid that shoves away from the cursor; the Kit's pixel-trail is the finger-driven version, and a still dot grid is one CSS radial-gradient. Tags: background, cursor, decor, layout. Fits: landing, tool.
- **Orb** · `@kit/aicss/orbs` · lite ✓ · touch ✓ · motion static — A glowing energy orb that warps under the pointer; the Kit's orb floats as an ambient presence without the pointer warp, and thinking-orbs adds named states. Tags: status, ambient, thinking, decor. Fits: status-thinking, chat-with-agent, tool, page.
- **Plasma** · `@kit/fancy/animated-gradient-with-svg` · lite ✓ · touch ✓ · motion static — Soft plasma gradients that swirl and morph behind a panel; the Kit's moving gradient blobs give the same wash without a shader. Tags: background, ambient, motion. Fits: landing-funnel, landing, page.
- **Soft Aurora** · `@kit/fancy/animated-gradient-with-svg` · lite ✓ · touch ✓ · motion static — Slow aurora-like colour drift behind a panel; at phone width the Kit's blurred gradient blobs read the same as the noise shader. Tags: background, ambient, hero, motion. Fits: landing-funnel, form-confirmation, landing, page.
- **Beams** · `@kit/restricted/beams` · three · lite ✗ — Crossing ribbons of light drifting behind a landing or product hero; one plate per app. Non-lite; you supply the still frame under reduced motion. Tags: background, webgl, hero, ambient. Fits: landing-funnel, product-page, countdown-ticker-hero, landing, store.
- **Dither** · `@kit/restricted/dither` · three · lite ✗ — Retro dithered noise drifting behind a pixel-styled hero or 2D game; mouse-wave off. Non-lite; you supply the still frame under reduced motion. Tags: background, webgl, ambient, filter, game. Fits: game-2d-canvas, landing-funnel, game-2d, landing.
- **Faulty Terminal** · `@kit/restricted/faulty-terminal` · webgl · lite ✗ — Flickering CRT terminal squares with scanlines and noise behind a status or retro game screen; mouse reaction off. Non-lite; you supply the still frame under reduced motion. Tags: background, webgl, ambient, status, game. Fits: status-thinking, game-2d-canvas, game-2d, tool, landing.
- **Galaxy** · `@kit/restricted/galaxy` · webgl · lite ✗ — A twinkling starfield slowly rotating behind a launch countdown or night-event hero, pointer parallax switched off. Non-lite; you supply the still frame under reduced motion. Tags: background, webgl, ambient, hero, motion. Fits: countdown-ticker-hero, landing-funnel, landing, game-3d, page.
- **Grainient** · `@kit/restricted/grainient` · webgl · lite ✗ — A grainy swirling gradient with soft wave distortion; the painterly backdrop for a landing or store hero with one display line over it. Non-lite; you supply the still frame under reduced motion. Tags: background, webgl, ambient, hero, material. Fits: landing-funnel, product-page, landing, store, page.
- **Iridescence** · `@kit/restricted/iridescence` · webgl · lite ✗ — A pearlescent surface with slow shifting waves tinted from the accent; a glossy material backdrop for a product or store hero behind one display line. Non-lite; you supply the still frame under reduced motion. Tags: background, webgl, material, hero, ambient. Fits: product-page, landing-funnel, store, landing.
- **Light Rays** · `@kit/restricted/light-rays` · webgl · lite ✗ — Volumetric rays falling from the top edge behind a confirmation or reveal hero, the only motion on the screen; pointer-follow off. Non-lite; you supply the still frame under reduced motion. Tags: background, webgl, ambient, hero, decor. Fits: form-confirmation, landing-funnel, countdown-ticker-hero, landing, page.
- **Liquid Chrome** · `@kit/restricted/liquid-chrome` · webgl · lite ✗ — A flowing mirror-chrome surface behind a hero word or product shot; libraries/metal-fx is the Tier A liquid-metal plate when Tier A is required. Non-lite; you supply the still frame under reduced motion. Tags: background, material, webgl, hero, ambient. Fits: landing-funnel, product-page, landing, store.
- **Liquid Ether** · `@kit/restricted/liquid-ether` · three · lite ✗ — Fluid colour that swirls under a finger and drifts on its own; a landing hero that invites a touch, one per app. Non-lite; you supply the still frame under reduced motion. Tags: background, webgl, ambient, hero, motion. Fits: landing-funnel, landing, game-3d.
- **Molten Metal** · `@kit/restricted/molten-metal` · webgl · lite ✗ — Swirling plasma filaments with white-hot cores; a dramatic game or launch hero, one per app. Non-lite; you supply the still frame under reduced motion. Tags: background, webgl, hero, ambient, material. Fits: game-3d-three, landing-funnel, game-3d, landing.
- **Prism** · `@kit/restricted/prism` · webgl · lite ✗ — A rotating glass prism throwing coloured light across the hero of a landing or store screen; rotate mode, not pointer-follow. Non-lite; you supply the still frame under reduced motion. Tags: background, webgl, hero, ambient. Fits: landing-funnel, product-page, landing, store.
- **Side Rays** · `@kit/restricted/side-rays` · webgl · lite ✗ — Light rays fanning in from one side edge behind a hero word or product shot; sibling of Light Rays, pick one per app. Non-lite; you supply the still frame under reduced motion. Tags: background, hero, webgl, ambient, decor. Fits: landing-funnel, product-page, countdown-ticker-hero, landing, store.
- **Silk** · `@kit/restricted/silk` · three · lite ✗ — Slow silk-like waves with soft lighting behind a hero; the heaviest packed plate, so make it the only 3D plate in the app. Non-lite; you supply the still frame under reduced motion. Tags: background, hero, webgl, material, ambient. Fits: landing-funnel, product-page, form-confirmation, landing, store, page.
- **Aero Shards** · `pack` · three · lite ✗ — A wind-blown field of folded foil shards drifting in 3D behind a landing or product hero, pointer input off. Until packed: use `@kit/restricted/liquid-chrome`. Tags: background, webgl, hero, material, ambient. Fits: landing-funnel, product-page, landing, store.
- **CRT Warp** · `pack` · webgl · lite ✗ — A curved CRT plasma screen with scanlines and bloom for a retro game title, pointer part off. Until packed: use `@kit/restricted/faulty-terminal`. Tags: background, webgl, ambient, hero, game. Fits: game-2d-canvas, game-2d, landing.
- **Ferrofluid** · `pack` · webgl · lite ✗ — Churning magnetic fluid drawn as glowing contour lines behind a landing hero, cursor magnet off or bound to a drag. Until packed: use `@kit/restricted/liquid-ether`. Tags: background, webgl, ambient, material, cursor. Fits: landing-funnel, landing.
- **Ghost Fibers** · `pack` · webgl · lite ✗ — A deep-blue twisting fibre field with luminous bands; a calm atmospheric backdrop for a landing hero or evening invite. Until packed: use `@kit/restricted/silk`. Tags: background, webgl, ambient, hero, motion. Fits: landing-funnel, rsvp-guest-list, landing, page.
- **Lightfall** · `pack` · webgl · lite ✗ — A tunnel of coloured light streaks raining toward the viewer, pointer steering off; the splash hero of a game-3d or launch screen. Until packed: use `@kit/restricted/beams`. Tags: background, webgl, hero, ambient, motion. Fits: game-3d-three, landing-funnel, game-3d, landing.
- **Plasma Wave** · `pack` · webgl · lite ✗ — Raymarched plasma sheets with two interfering waves rolling through each other; a hero for a game-3d or launch screen. Until packed: use `@kit/restricted/molten-metal`. Tags: background, webgl, hero, ambient, motion. Fits: game-3d-three, landing-funnel, game-3d, landing.
- **Acid Squares** · `build` · canvas2d · lite ✓ · touch ✓ · motion static — A square tunnel of nested frames receding into depth behind a game title screen. Build: Draw concentric squares on Canvas 2D that scale outward from the centre and respawn at the vanishing point, tinted from the accent token. Hold one frame of nested squares under reduced motion. Tags: background, ambient, hero, game. Fits: countdown-ticker-hero, game-2d-canvas, game-2d, game-3d, landing.
- **Evil Eye** · `build` · svg · lite ✓ · touch ✓ · motion static — A single animated eye with a slit pupil and fiery orange glow as a hero mark, one per screen. Build: Draw the eye in SVG: a radial-gradient iris, an ellipse pupil that narrows to a slit by scaling on transform, and one orange blur-filter glow that switches off under lite. Hold the open-eye frame under reduced motion. Tags: hero, decor, ambient, motion. Fits: countdown-ticker-hero, landing, game-2d.
- **Gradient Blinds** · `build` · css · lite ✓ · touch ✓ · motion static — Bold diagonal gradient stripes behind a store or landing hero; the spotlight sits fixed instead of following a pointer. Build: Layer repeating-linear-gradient stripes over a two-stop token gradient and place one fixed radial highlight where the spotlight would sit. Add grain with an SVG turbulence filter only outside lite. Tags: background, style, decor, hero. Fits: landing-funnel, product-page, landing, store.
- **Gradient Waves** · `build` · svg · lite ✓ · touch ✓ · motion static — Stacked sine waves rolling toward a hazy horizon; a calm hero backdrop for a landing or countdown screen where the slow roll is the only motion. Build: Stack three to five SVG sine paths with shrinking amplitude and spacing toward a hazy horizon, fill them from the token gradient and roll them with one slow transform loop. Stop the loop under reduced motion. Tags: background, ambient, hero, motion, decor. Fits: landing-funnel, countdown-ticker-hero, landing, page.
- **Grid Scan** · `build` · css · lite ✓ · touch ✓ · motion static — A perspective grid room with one light plane sweeping through it; the hero backdrop for a retro game or tool screen, with the sweep as the single hero motion. Build: Build a preserve-3d room whose floor and wall faces are repeating-linear-gradient grids in the accent colour, then translate one glowing scan bar through it. Show the room with the bar at rest under reduced motion; no blur under lite. Tags: background, game, hero, motion, decor. Fits: game-2d-canvas, game-3d-three, game-2d, game-3d, tool.
- **Letter Glitch** · `build` · canvas2d · lite ✓ · touch ✓ · motion static — A Matrix-style grid of glyphs flickering behind a terminal-themed game or tool screen, in the system mono font. Build: Fill a Canvas 2D grid with glyphs in the system mono font and swap random cells at a fixed interval, with a CSS radial-gradient overlay as the vignette. Keep the loop finite or aria-hidden and draw one still grid under reduced motion. Tags: background, text, ambient, game, motion. Fits: game-2d-canvas, game-2d, tool.
- **Light Pillar** · `build` · css · lite ✓ · touch ✓ · motion static — One glowing vertical column of light behind a headline or countdown number; a focal glow for a hero without a shader. Build: Draw a narrow vertical linear-gradient column from transparent through the accent and back, with two wider low-opacity copies for the glow, and breathe its opacity slowly as the one hero motion. Hold it still under reduced motion; no blur under lite. Tags: background, hero, decor, ambient, motion. Fits: countdown-ticker-hero, landing-funnel, landing, page, store.
- **Light Tunnel** · `build` · canvas2d · lite ✓ · touch ✓ · motion static — Radial strands converging on a vanishing point with pulses racing inward; a loud launch or game hero backdrop, one pulse cycle as the hero motion. Build: On Canvas 2D draw many thin bright lines radiating from a centre point with brighter segments travelling inward along them, and a radial gradient for depth fog. Run one finite pulse cycle and hold a still frame under reduced motion. Tags: background, game, hero, motion, ambient. Fits: game-3d-three, countdown-ticker-hero, game-3d, game-2d, landing.
- **Lightning** · `build` · canvas2d · lite ✓ · touch ✓ · motion static — A branching lightning bolt with a glow flicker, striking once as the hero moment of a storm, power-up or countdown-hits-zero screen, never a permanent flashing loop. Build: Draw a midpoint-displaced polyline with two or three branches on Canvas 2D, glow it with shadowBlur and flicker it once for under 300ms. Keep flashes under three per second and, under reduced motion, draw the bolt once and stop. Tags: background, hero, motion, decor, game. Fits: countdown-ticker-hero, game-2d-canvas, game-2d, landing.
- **Line Waves** · `build` · svg · lite ✓ · touch ✓ · motion static — Stacked sine lines rippling across a panel with a slow colour shift; a calm ambient header behind a title when a gradient blob feels too soft. Build: Draw eight to twelve SVG polylines displaced by a sine of position and time, stroked from the accent token with a slow hue shift. Freeze the phase under reduced motion. Tags: background, ambient, motion, decor. Fits: landing-funnel, landing, page.
- **Particles** · `build` · canvas2d · lite ✓ · touch ✓ · motion static — A field of drifting soft dots behind a hero or empty state, aria-hidden and still under reduced motion. Build: Draw forty to eighty soft-alpha circles on Canvas 2D at DPR 1 with slow random velocities that wrap at the edges, aria-hidden. Stop them under reduced motion. Tags: background, ambient, motion, decor. Fits: landing-funnel, landing, game-2d, page.
- **Pixel Blast** · `build` · canvas2d · lite ✓ · touch ✓ · motion static — Chunky pixel bursts rippling out from a tap; tap feedback in a game or a one-shot celebration when a form confirms. Build: On pointerdown spawn a ring of chunky squares on Canvas 2D that expand from the tap point and fade over a short lifetime; skip the liquid post-process. Under reduced motion draw the burst at full spread once. Tags: background, game, motion, action, decor. Fits: game-2d-canvas, form-confirmation, game-2d, landing.
- **Pixel Snow** · `build` · canvas2d · lite ✓ · touch ✓ · motion static — Pixelated snow drifting down behind a card for a seasonal invite or countdown, frozen to a still scatter under reduced motion. Build: Drop sixty to one hundred twenty small rects down a Canvas 2D at a slow y velocity with a sideways wobble, aria-hidden and paused when offscreen. Freeze to a still scatter under reduced motion. Tags: background, ambient, motion, decor. Fits: rsvp-guest-list, countdown-ticker-hero, landing, page.
- **Prismatic Burst** · `build` · css · lite ✓ · touch ✓ · motion static — Rainbow rays bursting from a centre point and slowly rotating; a celebration or reveal hero. Build: Paint a conic-gradient sunburst of rainbow wedges under a radial mask and rotate it slowly with transform. Hold one frame under reduced motion. Tags: background, hero, motion, decor. Fits: form-confirmation, countdown-ticker-hero, landing-funnel, landing, store.
- **Radar** · `build` · svg · lite ✓ · touch ✓ · motion static — Concentric rings, spokes and a sweeping beam; the searching state of a status or chat screen. Build: Draw concentric circles and spokes in SVG and rotate one conic-gradient wedge with transform as the sweeping beam. Hold the wedge in place under reduced motion. Tags: status, thinking, loading, motion, decor. Fits: status-thinking, chat-with-agent, tool, game-2d.
- **Ripple Grid** · `build` · canvas2d · lite ✓ · touch ✓ · motion static — A grid whose lines ripple outward in slow rings; an ambient backdrop for one hero screen of a tool or game lobby, never behind body copy. Build: Draw a grid of lines on Canvas 2D and displace each vertex by a radial sine from the centre, advancing only the phase each frame, aria-hidden. Draw one still frame under reduced motion and lite. Tags: background, ambient, motion, decor. Fits: landing-funnel, game-2d-canvas, landing, game-2d, tool.
- **Scanner** · `build` · css · lite ✓ · touch ✓ · motion static — Soft interference bands sweeping the panel like an oscilloscope trace; the ambient plate behind a working or scanning status, one per screen. Build: Layer two or three repeating-linear-gradient bands moved with transform and a slow opacity pulse, capped to the compact card height and never behind body copy. Pause the loop under reduced motion; one frame of bands is complete. Tags: background, ambient, status, motion, thinking. Fits: status-thinking, chat-with-agent, tool, page.
- **Shape Grid** · `build` · svg · lite ✓ · touch ✓ · motion static — A field of small squares, hexagons, circles or triangles drifting in one direction; a quiet decorative plate behind a hero card or lobby. Build: Tile one SVG shape symbol into a pattern and slide it in one direction with a single translate keyframe; use Canvas 2D only when each shape needs its own variation. Stop the pattern under reduced motion. Tags: background, decor, ambient, motion, game. Fits: game-2d-canvas, landing-funnel, game-2d, landing, page.
- **Shape Waves** · `build` · svg · lite ✓ · touch ✓ · motion static — Rows of tiny triangles, circles and squares that brighten and swell as waves roll across them, parting around an optional word; a hero plate for a landing or launch card. Build: Lay out a grid of small SVG shapes and scale and brighten each by a travelling sine of its position and time, staggered diagonally; cut the optional word out with an SVG mask in the system font. Hold one frame under reduced motion. Tags: background, hero, motion, text. Fits: landing-funnel, countdown-ticker-hero, landing, store.
- **Sliced Waves** · `build` · css · lite ✓ · touch ✓ · motion static — A row of soft glowing bars rippling like a slatted equalizer; an ambient plate for a music, playlist or listening-party card, one per screen. Build: Stand a dozen div bars in a row and drive transform: scaleY with staggered keyframes, with a box-shadow glow that switches off under lite. Keep it aria-hidden decoration, never real audio levels, and static under reduced motion. Tags: background, ambient, data, motion, media. Fits: gallery, page, landing.
- **Threads** · `build` · canvas2d · lite ✓ · touch ✓ · motion static — Fine wavy lines that sway like woven fabric; an ambient plate behind a hero headline, drawn once as a still under reduced motion. Build: Draw twenty to forty polylines on Canvas 2D with a sine offset per vertex so they sway like fabric, and give the plate density and swell props so it also serves Waves and Web Threads. Draw one still frame under reduced motion. Tags: background, ambient, motion, decor. Fits: landing-funnel, chat-with-agent, landing, page, tool.
- **Topography** · `build` · canvas2d · lite ✓ · touch ✓ · motion static — Glowing contour lines that slowly breathe like a living elevation map; a hero plate for a trip, hike, route or map-flavoured card. Build: Run a coarse value-noise grid through marching squares on Canvas 2D, tint contour levels by elevation from theme tokens and glow them with two or three layered low-alpha strokes. Advance the noise phase slowly and stop it under reduced motion. Tags: background, hero, data, ambient, motion. Fits: landing-funnel, rsvp-guest-list, landing, page, tool.
- **Waves** · `build` · canvas2d · lite ✓ · touch ✓ · motion static — Layered flowing lines with a gentle swell; a calm ambient plate behind a hero card, one per screen and still under reduced motion. Build: Draw layered noise-driven lines on Canvas 2D with a gentle swell, sharing the flowing-lines plate with Threads and Web Threads through density and swell props. Keep DPR 1 on lite and freeze the phase under reduced motion. Tags: background, ambient, motion, decor, hero. Fits: landing-funnel, form-confirmation, landing, page.
- **Web Threads** · `build` · canvas2d · lite ✓ · touch ✓ · motion static — Glowing sine threads converging on one bright focal point; a hero plate for a launch or landing card that wants a single luminous centre. Build: Draw sine polylines on Canvas 2D whose amplitude tapers toward one shared focal point, glowing through layered low-alpha strokes rather than shadowBlur. Show a flat still frame under reduced motion. Tags: background, hero, motion, ambient. Fits: landing-funnel, countdown-ticker-hero, landing, store.
- **Balatro** · `no` — Swirling paint blobs in the palette and motion of the card game Balatro; `no`: reproduces a third party's trade dress (§5). Instead: none.
- **Ballpit** · `no` — A 3D ball pit of bouncing coloured spheres filling the page; `no`: a physics or 3D playground far over the budgets. Instead: none.
- **Floating Lines** · `no` — Fine 3D lines that drift and bend toward the cursor; `no`: pointer-driven with no tap equivalent (no pointer on touch). Instead: `@kit/restricted/beams`.
- **Grid Distortion** · `no` — An image whose mesh warps smoothly under the cursor; `no`: pointer-driven with no tap equivalent (no pointer on touch). Instead: none.
- **Grid Motion** · `no` — Rows of oversized tiles on a rotated grid that slide with the pointer's horizontal position; `no`: pointer-driven with no tap equivalent (no pointer on touch). Instead: `@kit/fancy/simple-marquee`.
- **Hyperspeed** · `no` — Light streaks racing toward the viewer, faster while the screen is held; `no`: a physics or 3D playground far over the budgets. Instead: none.

### arlan.me vault studies

21 effects: 9 already in the Kit, 0 packed Tier B, 0 to pack, 2 to build, 10 not for a mini-app.

- **Apple's corners** · `@kit/arlan/squircle` · lite ✓ · touch ✓ · motion n/a — Apple's corner: the curve eases into the flat edge with no kink, so a button or card reads smooth all the way round; one CTA or hero card per screen, rows keep the Air pill. Tags: button, card, style, layout. Fits: approval-card, form-confirmation, product-page, landing, store, tool, page.
- **Ghosty reveal** · `@kit/arlan/ghosty-reveal` · lite ✓ · touch ✓ · motion static — A photo bleeds in through a soft, cloudy edge, as if forming out of fog; the one hero image or a confirmation reveal, cloud mask bundled by the owner. Tags: media, motion, gallery, hero. Fits: gallery, product-page, form-confirmation, landing, store, page.
- **Holo** · `@kit/arlan/holo` · lite ✓ · touch ✓ · motion static — An ID card on holographic foil: drag it or turn the phone and the foil catches the light, marks rise on the turned side and the photo flips colour; one membership, ticket or pass card per screen. Tags: card, motion, hero, material. Fits: rsvp-guest-list, product-page, landing, store, page.
- **Liquid UI** · `@kit/arlan/liquid-ui` · lite ✓ · touch ✓ · motion static — Two cards fuse and the corner between them bends inward, as if poured together, from crisp joints to gooey blobs; a 2-4 card cluster read as one poured shape, never layout. Tags: card, decor, motion, layout. Fits: gallery, product-page, landing, store, page.
- **Rush type** · `@kit/arlan/rush-type` · lite ✗ · touch ✓ · motion static — One lowercase word rests, then blasts twelve times taller and tears into green and violet streaks from speed alone, resolving to plain white at rest; the next word falls out of the blur. Tags: text, motion, hero, webgl. Fits: landing-funnel, landing.
- **Shutter type** · `@kit/arlan/shutter-type` · lite ✓ · touch ✓ · motion static — Two lines of type seen through a rolling shutter: stripes lag the ink so a word tears into bands only while it moves and is whole at rest; one hero motion for a title card. Tags: text, motion, hero. Fits: landing-funnel, landing, page.
- **Swing type** · `@kit/arlan/swing-type` · lite ✓ · touch ✓ · motion static — Enormous coloured letters on a pendulum, each turning edge-on as it passes so only two or three read at once and the word never assembles; the word changes by turning. Tags: text, motion, hero. Fits: landing-funnel, landing, page.
- **The art of color depth** · `@kit/arlan/color-depth` · lite ✓ · touch ✓ · motion n/a — Buttons that feel like real objects: glossy plastic, brushed metal, glass, a soft cushion, neon, a pressed-in key; one material per screen on the primary action, glass only when not lite. Tags: button, material, style, action. Fits: approval-card, form-confirmation, landing-funnel, landing, store, tool, page.
- **The typer** · `@kit/arlan/typer` · lite ✓ · touch ✓ · motion static — A headline that types in instead of fading: a wave runs along the line and each letter flickers through pill, highlight and outline, then lands as text; one headline or status line. Tags: text, motion, hero, status. Fits: status-thinking, countdown-ticker-hero, chat-with-agent, landing-funnel, landing, page, tool.
- **Kinetic typography** · `build` · dom · lite ✓ · touch ✓ · motion static — A hero letter or short word sliced into tiles that ripple on independent sine waves; the single hero motion on a title screen, never body copy. Build: Slice the word into DOM tiles with overflow clipping and drive each tile's translateY from its own sine offset in one requestAnimationFrame loop. Under reduced motion draw the tiles at rest. Tags: text, motion, hero, decor. Fits: countdown-ticker-hero, landing-funnel, landing, page, game-2d.
- **Pixel brushes** · `build` · canvas2d · lite ✓ · touch ✓ · motion n/a — A brush that stamps a small shape along the finger's path, from crisp line to loose spray; a doodle, signature or guestbook sketch. Build: Stamp a chosen shape onto a Canvas 2D at each pointer sample, spacing by distance and jittering size and offset for the spray; set touch-action none on the canvas so strokes never scroll the page. Tags: input, media, game, action. Fits: game-2d-canvas, form-confirmation, tool, game-2d.
- **Amo hover button** · `no` — A pill button whose label puffs into glossy 3D letters, played as a short bundled clip while the pointer rests on it; `no`: reproduces a third party's trade dress (§5). Instead: none.
- **Arcade pixel** · `no` — A word drawn tiny and blown up so every pixel becomes a big square, edges crisp, drawn on the GPU; `no`: §5 excludes the study (WebGL post-effect with no lite frame). Instead: `@kit/fancy/pixelate-svg-filter`.
- **Chromatic glow** · `no` — A soft light bloom around a headline that splits into colour fringes at its edges; `no`: §5 excludes the study (WebGL post-effect with no lite frame). Instead: none.
- **Dia Browser's gradient** · `no` — A bottom-of-screen glow of blurred colour bars that rises once on load and settles as a full-screen background; `no`: reproduces a third party's trade dress (§5). Instead: `@kit/fancy/animated-gradient-with-svg`.
- **Fade motion** · `no` — A headline smeared into a motion blur by stacking hundreds of faint copies of itself; `no`: §5 excludes the study (WebGL post-effect with no lite frame). Instead: none.
- **Figma vector editor** · `no` — Design-tool selection chrome: a thin blue box, corner handles, a size tag and draggable bezier points around a shape; `no`: reproduces a third party's trade dress (§5). Instead: none.
- **Midjourney Medical's ASCII** · `no` — Loose letters spin and settle into a wordmark as live text on load; `no`: reproduces a third party's trade dress (§5). Instead: `@kit/fancy/scramble-in`.
- **Ransom note** · `no` — Type set in real cut-out magazine letters taped down crooked, one letter image per glyph; `no`: needs a remote asset the CSP forbids. Instead: none.
- **Realistic emboss** · `no` — Lettering pressed into the surface with a directional light, so every edge carries a highlight and a shadow; `no`: §5 excludes the study (WebGL post-effect with no lite frame). Instead: none.
- **Symbols effect** · `no` — An image or live video cut into four brightness bands, each stamped with a tinted symbol; `no`: §5 excludes the study (WebGL post-effect with no lite frame). Instead: none.

## 5. Exclusions

Not in the Kit, so stop looking:

- **ReactBits** (`DavidHDev/react-bits`) — "MIT + Commons Clause": may compile into an app, may not be redistributed as source. Tier B: it never lives in Git or in this Kit; the Build Service resolves it from the restricted artifact (`restricted/README.md`). The 13 files under `apps/web/lib/miniapps/client/backgrounds/vendor/` are ReactBits ports and are governed by the same terms.
- **CanvasUI** (`DavidHDev/canvas-ui`) — same Commons Clause, and it needs WebGL2 + Three + experimental HTML-in-canvas. Excluded outright.
- **WebGL under lite** — `libraries/metal-fx` and `arlan/rush-type` are the only WebGL components and both are `lite="false"`; `AirMetalFx` renders its child on a flat plate when lite, reduced motion, or no WebGL, and Rush Type draws its resting word as a still. arlan's `arcade-pixel`, `fade-motion`, `chroma-glow`, `emboss` and `sandbox` (the Symbols effect; all WebGL) and Beautiful UI's `prompt-bar` (WebGL via `glimm`) are not harvested.
- **Pro / paid components** — AI CSS `file-diff`, `image-generation`, `inline-citations`, `comparison-table` are Pro (private, licensed). Only the ten free components are here.
- **Trade dress** — arlan's `amo`, `midjourney`, `figma` (URL slug `vector-editor`), `dia-gradient` reproduce other products' identities. Not harvested.
- **Proprietary icon fonts** — Beautiful UI `sidebar-nav` depends on `@central-icons-react`; every other icon import is rewritten to `lucide-react`.
- **Heavy** — Beautiful UI `insight-cards` (`liveline` charts), libraries.dev `img-fx`, Fancy's variable-font components (need the font), physics components other than `elastic-line` (`matter-js`).
- **Not available at harvest** — recorded as gaps in `kit.sources.json`, never fabricated: Beautiful UI `agent-screen` (registry lists it, `r/agent-screen.json` is 404), arlan `ransom-note` (depends on site-hosted imagery and a manifest that is not published).

## 6. Budgets

The Build Service enforces these; `scripts/verify.ts` enforces them on the Kit itself, so a component listed above already fits.

| Budget | Limit | Where it is enforced |
| --- | --- | --- |
| JS, `lite` set (everything marked `lite="true"` bundled together, gzip) | 300 KiB | verify.ts, Build Service |
| JS, hard (any app, gzip) | 1 MiB | Build Service |
| CSS (gzip) | 200 KiB | verify.ts, Build Service |
| One image | 2 MiB | Build Service (`validateBundle`) |
| Apps API state, one resource | 256 KiB | `PUT /api/apps/v1/state` (413) |
| Functions module | 1 MiB | `lib/functions/deploy.ts` |
| Functions request | 50 CPU-ms, 20 subrequests | Dispatcher |

Weights in the catalog are gzipped KiB after esbuild with react/react-dom and the shared vendor packages external, so they add. `jsFull` in `meta.json` is the cost when the app has nothing but React.

Measured for Kit 2026.09: the `lite` set (67 renderable components, bundled together with React) is 181.9 KiB JS and 19.8 KiB CSS gzipped against budgets of 300 KiB and 200 KiB.
