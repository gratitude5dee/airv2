<!-- GENERATED by packages/create-kit/scripts/harvest.ts — do not edit. Edit prompts/src/*, scripts/lib/catalog.ts or a component's ref.md source and re-run harvest. Design doc (goal-create-v11 §12.3). -->



# Air Create — Design doc (Kit 2026.09)

Read top to bottom once, then open `ref.md` only for the components you pick. 79 components; 76 are `lite`.


## 1. Doctrine

### The contract

A mini-app is one static bundle behind the Air CSP ceiling. Rules, not preferences:

- `index.html` at the bundle root; everything it loads is in the bundle. No host is ever referenced: no CDN script, no web font, no remote image, no `<iframe>`, no `<meta http-equiv>`, no analytics. The Build Service rejects a bundle that names a host.
- No client storage. `localStorage`, `sessionStorage`, `indexedDB`, cookies and service workers are forbidden; every app shares one origin, so anything persisted is readable by every other app. State lives behind `useAirState()` (the Apps API, 256 KiB per resource; owners write, guests read).
- No `eval(`, no `new Function(`, no inline event handlers, no `javascript:` URLs. `"use client"` where a component touches the DOM; nothing reads `window` at module scope.
- Messages viewport: `<meta name="viewport" content="width=device-width,initial-scale=1,viewport-fit=cover">`, single column ≤ 36rem, `min-height: 100svh`, `env(safe-area-inset-*)` padding, ≥ 44px (2.75rem) touch targets, 16px inputs so iOS does not zoom.
- Lite (`surface.lite`, `<html data-lite="1">`, `useLite()`): no `backdrop-filter`, no fixed backgrounds, DPR 1, no WebGL, no component marked `lite="false"`. Compact card (390×360) first; expanded (390×760) second; nothing depends on a desktop width.
- `prefers-reduced-motion: reduce` yields a static, complete frame. `useReducedMotion()` is the switch; every component in this Kit declares what it does under it (`reducedMotion` in the catalog).

### Air's visual system is data

Use tokens, never literals: `var(--canvas)`, `var(--ink)`, `var(--ink-muted)`, `var(--accent)`, `var(--on-accent)`, `var(--on-ink)`, `var(--panel-bg)`, `var(--well-bg)`, `var(--ring)`, `var(--shadow)`, `var(--blur)`, `var(--font-body)`, `var(--font-ui)`, `var(--radius-panel)`, `var(--radius-well)`, `var(--radius-pill)`, `var(--text-shadow)`, `var(--slide-in)`. `kit/air/theme.css` defines them for both themes; `kit/air/shell.css` gives the structural vocabulary (`.frame`, `header.bar`, `main.app`, `.panel`, `.notice`, `.row`, `.item`, `.card`, `.tile`, `.grid`, `.chip`, `.addrow`, `.tablewrap`, `.hero`, `.navlink`). Build screens from that vocabulary first and reach for a catalog component only where the recipe names one.

`atmosphere` is the default (Newsreader display over Azeret Mono labels, warm dark canvas, soft blur). `pixel` is the fallback (Inter/system, flat neutrals, no shader, no blur) and is what lite surfaces get. A component that only looks right under one theme is wrong.

### Motion

One hero motion per screen. Everything else settles in ≤ 300ms with an ease-out. Nothing moves on scroll inside a webview; no parallax, no scroll-linked reveals. Under reduced motion the hero is a static frame that already says everything. Loops are finite or explicitly `aria-hidden` decoration. Never animate `width`/`height`/`top`/`left`; use `transform` and `opacity`.

### Typography, density, copy

Display in `--font-body`, labels/buttons/counters in `--font-ui` with `.kicker` tracking. One display size per screen, body 16px, labels 12–13px uppercase. Touch first: single column, 44px rows, no hover-only affordance (hover may enhance, never reveal). Copy is short, present tense, no exclamation marks, no emoji in UI chrome. The owner's name and the app's purpose appear once, in the header pill.

### Data and identity

The app knows the viewer only as owner or guest, and learns which by trying to save: `useAirState().canWrite` is `null` until a write is attempted, `false` after a 403. Show guests the same screen, read-only, without a broken control. No secrets in `src/`; Functions secrets live in the Secrets tab and are read server-side only.

## 2. Recipes

Eight shapes cover almost every request. Start from the closest one and remove, never add, motion.

### Status / thinking view

The owner's agent is working on something and the app shows its progress. Components: `aicss/thinking-state` (the phase list), `libraries/thinking-orbs` (the hero, `AirThinkingOrb`), `aicss/streaming-text` or `fancy/typewriter` for the one live line.

```tsx
<main className="app">
  <section className="panel hero">
    <AirThinkingOrb state={status} />          {/* one hero motion */}
    <p className="kicker">Working</p>
    <Typewriter text={currentStep} />          {/* the live line */}
  </section>
  <ThinkingState steps={steps} />              {/* settles; no per-row animation */}
</main>
```

State comes from `useAirState<{ status; steps }>("progress")`, written by the owner's agent, read by everyone. Under reduced motion the orb is a still disc and the typewriter renders the full line.

### Approval card

One decision, two buttons, a receipt. Components: `beautiful/approval-card` (or `aicss/approval-card` for the compact variant), `beautiful/button`, `libraries/border-beam` only while the decision is pending.

```tsx
const { state, update, canWrite } = useAirState<{ decision: "pending" | "approved" | "declined" }>("approval");
<AirBorderBeam active={state.decision === "pending"}>
  <ApprovalCard
    title="Send the invoice to Nadia?"
    detail="$1,240 · net 30"
    onApprove={() => update((s) => ({ ...s, decision: "approved" }))}
    onDecline={() => update((s) => ({ ...s, decision: "declined" }))}
    disabled={canWrite === false}
  />
</AirBorderBeam>
```

Guests see the same card with the buttons disabled and the notice "Guests are read-only." The card is forwardable, so it never contains the thing being approved in full: one line of detail, and the receipt after the decision.

### List with an add row

The oldest mini-app shape: a `.panel` of `.item` rows and an `.addrow` at the bottom. No catalog component is required; `aicss/todo-list` is the ready-made version, `beautiful/task-rows` when rows carry status pills.

```tsx
const { state, update } = useAirState<{ items: { id: string; text: string; done: boolean }[] }>("list", { items: [] });
<section className="panel">
  {state.items.map((it) => (
    <label key={it.id} className="item row">
      <input type="checkbox" checked={it.done} onChange={() => toggle(it.id)} />
      <span className={it.done ? "muted" : ""}>{it.text}</span>
    </label>
  ))}
  <form className="addrow" onSubmit={add}>
    <input placeholder="Add…" />
    <button className="chip">Add</button>
  </form>
</section>
```

Rows are 44px, inputs 16px. Reorder by explicit up/down chips, not drag. Nothing animates except the new row's ≤ 300ms fade.

### Countdown / ticker hero

A number that matters, large. Components: `fancy/basic-number-ticker` (the hero), `fancy/text-rotate` or `fancy/vertical-cut-reveal` for the label, `fancy/simple-marquee` for a one-line ticker under it.

```tsx
<section className="panel hero">
  <p className="kicker">Doors open in</p>
  <NumberTicker from={0} to={hours} autoStart />
  <VerticalCutReveal>hours</VerticalCutReveal>
</section>
<SimpleMarquee baseVelocity={2}><span className="chip">Sat 8pm</span> …</SimpleMarquee>
```

The ticker is the only hero motion; the marquee stops under reduced motion and lite (`useReducedMotion()` → render a static row of chips). Time comes from the client clock; never fetch time from a host.

### RSVP / guest list (Functions)

Many people write, so state cannot be the owner's 256 KiB resource. This shape needs a Functions backend: `functions/index.ts` with `hono` + `zod`, one D1 table, `POST /api/rsvp` and `GET /api/guests`. Front end: `beautiful/records-table` or a `.panel` list of `.item` rows, `beautiful/entity-chip` for each guest, `beautiful/button` to submit.

```ts
// functions/index.ts
import { Hono } from "hono";
import { z } from "zod";
import { air } from "@air/functions";
const app = new Hono();
const Rsvp = z.object({ name: z.string().min(1).max(60), going: z.boolean() });
app.post("/api/rsvp", async (c) => {
  const body = Rsvp.parse(await c.req.json());
  await air.db(c).prepare("insert into rsvp(principal,name,going) values(?,?,?) on conflict(principal) do update set name=excluded.name, going=excluded.going")
    .bind(air.user(c.req.raw).principal, body.name, body.going ? 1 : 0).run();
  return c.json({ ok: true });
});
export default app;
```

The guest never sees another guest's principal; names only. The owner sees counts and the list. Rate limits and identity are the Dispatcher's; the app trusts `X-Air-Role` via `air.user()` and nothing else.

### Gallery

Images from the owner's media prefix (`/api/media/*` on the app origin or `https://media.wzrd.tech` via `img-src`), never anywhere else. Components: `.grid` of `.tile` from the shell, `fancy/stacking-cards` for a stacked hero, `fancy/simple-carousel` when the set is ≤ 8, `arlan/squircle` for the tile shape.

```tsx
<section className="grid">
  {photos.map((p) => (
    <figure key={p.src} className="tile squircle">
      <img src={p.src} alt={p.alt} loading="lazy" decoding="async" />
      <figcaption className="muted">{p.caption}</figcaption>
    </figure>
  ))}
</section>
```

Each image ≤ 2 MiB, sized for 390px × DPR 3 at most. No lightbox motion beyond a 200ms fade; no pinch-zoom implementation (the webview does it). Under lite the carousel becomes the grid.

### Form with confirmation

Collect a few fields, show a receipt. Components: shell `.panel` + `.row` inputs, `beautiful/button`, `beautiful/value-pill` on the receipt, `fancy/scramble-in` or `arlan/typer` for the "Sent" line.

```tsx
const [sent, setSent] = useState<null | Form>(null);
return sent ? (
  <section className="panel notice">
    <TyperText text="Sent." play="in" />
    <div className="row"><ValuePill label="Name" value={sent.name} /><ValuePill label="Date" value={sent.date} /></div>
  </section>
) : (
  <form className="panel" onSubmit={submit}>
    <label className="row"><span className="kicker">Name</span><input required maxLength={80} /></label>
    <label className="row"><span className="kicker">Date</span><input type="date" required /></label>
    <Button type="submit" variant="primary">Send</Button>
  </form>
);
```

Where does it go? Either `useAirState().save()` (the owner reads it in the Files tab) or a Functions `POST` (many submitters). Never email, never a webhook to a host. Validate with `zod` on both sides.

### Chat with the owner's agent

A thread with the owner's Hermes through Functions → `POST https://air.internal/v1/chat/completions`. Components: `beautiful/chat-composer` (input), `beautiful/stream-text` or `aicss/text-response` (assistant bubbles), `aicss/code-block` for code, `beautiful/tool-chips` when a turn used tools, `libraries/thinking-orbs` while waiting.

```tsx
const { messages, send, pending } = useThread();     // your hook over fetch("/api/chat", { method: "POST", body })
<main className="app">
  <section className="panel thread">
    {messages.map((m) => m.role === "assistant" ? <StreamText key={m.id} text={m.text} /> : <p key={m.id} className="item">{m.text}</p>)}
    {pending && <AirThinkingOrb state="thinking" size={20} />}
  </section>
  <ChatComposer onSubmit={send} disabled={pending} placeholder="Ask…" />
</main>
```

```ts
// functions/index.ts
app.post("/api/chat", async (c) => {
  const { messages } = z.object({ messages: z.array(Msg).max(40) }).parse(await c.req.json());
  const res = await air.ai.chat(c, { model: "fast", messages, stream: true });
  return new Response(res.body, { headers: { "content-type": "text/event-stream" } });
});
```

`model` is one of `fast | balanced | deep`, never a vendor name. The daily cap is the owner's; when the gateway returns 429 the app says "The agent is resting today." Streaming renders progressively; under reduced motion the bubble fills without the caret.

## 3. Catalog

One line per component: `path` is the reference to open, `lite` whether it may be used on a lite surface, `weight` gzipped KiB it adds (JS+CSS, own code), `tier` the license tier (only A ships in source), `touch` whether it works without hover, `motion` what happens under `prefers-reduced-motion` (static: a complete still frame; reduced: shorter; none: no motion to begin with; n/a: not animated).

### Text and typography

<components>
<aicss/code-block path="kit/aicss/code-block/ref.md" lite="true" weight="1.6kb" tier="A" touch="true" motion="n/a">Code Block — Monospace block with a copy affordance; no highlighter dependency. Tags: code, chat, text</aicss/code-block>
<aicss/streaming-text path="kit/aicss/streaming-text/ref.md" lite="true" weight="0.8kb" tier="A" touch="true" motion="static">Streaming Text — Token-by-token reveal of an incoming reply. Tags: ai, chat, text, streaming</aicss/streaming-text>
<aicss/text-response path="kit/aicss/text-response/ref.md" lite="true" weight="0.6kb" tier="A" touch="true" motion="n/a">Text Response — A finished agent reply with light markdown-ish structure. Tags: ai, chat, text</aicss/text-response>
<arlan/rush-type path="kit/arlan/rush-type/ref.md" lite="false" weight="6.6kb" tier="A" touch="true" motion="static">Rush Type — One lowercase word rests, then blasts twelve times taller and tears into green and violet streaks from speed alone, resolving to plain white at rest; the next word falls out of the blur. Non-lite: WebGL1. Tags: text, motion, hero, webgl</arlan/rush-type>
<arlan/shutter-type path="kit/arlan/shutter-type/ref.md" lite="true" weight="3.8kb" tier="A" touch="true" motion="static">Shutter Type — Two lines of type seen through a rolling shutter: stripes lag the ink so a word tears into bands only while it moves and is whole at rest. One hero motion for a title card; the phrases are props. Tags: text, motion, hero, canvas</arlan/shutter-type>
<arlan/swing-type path="kit/arlan/swing-type/ref.md" lite="true" weight="3.1kb" tier="A" touch="true" motion="static">Swing Type — Enormous coloured letters on a pendulum, each turning edge-on as it passes so only two or three read at once and the word never assembles; the word changes by turning. A hero for a title screen, never body copy. Tags: text, motion, hero, canvas</arlan/swing-type>
<arlan/typer path="kit/arlan/typer/ref.md" lite="true" weight="2.7kb" tier="A" touch="true" motion="static">Typer — A headline that types in instead of fading: a wave runs along the line and each letter flickers through pill, highlight and outline, then lands as text. Pick it over fancy/typewriter when the line must also type away. Tags: text, motion, hero, status</arlan/typer>
<beautiful/code-block path="kit/beautiful/code-block/ref.md" lite="true" weight="2.4kb" tier="A" touch="true" motion="n/a">Code Block — Code block with line numbers and copy. aicss/code-block is the lighter default. Tags: code, text</beautiful/code-block>
<beautiful/selection-actions path="kit/beautiful/selection-actions/ref.md" lite="true" weight="4.2kb" tier="A" touch="true" motion="reduced">Selection Actions — Floating action bar for a text selection (rewrite, shorten, ask). Tags: actions, menu, text, ai</beautiful/selection-actions>
<beautiful/stream-text path="kit/beautiful/stream-text/ref.md" lite="true" weight="0.6kb" tier="A" touch="true" motion="static">Stream Text (atom) — Word-stream reveal used by selection-actions. Tags: text, streaming, atom, helper</beautiful/stream-text>
<beautiful/streaming-text path="kit/beautiful/streaming-text/ref.md" lite="true" weight="2.6kb" tier="A" touch="true" motion="static">Streaming Text — Streaming reply with inline source chips. For plain streaming use aicss/streaming-text. Tags: ai, chat, text, streaming, sources</beautiful/streaming-text>
<fancy/breathing-text path="kit/fancy/breathing-text/ref.md" lite="true" weight="0.8kb" tier="A" touch="true" motion="static">Breathing Text — A single word whose weight/width breathes slowly. Ambient only; never for status. Tags: text, motion, ambient</fancy/breathing-text>
<fancy/letter-3d-swap path="kit/fancy/letter-3d-swap/ref.md" lite="true" weight="1.6kb" tier="A" touch="false" motion="none">Letter 3D Swap — Letters flip on a 3D axis on hover. Hover-only; heavier than the 2D swaps. Tags: text, motion, hover, 3d</fancy/letter-3d-swap>
<fancy/letter-swap-forward-anim path="kit/fancy/letter-swap-forward-anim/ref.md" lite="true" weight="0.7kb" tier="A" touch="false" motion="none">Letter Swap (forward) — Nav/button label whose letters roll upward on hover. Hover-only. Tags: text, motion, hover, nav</fancy/letter-swap-forward-anim>
<fancy/letter-swap-pingpong-anim path="kit/fancy/letter-swap-pingpong-anim/ref.md" lite="true" weight="0.8kb" tier="A" touch="false" motion="none">Letter Swap (ping-pong) — Like Letter Swap (forward) but rolls back on hover-out. Hover-only. Tags: text, motion, hover, nav</fancy/letter-swap-pingpong-anim>
<fancy/media-between-text path="kit/fancy/media-between-text/ref.md" lite="true" weight="1.0kb" tier="A" touch="true" motion="static">Media Between Text — Headline where an image expands between two words on hover/view. Tags: text, media, motion, hero</fancy/media-between-text>
<fancy/random-letter-swap-forward-anim path="kit/fancy/random-letter-swap-forward-anim/ref.md" lite="true" weight="0.8kb" tier="A" touch="false" motion="none">Random Letter Swap (forward) — Letter Swap with randomized per-letter direction. Hover-only. Tags: text, motion, hover, nav</fancy/random-letter-swap-forward-anim>
<fancy/random-letter-swap-pingpong-anim path="kit/fancy/random-letter-swap-pingpong-anim/ref.md" lite="true" weight="0.8kb" tier="A" touch="false" motion="none">Random Letter Swap (ping-pong) — Randomized Letter Swap that rolls back on hover-out. Hover-only. Tags: text, motion, hover, nav</fancy/random-letter-swap-pingpong-anim>
<fancy/scramble-hover path="kit/fancy/scramble-hover/ref.md" lite="true" weight="1.2kb" tier="A" touch="false" motion="none">Scramble Hover — A nav label that scrambles on hover/focus. Hover-only flourish; no touch equivalent. Tags: text, motion, hover</fancy/scramble-hover>
<fancy/scramble-in path="kit/fancy/scramble-in/ref.md" lite="true" weight="0.8kb" tier="A" touch="true" motion="static">Scramble In — Reveal a short label by unscrambling it once on mount or on view. Tags: text, motion, reveal</fancy/scramble-in>
<fancy/text-along-path path="kit/fancy/text-along-path/ref.md" lite="true" weight="1.0kb" tier="A" touch="true" motion="static">Text Along Path — Text set along an SVG path (circles, arcs). Decorative headers only. Tags: text, svg, motion, hero</fancy/text-along-path>
<fancy/text-highlighter path="kit/fancy/text-highlighter/ref.md" lite="true" weight="1.0kb" tier="A" touch="true" motion="static">Text Highlighter — Marker-style highlight that sweeps across a phrase when it scrolls into view. Tags: text, motion, emphasis</fancy/text-highlighter>
<fancy/text-rotate path="kit/fancy/text-rotate/ref.md" lite="true" weight="1.5kb" tier="A" touch="true" motion="static">Text Rotate — One slot in a headline that cycles through a short list of words. Tags: text, motion, hero</fancy/text-rotate>
<fancy/typewriter path="kit/fancy/typewriter/ref.md" lite="true" weight="0.9kb" tier="A" touch="true" motion="static">Typewriter — A headline or status line that should feel typed by someone; not for body copy. Tags: text, motion, hero, status</fancy/typewriter>
<fancy/underline-center path="kit/fancy/underline-center/ref.md" lite="true" weight="0.9kb" tier="A" touch="true" motion="reduced">Underline (center) — Inline link whose underline grows from the center on hover/focus. Tags: text, link, hover, motion</fancy/underline-center>
<fancy/underline-comes-in-goes-out path="kit/fancy/underline-comes-in-goes-out/ref.md" lite="true" weight="0.9kb" tier="A" touch="true" motion="reduced">Underline (comes in, goes out) — Inline link underline that enters from one side and exits the other. Tags: text, link, hover, motion</fancy/underline-comes-in-goes-out>
<fancy/underline-goes-out-comes-in path="kit/fancy/underline-goes-out-comes-in/ref.md" lite="true" weight="0.9kb" tier="A" touch="true" motion="reduced">Underline (goes out, comes in) — Inline link underline that leaves before re-entering on hover. Tags: text, link, hover, motion</fancy/underline-goes-out-comes-in>
<fancy/underline-to-background path="kit/fancy/underline-to-background/ref.md" lite="true" weight="0.9kb" tier="A" touch="true" motion="reduced">Underline to Background — Link underline that swells into a highlight on hover; good for a single CTA link. Tags: text, link, hover, motion</fancy/underline-to-background>
<fancy/vertical-cut-reveal path="kit/fancy/vertical-cut-reveal/ref.md" lite="true" weight="1.4kb" tier="A" touch="true" motion="static">Vertical Cut Reveal — Reveal a headline line by line (or word by word) with a clipped upward slide. Tags: text, motion, reveal, hero</fancy/vertical-cut-reveal>
</components>

### Status, thinking, streaming

<components>
<aicss/orbs path="kit/aicss/orbs/ref.md" lite="true" weight="5.6kb" tier="A" touch="true" motion="static">Orb — Ambient CSS orb as the agent's presence. Use thinking-orbs when you need named states. Tags: ai, status, avatar, ambient</aicss/orbs>
<aicss/thinking-reasoning path="kit/aicss/thinking-reasoning/ref.md" lite="true" weight="2.6kb" tier="A" touch="true" motion="reduced">Thinking Reasoning — Collapsible reasoning trace under a thinking indicator. Tags: ai, status, disclosure, thinking</aicss/thinking-reasoning>
<aicss/thinking-state path="kit/aicss/thinking-state/ref.md" lite="true" weight="0.5kb" tier="A" touch="true" motion="reduced">Thinking State — The agent is working and you have a verb for it. Default for the status/thinking recipe. Tags: ai, status, loading, thinking</aicss/thinking-state>
<aicss/todo-list path="kit/aicss/todo-list/ref.md" lite="true" weight="3.2kb" tier="A" touch="true" motion="reduced">Todo List — Agent task checklist with pending/active/done rows. Tags: list, tasks, status</aicss/todo-list>
<beautiful/loading-state path="kit/beautiful/loading-state/ref.md" lite="true" weight="1.3kb" tier="A" touch="true" motion="static">Loading State — Full-panel waiting state with a message; pass your own media or none. Tags: loading, status, ai</beautiful/loading-state>
<beautiful/shimmer path="kit/beautiful/shimmer/ref.md" lite="true" weight="0.4kb" tier="A" touch="true" motion="static">Shimmer (atom) — Text shimmer used by selection-actions. Tags: loading, atom, helper</beautiful/shimmer>
<beautiful/task-rows path="kit/beautiful/task-rows/ref.md" lite="true" weight="2.2kb" tier="A" touch="true" motion="reduced">Task Rows — Task rows with progress and owners; heavier than aicss/todo-list. Tags: list, tasks, status</beautiful/task-rows>
<beautiful/thinking path="kit/beautiful/thinking/ref.md" lite="true" weight="2.6kb" tier="A" touch="true" motion="reduced">Thinking — Thinking indicator that lists steps and sources as they arrive. Tags: ai, status, thinking, sources</beautiful/thinking>
<beautiful/tool-chips path="kit/beautiful/tool-chips/ref.md" lite="true" weight="3.1kb" tier="A" touch="true" motion="reduced">Tool Chips — Row of tool-call chips with running/done states. Tags: ai, status, chips, tools</beautiful/tool-chips>
<fancy/basic-number-ticker path="kit/fancy/basic-number-ticker/ref.md" lite="true" weight="0.7kb" tier="A" touch="true" motion="static">Number Ticker — A number that counts to its value: RSVP totals, countdowns, prices. Tags: number, motion, hero, status, countdown</fancy/basic-number-ticker>
<libraries/thinking-orbs path="kit/libraries/thinking-orbs/ref.md" lite="true" weight="0.7kb" tier="A" touch="true" motion="static">Thinking Orbs — Nine named agent states (working, searching, solving…) on a 2D canvas. Best 'the agent is doing X' indicator. Tags: ai, status, thinking, canvas</libraries/thinking-orbs>
</components>

### Inputs and actions

<components>
<aicss/agent-input path="kit/aicss/agent-input/ref.md" lite="true" weight="9.5kb" tier="A" touch="true" motion="n/a">Agent Input — Prompt composer with attachments and send. Default composer for the chat recipe. Tags: input, chat, composer</aicss/agent-input>
<aicss/approval-card path="kit/aicss/approval-card/ref.md" lite="true" weight="7.4kb" tier="A" touch="true" motion="n/a">Approval Card — Approve/deny a proposed action with a short summary. Default for the approval recipe. Tags: approval, decision, card, actions</aicss/approval-card>
<arlan/color-depth path="kit/arlan/color-depth/ref.md" lite="true" weight="3.4kb" tier="A" touch="true" motion="n/a">Color Depth — Buttons that feel like real objects: glossy plastic, brushed metal, glass, a soft cushion, neon, a pressed-in key. Pick one material per screen for the primary action; glass needs backdrop-filter, so never under lite. Tags: button, style, material, action</arlan/color-depth>
<arlan/squircle path="kit/arlan/squircle/ref.md" lite="true" weight="1.6kb" tier="A" touch="true" motion="n/a">Squircle — Apple's corner: the curve eases into the flat edge with no kink, so a button or card reads smooth all the way round. Pick it for the one CTA or hero card whose outline matters; rows and chips keep the Air pill radius. Tags: shape, button, card, style</arlan/squircle>
<beautiful/approval-card path="kit/beautiful/approval-card/ref.md" lite="true" weight="4.1kb" tier="A" touch="true" motion="reduced">Approval Card — Rich approval with a glide menu of alternatives. aicss/approval-card is the lighter default. Tags: approval, decision, card, actions</beautiful/approval-card>
<beautiful/button path="kit/beautiful/button/ref.md" lite="true" weight="1.0kb" tier="A" touch="true" motion="n/a">Button (atom) — Beautiful UI button used by its cards and tables. Prefer Air's .row.actions buttons for app chrome. Tags: button, atom, helper</beautiful/button>
<beautiful/chat-composer path="kit/beautiful/chat-composer/ref.md" lite="true" weight="2.1kb" tier="A" touch="true" motion="n/a">Chat Composer — Multi-line composer with mode pills. aicss/agent-input is the lighter default. Tags: input, chat, composer</beautiful/chat-composer>
<beautiful/search path="kit/beautiful/search/ref.md" lite="true" weight="1.7kb" tier="A" touch="true" motion="reduced">Search — Search field with a filtered result list and glide highlight. Tags: search, list, input</beautiful/search>
</components>

### Lists, tables, cards

<components>
<aicss/data-table path="kit/aicss/data-table/ref.md" lite="true" weight="2.6kb" tier="A" touch="true" motion="n/a">Data Table — Small read-only data table inside a reply. For sorting/filtering use beautiful/filter-table. Tags: table, data</aicss/data-table>
<arlan/holo path="kit/arlan/holo/ref.md" lite="true" weight="6.0kb" tier="A" touch="true" motion="static">Holo Card — An ID card on holographic foil: tilt it or turn the phone and the foil catches the light, marks rise on the turned side and the photo flips colour. Pick it for one membership, ticket or pass card, the screen's hero. Tags: card, motion, hero, material</arlan/holo>
<arlan/liquid-ui path="kit/arlan/liquid-ui/ref.md" lite="true" weight="2.9kb" tier="A" touch="true" motion="static">Liquid UI — Two cards fuse and the corner between them bends inward, as if poured together; one knob runs from crisp joints to gooey blobs. Pick it for a 2-4 card cluster read as one poured shape; menus take libraries/liquid-gooey. Tags: motion, card, layout, decor</arlan/liquid-ui>
<beautiful/diff-table path="kit/beautiful/diff-table/ref.md" lite="true" weight="2.9kb" tier="A" touch="true" motion="n/a">Diff Table — Before/after rows with accept controls. Tags: table, diff, review</beautiful/diff-table>
<beautiful/entity-chip path="kit/beautiful/entity-chip/ref.md" lite="true" weight="0.5kb" tier="A" touch="true" motion="n/a">Entity Chip (atom) — Small entity chip used by recommendation-card. Tags: chip, atom, helper</beautiful/entity-chip>
<beautiful/filter-table path="kit/beautiful/filter-table/ref.md" lite="true" weight="1.5kb" tier="A" touch="true" motion="n/a">Filter Table — Table with filter chips and sorting. Tags: table, data, filter</beautiful/filter-table>
<beautiful/fine-tune-card path="kit/beautiful/fine-tune-card/ref.md" lite="true" weight="3.1kb" tier="A" touch="true" motion="n/a">Fine-tune Card — Card of sliders/toggles to tune a result before confirming. Tags: card, settings, sliders</beautiful/fine-tune-card>
<beautiful/recommendation-card path="kit/beautiful/recommendation-card/ref.md" lite="true" weight="2.5kb" tier="A" touch="true" motion="n/a">Recommendation Card — One recommendation with entity chips, a value and accept/skip. Tags: card, decision, actions</beautiful/recommendation-card>
<beautiful/records-table path="kit/beautiful/records-table/ref.md" lite="true" weight="14kb" tier="A" touch="true" motion="n/a">Records Table — Spreadsheet-like records grid with sticky first column and column selection. Heavy; one per app. Tags: table, data, records</beautiful/records-table>
<beautiful/value-pill path="kit/beautiful/value-pill/ref.md" lite="true" weight="0.6kb" tier="A" touch="true" motion="n/a">Value Pill (atom) — Numeric pill used by recommendation-card. Tags: pill, atom, helper</beautiful/value-pill>
<libraries/border-beam path="kit/libraries/border-beam/ref.md" lite="true" weight="0.6kb" tier="A" touch="true" motion="static">Border Beam — Animated beam around one card that deserves attention (the pending approval, the live item). Tags: decor, motion, card, focus</libraries/border-beam>
</components>

### Layout, media, motion blocks

<components>
<air path="kit/air/ref.md" lite="true" weight="5.7kb" tier="A" touch="true" motion="n/a">Air shell, theme and hooks — Always: theme.css + shell.css give every screen its tokens and vocabulary; index.ts gives useLite(), useReducedMotion(), useTheme(), useAirState(). Tags: air, layout, hooks, state, theme</air>
<arlan/ghosty-reveal path="kit/arlan/ghosty-reveal/ref.md" lite="true" weight="1.0kb" tier="A" touch="true" motion="static">Ghosty Reveal — A photo bleeds in through a soft, cloudy edge, as if forming out of fog. Pick it for the one hero image or a confirmation reveal; the owner bundles the cloud mask, and text takes fancy/vertical-cut-reveal. Tags: reveal, media, motion, hero, gallery</arlan/ghosty-reveal>
<fancy/animated-gradient-with-svg path="kit/fancy/animated-gradient-with-svg/ref.md" lite="true" weight="0.9kb" tier="A" touch="true" motion="static">Animated Gradient (SVG) — Soft moving color blobs behind a panel. Uses blur; pixel theme and lite render it flat. Tags: background, motion, ambient</fancy/animated-gradient-with-svg>
<fancy/circling-elements path="kit/fancy/circling-elements/ref.md" lite="true" weight="0.7kb" tier="A" touch="true" motion="static">Circling Elements — Small items orbiting a center: avatars around a host, badges around a logo. Tags: motion, ambient, decor, gallery</fancy/circling-elements>
<fancy/css-box path="kit/fancy/css-box/ref.md" lite="true" weight="1.3kb" tier="A" touch="true" motion="static">CSS Box — A CSS 3D cube with a face per side; rotate to show six images or words. Tags: 3d, motion, hero, gallery</fancy/css-box>
<fancy/float path="kit/fancy/float/ref.md" lite="true" weight="0.7kb" tier="A" touch="true" motion="static">Float — Gentle idle float for a hero image or badge. One per screen. Tags: motion, ambient, decor</fancy/float>
<fancy/screensaver path="kit/fancy/screensaver/ref.md" lite="true" weight="0.9kb" tier="A" touch="true" motion="static">Screensaver — DVD-logo bounce for a badge inside a container. Ambient only. Tags: motion, ambient, decor</fancy/screensaver>
<fancy/simple-carousel path="kit/fancy/simple-carousel/ref.md" lite="true" weight="1.4kb" tier="A" touch="true" motion="reduced">Simple Carousel — Swipeable horizontal carousel with snap; the gallery recipe default. Tags: layout, gallery, touch</fancy/simple-carousel>
<fancy/simple-marquee path="kit/fancy/simple-marquee/ref.md" lite="true" weight="1.4kb" tier="A" touch="true" motion="static">Simple Marquee — Endless horizontal strip of logos, photos or words. Pauses on reduced motion. Tags: layout, motion, gallery, ambient</fancy/simple-marquee>
<fancy/stacking-cards path="kit/fancy/stacking-cards/ref.md" lite="true" weight="0.8kb" tier="A" touch="true" motion="reduced">Stacking Cards — Sections that stack as the page scrolls. Needs at least three cards to read. Tags: layout, scroll, cards</fancy/stacking-cards>
</components>

### Filters, backgrounds, materials

<components>
<beautiful/foundation path="kit/beautiful/foundation/ref.md" lite="true" weight="2.5kb" tier="A" touch="true" motion="n/a">Beautiful UI foundation — Shared CSS for every beautiful/* component: token bridge onto Air, keyframes, primitive classes. Import once. Tags: style, tokens, helper</beautiful/foundation>
<fancy/gooey-svg-filter path="kit/fancy/gooey-svg-filter/ref.md" lite="true" weight="0.5kb" tier="A" touch="true" motion="n/a">Gooey SVG Filter — Provides an SVG goo filter by id for blobby merges. Pair with liquid-gooey only when that package is too heavy. Tags: filter, svg, helper</fancy/gooey-svg-filter>
<fancy/pixel-trail path="kit/fancy/pixel-trail/ref.md" lite="true" weight="1.1kb" tier="A" touch="true" motion="none">Pixel Trail — Pixels light up under the pointer/finger. Decorative background for one screen. Tags: background, pointer, motion</fancy/pixel-trail>
<fancy/pixelate-svg-filter path="kit/fancy/pixelate-svg-filter/ref.md" lite="true" weight="0.7kb" tier="A" touch="true" motion="n/a">Pixelate SVG Filter — Provides an SVG pixelation filter by id; apply to an image that resolves on load. Tags: filter, svg, helper</fancy/pixelate-svg-filter>
<libraries/metal-fx path="kit/libraries/metal-fx/ref.md" lite="false" weight="0.8kb" tier="A" touch="true" motion="static">Metal FX — Liquid-metal surface behind a hero word. Non-lite only; renders a flat plate when WebGL is off. Tags: shader, webgl, decor, hero</libraries/metal-fx>
</components>

### Foundations and helpers

<components>
<beautiful/context-cards path="kit/beautiful/context-cards/ref.md" lite="true" weight="1.4kb" tier="A" touch="true" motion="reduced">Context Cards — Small stack of context/source cards the agent used. Tags: cards, context, sources</beautiful/context-cards>
<beautiful/flowchart path="kit/beautiful/flowchart/ref.md" lite="true" weight="3.8kb" tier="A" touch="true" motion="reduced">Flowchart — Simple node/edge flowchart for a plan or pipeline. Tags: diagram, svg, plan</beautiful/flowchart>
<beautiful/glide-menu path="kit/beautiful/glide-menu/ref.md" lite="true" weight="0.7kb" tier="A" touch="true" motion="reduced">Glide Menu (atom) — Sliding highlight menu used inside approval-card, records-table, search. Tags: menu, atom, helper</beautiful/glide-menu>
<fancy/elastic-line path="kit/fancy/elastic-line/ref.md" lite="false" weight="1.3kb" tier="A" touch="false" motion="static">Elastic Line — A divider line that stretches toward the pointer and snaps back. Non-lite (physics). Tags: physics, pointer, motion, divider</fancy/elastic-line>
<libraries/liquid-gooey path="kit/libraries/liquid-gooey/ref.md" lite="true" weight="0.6kb" tier="A" touch="true" motion="reduced">Liquid Gooey — Items that merge like liquid: a plus-menu that splits into droplets, a tab thumb that trails. Tags: motion, menu, decor, svg</libraries/liquid-gooey>
</components>

### Sources

| Source | Name | License | Tier | Pin |
| --- | --- | --- | --- | --- |
| `fancy` | Fancy Components (fancycomponents.dev) | MIT | A | danielpetho/fancy@f9f62c61207b |
| `aicss` | AI CSS (free tier) (aicss.dev) | MIT | A | kvnkld/aicss@4556a918fd8c |
| `beautiful` | Beautiful UI (beautifului.dev) | MIT | A | captured 2026-09-04 |
| `libraries` | libraries.dev (libraries.dev) | MIT | A | Jakubantalik/Libraries.dev@aea4c00db4b7 |
| `arlan` | arlan.me vault (arlan.me/vault) | MIT | A | captured 2026-09-04 |

## 4. Effects vocabulary

Owners name looks, not components: "make the title glitch", "an aurora behind it", "a card that tilts", "a trail behind my finger". This section maps 190 named effects from two public catalogues — the React Bits public index (`reactbits.dev/llms.txt`, MIT + Commons Clause, Tier B) and the arlan.me vault (MIT, Tier A) — onto what the Kit actually does for each. It was annotated for the Kit on 2026-09-17 (evidence in `evidence/catalogues/`) and is regenerated, never hand-edited, from that annotation.

Read a line as: **Name** · how · renderer · lite · touch · motion — what it is and when to use it. `how` is one of:

- `@kit/<id>` — a Tier A component already in the Kit covers the look. Import it; do not rebuild it.
- `@kit/restricted/<name>` — a React Bits background compiled from the private Tier B artifact. Non-lite by policy; the surface must not be `lite`, and the poster frame under reduced motion is yours to supply.
- `pack` — a React Bits piece worth adding to the Tier B artifact (shader, physics or 3D that is hard to rebuild well). Until the operator packs it, treat it as `build` if the surface allows, otherwise decline.
- `build` — implement an original version under the contract from the one-line description. Never copy upstream source; the description is the brief. Most text, card, menu and tap effects are `build`.
- `no` — not for a mini-app, with the rule it breaks: no pointer on touch, nothing moves on scroll, WebGL under lite, remote assets, or no phone-sized use. Offer the nearest `build` alternative named on the line.

`lite ✓` means it may appear on a lite surface; `touch ✓` means it works without hover; `motion` is the reduced-motion behaviour (static: complete still frame; reduced: shorter; none: nothing to still; n/a: not animated). On an `@kit/<id>` line the flags describe the catalogued effect as its source ships it; the Kit component you import carries its own measured flags in §3, which is what governs the surface. Tags feed the same groups as the catalog; "fits" names the recipes and templates the effect suits. Weight is an estimate for an original build, not a measurement.

### Text

32 effects: 10 already in the Kit, 0 packed Tier B, 0 to pack, 13 to build, 9 not for a mini-app.

- **Circular Text** · `@kit/fancy/text-along-path` · dom · lite ✓ · touch ✓ · motion static — Short label set around a circle with a slow spin; a decorative badge or stamp beside a hero. Already fancy/text-along-path. Tags: text, decor, motion. Fits: landing, page.
- **Count Up** · `@kit/fancy/basic-number-ticker` · dom · lite ✓ · touch ✓ · motion static — A number that counts to its value with formatting and decimals: totals, prices, guest counts. Already fancy/basic-number-ticker. Tags: text, data, status, motion. Fits: countdown-ticker-hero, rsvp-guest-list, store, landing, tool.
- **Curved Loop** · `@kit/fancy/text-along-path` · svg · lite ✓ · touch ✓ · motion static — Phrase that loops along an SVG curve and can be dragged; a decorative band under a hero. fancy/text-along-path plus fancy/simple-marquee cover it. Tags: text, marquee, decor, motion. Fits: landing, page.
- **Decrypted Text** · `@kit/fancy/scramble-in` · dom · lite ✓ · touch ✓ · motion static — Label that cycles random glyphs before resolving to real text, once on mount or on view. Already fancy/scramble-in (fancy/scramble-hover for hover). Tags: text, motion, hero. Fits: landing, game-2d.
- **Rotating Text** · `@kit/fancy/text-rotate` · dom · lite ✓ · touch ✓ · motion static — Slot in a headline that cycles phrases with a flip transition. Already fancy/text-rotate. Tags: text, motion, hero. Fits: landing-funnel, landing.
- **Shiny Text** · `@kit/beautiful/shimmer` · css · lite ✓ · touch ✓ · motion static — A sheen that sweeps across a word or label; use it for a thinking line or one hero word, never on body copy or on more than one element per screen. Tags: text, loading, motion, style. Fits: status-thinking, landing-funnel, landing, tool, page.
- **Shuffle** · `@kit/fancy/scramble-in` · dom · lite ✓ · touch ✓ · motion static — Characters shuffle before settling into a short label; fancy/scramble-in already does this once on mount for a kicker, a price or a status word. Tags: text, motion, hero. Fits: landing-funnel, countdown-ticker-hero, landing, page.
- **Split Text** · `@kit/fancy/vertical-cut-reveal` · dom · lite ✓ · touch ✓ · motion static — A headline whose words or letters enter with a stagger; fancy/vertical-cut-reveal is the Kit's version, run once on mount as the screen's hero motion. Tags: text, motion, hero. Fits: landing-funnel, form-confirmation, landing, store, page.
- **Text Loop** · `@kit/fancy/text-along-path` · svg · lite ✓ · touch ✓ · motion static — Text that flows endlessly along a curve; fancy/text-along-path already sets a moving marquee on an SVG arc or circle for a decorative header. Tags: text, marquee, motion, decor. Fits: landing-funnel, gallery, landing, store, page.
- **Text Type** · `@kit/fancy/typewriter` · dom · lite ✓ · touch ✓ · motion static — A typewriter with a blinking caret and adjustable cadence; fancy/typewriter (or arlan/typer when you need the erase) is the Kit's version for a headline or status line. Tags: text, motion, status, hero. Fits: status-thinking, chat-with-agent, landing-funnel, landing, tool, page.
- **ASCII Text** · `build` · three · lite ✗ · touch ✓ · motion static — Hero word drawn as animated ASCII glyphs for a retro terminal feel; a game or lo-fi title, one per screen, using the bundled monospace, still glyphs under reduced motion. The vendored version drags in Three for one word. Tags: text, hero, decor, motion. Fits: game-2d, landing.
- **Blur Text** · `build` · css · lite ✓ · touch ✓ · motion static — Headline or confirmation line that resolves from soft blur to crisp, word by word; a one-shot mount reveal for the hero, never for body copy. Two keyframes per word (blur+translateY to none) with a stagger; the Kit has no blur reveal, nearest is fancy/vertical-cut-reveal. Tags: text, motion, hero. Fits: form-confirmation, landing-funnel, landing, page, tool.
- **Echo Text** · `build` · dom · lite ✓ · touch ✓ · motion static — Ghosted copies converge and settle into one hero word; a single mount entrance for a title, static word under reduced motion. Three or four aria-hidden copies offset by translate and faded, collapsing onto the real word in under 600ms. Tags: text, motion, hero. Fits: landing-funnel, landing.
- **Falling Text** · `build` · dom · lite ✗ · touch ✓ · motion static — Letters drop and bounce into a headline as a playful entrance for a game or party app; one per screen, letters already landed under reduced motion. Exclusions already rule out matter-js components other than elastic-line. Tags: text, motion, hero, game. Fits: game-2d, landing.
- **Fold Text** · `build` · css · lite ✓ · touch ✓ · motion static — Lines unfold into place like creased paper; a one-shot hero entrance for a two or three line title, flat lines under reduced motion. One keyframe: rotateX(-90deg) to 0 with perspective on the parent and alternating transform-origin per line, staggered. Tags: text, motion, hero. Fits: landing-funnel, landing, page.
- **Fuzzy Text** · `build` · canvas2d · lite ✓ · touch ✓ · motion static — Word that vibrates with a fuzzy edge on a small canvas; decorative game title or lo-fi hero, aria-hidden, crisp text under reduced motion. An infinite loop, so it is decoration only and must be aria-hidden with the real text in the DOM for accessibility. Tags: text, motion, decor, game. Fits: game-2d.
- **Glitch Text** · `build` · css · lite ✓ · touch ✓ · motion static — RGB-split jitter over a hero word or game title; a short finite CSS glitch, never on status or body text, still under reduced motion. Pure CSS in under 1 KB. Tags: text, motion, decor, game. Fits: game-2d, landing.
- **Gradient Text** · `build` · css · lite ✓ · touch ✓ · motion static — Headline with a colour gradient sweeping across the glyphs; one hero word or CTA per screen, gradient held still under reduced motion. A few lines of CSS using var(--accent) stops so it survives both themes. Tags: text, style, hero, motion. Fits: landing-funnel, product-page, landing, store.
- **Masked Heading** · `build` · css · lite ✓ · touch ✓ · motion static — Large headline with a drifting gradient (or bundled image) showing through the glyphs, revealed word by word; a landing hero, flat gradient under lite or reduced motion. Compose from Kit parts: fancy/animated-gradient-with-svg behind, background-clip text, fancy/vertical-cut-reveal for the word stagger. Tags: text, hero, motion, media. Fits: landing-funnel, landing.
- **Particle Text** · `build` · canvas2d · lite ✓ · touch ✓ · motion static — Hero word that assembles from drifting particles and scatters again on tap; a Canvas 2D moment for a reveal or game title, assembled text under reduced motion. Sample glyph pixels from an offscreen canvas into target points and ease particles toward them; a few KB, no engine. Tags: text, motion, hero, game. Fits: landing-funnel, landing, game-2d.
- **Split Flap Text** · `build` · dom · lite ✓ · touch ✓ · motion static — A departure board that clacks through phrases tile by tile; the hero for a countdown, a guest count or an event status when fancy/text-rotate feels too soft. Build it: fixed-width tiles set in var(--font-ui) (Azeret Mono is already monospace, so no board font is loaded), each tile a top and bottom half wit… Tags: text, motion, hero, status. Fits: countdown-ticker-hero, rsvp-guest-list, landing-funnel, landing, tool, page.
- **Stroke Text** · `build` · svg · lite ✓ · touch ✓ · motion static — Outlined letters draw themselves on and then fill; a one-shot hero for a confirmation or a launch headline on a screen with no other motion. Build with inline SVG <text> in var(--font-body), stroke var(--ink), stroke-dasharray/dashoffset eased over about 1.2s, then fill-opacity 0 to 1; Web… Tags: text, motion, hero. Fits: form-confirmation, landing-funnel, landing, page.
- **True Focus** · `build` · dom · lite ✓ · touch ✓ · motion static — A focus frame steps across a short phrase, sharpening one word at a time; a finite hero for a three or four word promise on a landing screen, never for copy people must read. Build it: one span per word, the active word filter: none and the rest blur(4px) (opacity .5 under lite/pixel), four corner brackets moved between wo… Tags: text, motion, hero, filter. Fits: landing-funnel, landing, page.
- **Depth Text** · `no` · css · lite ✓ · touch ✗ · motion static — Extruded layered type whose parallax follows the pointer; there is no pointer in Messages, so write the static extrusion as text-shadow and skip the motion. Nearest in the Kit: `@kit/arlan/holo`. Tags: text, cursor, style.
- **Scrambled Text** · `no` · dom · lite ✓ · touch ✗ · motion none — Text that distorts around the cursor; there is no cursor in Messages, so skip it and unscramble a label once on mount with fancy/scramble-in. Nearest in the Kit: `@kit/fancy/scramble-hover`. Tags: text, cursor, motion.
- **Scroll Float** · `no` · dom · lite ✓ · touch ✓ · motion static — Text that drifts and parallax-shifts as the page scrolls; nothing moves on scroll inside the webview, so give a hero a gentle idle drift with fancy/float instead. Tags: text, scroll, motion.
- **Scroll Reveal** · `no` · dom · lite ✓ · touch ✓ · motion static — Words unblur and rise as they scroll into place; scroll-linked reveals are forbidden, so reveal the headline once on mount with fancy/vertical-cut-reveal. Tags: text, scroll, motion, filter.
- **Scroll Velocity** · `no` · dom · lite ✓ · touch ✓ · motion static — A marquee whose speed and skew follow scroll velocity; scroll may not drive motion here, so run a constant strip with fancy/simple-marquee. Tags: text, marquee, scroll, motion.
- **Text Cursor** · `no` · dom · lite ✓ · touch ✗ · motion none — Copies of a word trail the cursor; Messages has no cursor and a drag scrolls the card, so leave it out (fancy/pixel-trail is the finger-safe trail if one is wanted). Nearest in the Kit: `@kit/fancy/pixel-trail`. Tags: text, cursor, motion, decor.
- **Text Pressure** · `no` · dom · lite ✓ · touch ✗ · motion none — Letters swell and squeeze under the pointer through a variable font; it needs a web font and a cursor, both forbidden, so use fancy/breathing-text for an ambient word. Tags: text, cursor, motion, style.
- **Variable Proximity** · `no` · dom · lite ✓ · touch ✗ · motion none — Letter weight and width bend toward the pointer through a variable font; no cursor in Messages and no web fonts under the CSP, so use fancy/breathing-text for an ambient word. Tags: text, cursor, motion, style.
- **Warp Text** · `no` · webgl · lite ✗ · touch ✗ · motion none — A WebGL pass bends and refracts a headline around the pointer; cursor-driven and non-lite, so leave it out; libraries/metal-fx is the one WebGL hero treatment the Kit allows. Tags: text, webgl, cursor, motion, hero.

### Motion, pointer and tap feedback

38 effects: 9 already in the Kit, 0 packed Tier B, 1 to pack, 10 to build, 18 not for a mini-app.

- **Animated Content** · `@kit/air` · css · lite ✓ · touch ✓ · motion static — A block that fades and slides in from a chosen side on mount; reach for Air's --slide-in for a panel's first paint and never tie the entrance to scroll. Tags: motion, layout, hero, scroll. Fits: form-confirmation, approval-card, landing, page, tool.
- **Cubes** · `@kit/fancy/css-box` · css · lite ✓ · touch ✓ · motion static — A small cluster of CSS cubes turning slowly as a hero object; compose it from fancy/css-box, auto-rotating, with hover as an optional extra and never the only input. Tags: hero, decor, motion, ambient. Fits: landing-funnel, landing, page.
- **Cursor Grid** · `@kit/fancy/pixel-trail` · canvas2d · lite ✓ · touch ✓ · motion static — A canvas grid whose cells glow under the pointer or finger and pulse on tap; the Kit's pixel-trail already does this, so use that. Tags: cursor, background, decor, motion. Fits: landing.
- **Logo Loop** · `@kit/fancy/simple-marquee` · dom · lite ✓ · touch ✓ · motion static — Endless strip of logos with seamless repeat and pause on hover. Already covered by fancy/simple-marquee, which takes logos, photos or words and pauses under reduced motion. Tags: marquee, gallery, ambient, layout. Fits: gallery, landing-funnel, landing, store.
- **Meta Balls** · `@kit/arlan/liquid-ui` · webgl · lite ✗ · touch ✓ · motion static — Soft blobs that merge and split as they drift. Already covered: arlan/liquid-ui merges card silhouettes and fancy/gooey-svg-filter goos any animated circles, both without WebGL. Tags: ambient, decor, webgl, motion, background. Fits: landing.
- **Metallic Paint** · `@kit/libraries/metal-fx` · webgl · lite ✗ · touch ✓ · motion static — Liquid chrome shimmer filling an SVG logo or word. Already covered by libraries/metal-fx, which renders a flat plate when WebGL, lite or reduced motion is off. Tags: material, webgl, hero, decor. Fits: countdown-ticker-hero, landing-funnel, landing, store.
- **Orbit Images** · `@kit/fancy/circling-elements` · svg · lite ✓ · touch ✓ · motion static — Images that travel around a custom SVG path. Already covered: fancy/circling-elements orbits avatars or badges around a center, and fancy/text-along-path handles arbitrary paths. Tags: gallery, media, ambient, decor, motion. Fits: rsvp-guest-list, gallery, landing, page.
- **Pixel Trail** · `@kit/fancy/pixel-trail` · three · lite ✗ · touch ✗ · motion none — Squares light up and fade behind the pointer for a retro digital feel; use fancy/pixel-trail instead, which does the same under a finger on a 2D canvas and stays lite. Tags: cursor, background, motion, decor. Fits: landing, game-2d.
- **Star Border** · `@kit/libraries/border-beam` · css · lite ✓ · touch ✓ · motion static — A sparkle border orbits a button to mark the one action that matters; libraries/border-beam already does this around a card or CTA, so use it and keep it to one per screen. Tags: button, decor, motion, style. Fits: approval-card, countdown-ticker-hero, landing-funnel, landing, store, tool.
- **Laser Flow** · `pack` · three · lite ✗ · touch ✓ · motion static — A laser beam that pours light onto a surface behind a hero title. One per screen as the hero motion of a launch or game landing; a flat gradient plate under lite and reduced motion. A shader scene that cannot be rebuilt in CSS, so it qualifies for the restricted allowlist next to Beams and Light Rays. Tags: background, webgl, hero, ambient. Fits: countdown-ticker-hero, landing-funnel, landing, game-3d.
- **Click Spark** · `build` · canvas2d · lite ✓ · touch ✓ · motion none — A short burst of sparks where the finger taps; use it once for the confirming tap (RSVP sent, item added), never as ambient decoration. A dozen lines of Canvas 2D: on pointerdown draw N radial strokes that translate outward and fade. Tags: action, button, motion, decor, game. Fits: rsvp-guest-list, form-confirmation, approval-card, list-add-row, game-2d, store, landing.
- **Electric Border** · `build` · svg · lite ✓ · touch ✓ · motion static — A crackling electric outline around one card; use it for the single live or pending item and fall back to a steady glow under lite or reduced motion. An original is an inline SVG filter (feTurbulence + feDisplacementMap, seed animated on a short loop) applied to a border layer, with a box-shadow gl… Tags: decor, card, motion, filter, style. Fits: approval-card, countdown-ticker-hero, status-thinking, game-2d, landing, store.
- **Glare Hover** · `build` · css · lite ✓ · touch ✗ · motion none — A light glare sweeping across a card on hover; rebuild it as a one-shot CSS sweep on press or on mount, since hover never reveals on touch. Ten lines of CSS: a pseudo-element gradient translated across the card on :active or once on mount. Tags: card, style, motion, decor, media. Fits: gallery, product-page, store, landing.
- **Gradual Blur** · `build` · css · lite ✗ · touch ✓ · motion static — Content that un-blurs from soft to sharp on a trigger. Build it as a one-shot mount reveal (filter: blur to 0 in 300ms), never scroll-driven; sharp from the start under reduced motion. Only the trigger mode is usable: the scroll mode breaks 'nothing moves on scroll inside the webview' and the source's stacked backdrop-filter layers… Tags: filter, motion, hero, scroll. Fits: form-confirmation, landing-funnel, landing, page.
- **Magic Rings** · `build` · webgl · lite ✗ · touch ✗ · motion static — Concentric glowing rings that ripple as the pointer moves. Build it: a few SVG circles with a stroke pulse fired by a tap; rings sit still under reduced motion. Vague description and the renderer is inferred. Tags: decor, ambient, hero, motion. Fits: landing.
- **Noise** · `build` · canvas2d · lite ✗ · touch ✓ · motion static — Film-grain overlay that gives a flat canvas some texture. Build it: SVG feTurbulence as a static tile at low opacity; animate only in atmosphere and only as aria-hidden decoration. A continuously redrawn full-screen canvas burns battery inside Messages. Tags: ambient, material, background, decor. Fits: landing, game-2d, page.
- **Pixel Swap** · `build` · three · lite ✗ · touch ✓ · motion static — A grid of pixel blocks covers a card, the content swaps underneath, and the blocks dissolve. Build it as a CSS grid with staggered opacity for a tap-to-flip card or a confirmation swap. Renderer is a guess; the original may be Three-based, but the effect is a DOM grid of squares with staggered transitions settling in 300ms, and it co… Tags: card, motion, media, decor. Fits: gallery, form-confirmation, landing, game-2d, store.
- **Pixel Transition** · `build` · dom · lite ✓ · touch ✗ · motion static — A pixel-grid dissolve swaps one face of a card for another on tap: a photo to its caption, a price to its detail. One per screen; both faces ship in the bundle. Weight is for the gsap original; an original build is a CSS grid of squares with per-cell animation-delay from a CSS variable, toggled by a tap, abou… Tags: card, media, motion, decor. Fits: gallery, product-page, landing, store, page.
- **Sticker Peel** · `build` · dom · lite ✓ · touch ✓ · motion static — A sticker whose corner lifts and peels when you tap or drag it: a 'going' badge on an RSVP, a 'confirmed' seal on a receipt. One per screen; the image lives in the bundle. Weight is for the gsap+Draggable original; an original build is a CSS 3D rotate on the corner, clip-path for the peeled flap and a transform-driven s… Tags: media, decor, motion, card. Fits: rsvp-guest-list, form-confirmation, gallery, landing, store, page.
- **Strands** · `build` · webgl · lite ✗ · touch ✓ · motion static — Glowing strands ripple and weave behind a landing hero; build it as a few translucent Canvas 2D curves so it stays lite, and reach for packed Beams or Silk only when a recipe wants the shader look. Original build: four to six sine-offset bezier curves on a Canvas 2D at DPR 1, stroked two or three times at widening lineWidth and falling alpha wit… Tags: background, ambient, webgl, hero. Fits: landing-funnel, countdown-ticker-hero, landing.
- **Antigravity** · `no` · three · lite ✗ · touch ✗ · motion static — A 3D particle field that scatters away from the mouse cursor; a desktop pointer toy with no finger equivalent and no place inside Messages. Tags: cursor, webgl, background, ambient, motion.
- **Blob Cursor** · `no` · dom · lite ✓ · touch ✗ · motion none — A gooey blob that trails the mouse pointer with inertia; there is no pointer on a phone, so it has no use in a mini-app. Nearest in the Kit: `@kit/libraries/liquid-gooey`, `@kit/fancy/gooey-svg-filter`. Tags: cursor, motion, decor.
- **Crosshair** · `no` · dom · lite ✓ · touch ✗ · motion none — A custom crosshair cursor that tracks the mouse and reacts to links; replacing the cursor means nothing on touch. Tags: cursor, nav, decor.
- **Elastic Mesh** · `no` · canvas2d · lite ✗ · touch ✗ · motion static — A full-surface spring mesh that stretches under the pointer and settles; a hover physics playground, inert under a thumb and too heavy for a card. Nearest in the Kit: `@kit/fancy/elastic-line`. Tags: cursor, background, motion, material.
- **Fade Content** · `no` · css · lite ✓ · touch ✓ · motion static — A block that fades or slides in once it scrolls into view; nothing reveals on scroll inside Messages, so use Air's --slide-in on mount instead. Tags: motion, layout, scroll.
- **Ghost Cursor** · `no` · webgl · lite ✗ · touch ✗ · motion none — A translucent trail that follows the mouse cursor; cursor followers do not exist on touch and are out of scope for a mini-app. Tags: cursor, webgl, motion, decor.
- **Glow Cursor** · `no` · webgl · lite ✗ · touch ✗ · motion none — A shader light trail drawn behind the mouse pointer; WebGL, cursor-bound and non-lite, so it is not for a mini-app. Tags: cursor, webgl, motion, decor.
- **Halftone Reveal** · `no` · webgl · lite ✗ · touch ✗ · motion static — Halftone dot screen over an image that sharpens where the pointer is. Not used: a hover-revealed image is a hover-only affordance and there is no cursor in Messages. Tags: media, cursor, filter, decor.
- **Image Trail** · `no` · dom · lite ✓ · touch ✗ · motion none — Images that spawn and fade along the pointer's path. Not used: cursor-following decoration has no meaning on a 390px touch card; show photos with fancy/simple-carousel. Nearest in the Kit: `@kit/fancy/simple-carousel`. Tags: cursor, media, gallery, motion.
- **Magnet** · `no` · dom · lite ✓ · touch ✗ · motion none — A button that leans toward the cursor and springs back. Not used: there is no cursor in Messages; a 44px target with a press state is the touch affordance. Tags: button, cursor, motion.
- **Magnet Lines** · `no` · dom · lite ✓ · touch ✗ · motion none — A grid of short lines that rotate to point at the cursor. Not used: cursor-following decoration with no touch equivalent, and hundreds of nodes for a 390px card. Nearest in the Kit: `@kit/fancy/pixel-trail`. Tags: cursor, decor, background, motion.
- **Ribbons** · `no` · webgl · lite ✗ · touch ✗ · motion none — Physics ribbons stream behind the pointer; there is no pointer in Messages and the layer is empty at rest, so the Kit does not use it. Tags: cursor, motion, webgl, decor.
- **Ripple Distortion** · `no` · webgl · lite ✗ · touch ✗ · motion static — A water-displacement shader warps an image under the pointer and trails a wake; a pointer-following distortion on a WebGL canvas has no place in a touch-only, lite-first card. Tags: cursor, media, webgl, filter.
- **Scroll Expand** · `no` · dom · lite ✓ · touch ✓ · motion static — A rounded media frame grows to full bleed as the page scrolls; nothing moves on scroll inside the webview, so the Kit does not use it. Use a full-bleed .hero from the start. Tags: scroll, media, hero, motion.
- **Shape Blur** · `no` · three · lite ✗ · touch ✗ · motion none — A blurred geometric outline morphs toward the pointer on hover; hover-only and Three-rendered, it fails touch-first and lite. For an ambient blob use fancy/animated-gradient-with-svg. Nearest in the Kit: `@kit/fancy/animated-gradient-with-svg`. Tags: cursor, ambient, webgl, decor.
- **Splash Cursor** · `no` · webgl · lite ✗ · touch ✗ · motion none — A fluid-simulation splash bursts under the cursor and curls into waves; a full-page cursor effect on WebGL is off-limits in Messages. Tags: cursor, webgl, motion, ambient.
- **Swarm Cursor** · `no` · webgl · lite ✗ · touch ✗ · motion none — A flocking particle swarm chases the pointer and scatters at rest; a cursor-following WebGL layer has no role on a touch-only card in Messages. Tags: cursor, webgl, motion, ambient.
- **Target Cursor** · `no` · dom · lite ✓ · touch ✗ · motion none — Four corner brackets replace the mouse cursor and snap onto hovered targets; Messages has no cursor, so the Kit does not use it. Use a focus ring on the .item instead. Tags: cursor, nav, motion, decor.

### Cards, lists, galleries, navigation

45 effects: 17 already in the Kit, 0 packed Tier B, 1 to pack, 15 to build, 12 not for a mini-app.

- **Border Glow** · `@kit/libraries/border-beam` · css · lite ✓ · touch ✗ · motion none — A glowing border that chases the cursor; the Kit's border-beam already marks the one card that deserves attention without needing a pointer. Tags: decor, card, cursor, style. Fits: approval-card, landing, store.
- **Carousel** · `@kit/fancy/simple-carousel` · dom · lite ✓ · touch ✓ · motion reduced — Swipeable carousel with loop and snap; fancy/simple-carousel is the gallery default and already covers it. Tags: carousel, gallery, layout, media. Fits: gallery, product-page, store, landing, page.
- **Circular Gallery** · `@kit/fancy/simple-carousel` · webgl · lite ✗ · touch ✓ · motion static — WebGL ring of bent images you drag to spin; a gallery on a phone uses fancy/simple-carousel, since lite forbids WebGL and the fallback is the carousel anyway. Tags: gallery, media, webgl, carousel. Fits: gallery, store, landing.
- **Counter** · `@kit/fancy/basic-number-ticker` · dom · lite ✓ · touch ✓ · motion static — Digits that roll to a value with easing; fancy/basic-number-ticker already counts RSVP totals, prices and countdowns. Tags: data, text, motion, status. Fits: countdown-ticker-hero, rsvp-guest-list, landing, store, tool.
- **Dome Gallery** · `@kit/fancy/simple-carousel` · css · lite ✗ · touch ✓ · motion static — Photos wrapped on a draggable 3D dome as a full-screen showpiece; on a 390px card use the gallery recipe's simple-carousel, or css-box for six faces. Tags: gallery, media, hero, motion. Fits: gallery, page, landing.
- **Drift Wall** · `@kit/fancy/simple-marquee` · css · lite ✗ · touch ✓ · motion static — An endless perspective wall of tiles drifting past as ambient decor behind a hero; the Kit's simple-marquee tilted with a CSS perspective does the same job. Tags: marquee, ambient, decor, motion, background. Fits: landing-funnel, gallery, landing.
- **Flowing Menu** · `@kit/beautiful/glide-menu` · dom · lite ✓ · touch ✓ · motion reduced — A highlight that slides between tabs or menu rows as the active item changes; use for segmented controls and small nav, never as a hover reveal. Tags: menu, nav, motion. Fits: approval-card, tool, page.
- **Glass Icons** · `@kit/arlan/color-depth` · css · lite ✗ · touch ✓ · motion none — Frosted-glass icon tiles for a feature row or action grid; on Air use the color-depth glass material so lite surfaces get layered gradients, not blur. Tags: button, style, material. Fits: landing-funnel, landing, page.
- **Glass Surface** · `@kit/air` · mixed · lite ✗ · touch ✓ · motion n/a — A refractive glass panel with edge lighting; Air's .panel with var(--blur) is the glass in atmosphere, and pixel or lite render it flat. Tags: material, style, layout, filter. Fits: landing-funnel, product-page, landing, page, store.
- **Gooey Nav** · `@kit/libraries/liquid-gooey` · mixed · lite ✓ · touch ✓ · motion reduced — A tab thumb that stretches like liquid and splits into droplets as it moves to the tapped item; use liquid-gooey, one nav per screen. Tags: nav, menu, motion, filter. Fits: product-page, tool, page, store.
- **Infinite Menu** · `@kit/fancy/simple-carousel` · webgl · lite ✗ · touch ✓ · motion static — A menu of items that wraps endlessly as you drag or swipe; on a phone a snap carousel or a wrapping chip row covers it without the WebGL sphere. Tags: menu, nav, carousel, webgl. Fits: gallery, page, store.
- **Infinite Spiral** · `@kit/fancy/simple-carousel` · css · lite ✗ · touch ✓ · motion static — Images on an endlessly turning 3D helix as a hero gallery; heavy, non-lite, and covered on a phone by simple-carousel or css-box. Tags: gallery, media, hero, motion. Fits: gallery, landing, page.
- **Pill Nav** · `@kit/beautiful/glide-menu` · dom · lite ✓ · touch ✓ · motion reduced — Row of pills with a highlight that slides to the active one; the section switcher for a tool or page app with two to four sections. Tags: nav, menu, pill, motion. Fits: tool, page, store, landing.
- **Profile Card** · `@kit/arlan/holo` · css · lite ✗ · touch ✗ · motion static — Portrait card with a glare that tilts in 3D; the host or artist card at the top of a landing screen, one per screen, built on holo. Tags: card, motion, material, hero. Fits: landing-funnel, landing, page.
- **Scroll Stack** · `@kit/fancy/stacking-cards` · dom · lite ✓ · touch ✓ · motion static — Cards that pin and pile up as the page scrolls; a scroll-linked landing section the Kit only allows as sticky stacking-cards, never as JS driven by scroll position. Tags: scroll, card, layout, motion. Fits: landing-funnel, landing, page.
- **Specular Button** · `@kit/libraries/border-beam` · webgl · lite ✗ · touch ✗ · motion static — Glass button with a bright rim light sweeping its edge; the one primary action on a screen, drawn with border-beam over a color-depth glass material. Tags: button, material, webgl, motion, action. Fits: approval-card, landing-funnel, landing, store.
- **Tilted Card** · `@kit/arlan/holo` · css · lite ✓ · touch ✗ · motion static — Card that tilts in perspective toward the pointer; on a phone use holo, which tilts with device orientation and adds the foil only if you want it. Tags: card, motion, media, hero. Fits: product-page, landing-funnel, landing, store, page.
- **Morph Slider** · `pack` · webgl · lite ✗ · touch ✓ · motion static — Hero image slider that melts one photo into the next through a displacement shader; one per landing or product hero, a plain crossfade or still under lite. A melt/displacement between images cannot be rebuilt well without a shader, and a photo hero for a store or landing is a legitimate mini-app use, so it earns a restricte… Tags: carousel, gallery, media, webgl, hero. Fits: gallery, landing-funnel, product-page, landing, store.
- **Animated List** · `build` · dom · lite ✓ · touch ✓ · motion static — List rows that enter with a short stagger on mount; use it once as the hero motion when a guest list or task list first appears, never on scroll. scroll_linked=true describes the source component: it reveals rows as they scroll into view inside their own container and fades the edges by scroll… Tags: list, motion. Fits: rsvp-guest-list, list-add-row, tool, page, landing.
- **Bounce Cards** · `build` · dom · lite ✓ · touch ✓ · motion static — A fan of cards that springs into place on mount; a hero motion for a gallery or product spread, settling to the same fanned layout under reduced motion. React Bits pulls in GSAP for a spring; an original build is a CSS keyframe with an overshoot cubic-bezier and per-card delay, well under 1 KiB. Tags: card, motion, gallery, hero. Fits: gallery, landing, store.
- **Bubble Menu** · `build` · dom · lite ✓ · touch ✓ · motion static — A round toggle that expands into a staggered ring of action bubbles; use for a compact plus-menu of three to five actions, opened by tap. React Bits uses GSAP for the stagger; an original is CSS transitions with transition-delay on each bubble and 44px targets. Tags: menu, nav, action, motion. Fits: tool, page, landing.
- **Card Nav** · `build` · dom · lite ✓ · touch ✓ · motion static — A header that expands into card panels of grouped links; rarely needed inside Messages, where one screen and header.bar carry navigation, build as a plain disclosure if a landing needs it. A website nav pattern; mini-apps are one screen with header.bar and .navlink from shell.css. Tags: nav, menu, layout. Fits: landing-funnel, landing, page.
- **Card Swap** · `build` · dom · lite ✓ · touch ✓ · motion static — A small stack of cards where the front card drops to the back on a timer; a testimonial or feature rotator for a landing, paused to the front card under reduced motion. React Bits drives it with a GSAP timeline; an original is a 3-card stack with CSS transforms and a JS interval that rotates z-order, ~1 KiB. Tags: card, carousel, motion, hero. Fits: gallery, landing-funnel, landing, store.
- **Depth Carousel** · `build` · dom · lite ✓ · touch ✓ · motion reduced — Coverflow-style rail where cards recede into perspective as you drag; build with CSS 3D on top of simple-carousel's snap when a product spread needs depth. Start from fancy/simple-carousel and add perspective, per-slot rotateY/translateZ and opacity; no library needed. Tags: carousel, card, gallery, motion. Fits: gallery, product-page, store, landing.
- **Elastic Slider** · `build` · dom · lite ✓ · touch ✓ · motion reduced — A range slider whose thumb overshoots and springs back on release; use for one tunable value (tip, quantity, budget) where the flourish confirms the drop. The 'spring physics' is a motion-library spring, not a physics engine. Tags: input, motion, data. Fits: form-confirmation, tool, store.
- **Folder** · `build` · css · lite ✓ · touch ✓ · motion reduced — A folder icon that opens on tap to fan out two or three cards; a playful disclosure for a small photo set or the items an agent gathered. Simple enough to write under the contract: a 44px tappable folder colored with var(--accent), papers translated and rotated with transform over 300ms… Tags: card, motion, decor, layout. Fits: gallery, page, landing.
- **Magic Bento** · `build` · dom · lite ✓ · touch ✗ · motion static — A bento grid of tiles with tap-to-expand; build it from Air's .grid and .tile and drop the cursor spotlight, glow and magnetism that need a mouse. React Bits' version is a gsap grid whose signature effects (cursor spotlight, border glow, particles, tilt, magnetism) are pointer-position driven an… Tags: layout, card, cursor, motion. Fits: landing-funnel, product-page, landing, store.
- **Masonry** · `build` · dom · lite ✓ · touch ✓ · motion static — Two-column grid of mixed-height photo tiles that settle into place once on load; the gallery grid when images vary in shape, never a layout for text. Source needs gsap for the animated reflow (heavy). Tags: layout, gallery, media, list. Fits: gallery, product-page, store, page, landing.
- **Option Wheel** · `build` · dom · lite ✓ · touch ✓ · motion static — Curved drum picker you drag to choose one value from a short list; use it when the pick is the screen's hero, otherwise reach for a native select. Not scroll-linked in the doctrine sense: it consumes wheel/drag deltas as input, it does not react to page scroll position. Tags: input, list, motion. Fits: rsvp-guest-list, form-confirmation, tool, store.
- **Pixel Card** · `build` · canvas2d · lite ✓ · touch ✗ · motion static — Card whose surface fills with animated pixel squares as it appears; a once-on-mount reveal for one featured card, triggered by mount or tap, never hover. Simple to build original: a canvas grid of squares that fade or scale in with per-cell delay, run once on mount or on tap, then the canvas goes inert… Tags: card, decor, motion, media. Fits: product-page, landing, page.
- **Stack** · `build` · dom · lite ✓ · touch ✓ · motion static — Deck of cards you swipe to send the top one to the back; a gallery or guest-photo hero when a flat horizontal carousel is not enough. Straightforward to build original with pointer events and transforms: three to five cards offset by a few px and a slight rotation, drag the top past… Tags: card, carousel, gallery, motion, media. Fits: gallery, landing, store, page.
- **Staggered Menu** · `build` · dom · lite ✓ · touch ✓ · motion static — Overlay menu whose items slide in one after another on open; the nav for a multi-section page or tool app, opened from the header bar. The stagger is animation-delay per row on a transform/opacity keyframe; no library needed, and the whole open must settle in about 300ms so the last… Tags: menu, nav, motion, layout. Fits: page, tool, landing.
- **Stepper** · `build` · dom · lite ✓ · touch ✓ · motion static — Multi-step form with a progress indicator and animated step changes; use it for RSVP, checkout and any form longer than one compact card. A wizard is ordinary DOM: a row of dots (done/active/pending, the same states as aicss/todo-list) and one visible step panel with a ≤300ms slide betw… Tags: input, status, layout, motion, nav. Fits: form-confirmation, rsvp-guest-list, landing-funnel, product-page, tool, store, landing.
- **Accordion Gallery** · `no` · dom · lite ✓ · touch ✗ · motion none — Hover-widening panels that reveal parallax imagery and captions; not for a mini-app, hover never reveals and there is no cursor to parallax, so a gallery uses fancy/simple-carousel. Nearest in the Kit: `@kit/fancy/simple-carousel`. Tags: gallery, media, layout, cursor.
- **Chroma Grid** · `no` · css · lite ✗ · touch ✗ · motion none — Grayscale tiles that regain color under a cursor spotlight; not for a mini-app, hover never reveals and the backdrop-filter overlay is non-lite, use an Air .grid of .tile. Tags: gallery, card, cursor, filter.
- **Curved Input** · `no` · svg · lite ✓ · touch ✓ · motion n/a — An input bent along an arc with a fake caret; not for a mini-app, inputs stay native 16px controls, and decorative arc text is fancy/text-along-path. Nearest in the Kit: `@kit/aicss/agent-input`, `@kit/beautiful/chat-composer`. Tags: input, composer, text, decor.
- **Decay Card** · `no` · mixed · lite ✓ · touch ✗ · motion none — A card whose image displaces and dissolves as the pointer moves; not for a mini-app, there is no cursor to drive it and hover never carries meaning. Nearest in the Kit: `@kit/arlan/holo`. Tags: card, media, cursor, filter, motion.
- **Dock** · `no` · dom · lite ✓ · touch ✗ · motion none — Icon bar that magnifies items near the cursor; not for a mini-app, proximity scaling has no touch equivalent and it recreates Apple's dock, use a 44px tab row. Tags: nav, menu, cursor, motion.
- **Fluid Glass** · `no` · three · lite ✗ · touch ✗ · motion static — A Three.js lens that refracts the page under a pointer-following glass blob; a desktop showpiece with no place inside a 390px lite card. Tags: material, webgl, cursor, style.
- **Flying Posters** · `no` · webgl · lite ✗ · touch ✓ · motion static — Posters on a WebGL ribbon that spin as the page scrolls; scroll-linked motion is forbidden in the webview, so it has no mini-app use. Nearest in the Kit: `@kit/fancy/simple-carousel`. Tags: scroll, gallery, webgl, media.
- **Lanyard** · `no` · three · lite ✗ · touch ✓ · motion static — A badge card swinging on a physics-simulated lanyard; three, rapier and GLB assets make it a desktop showcase, not a Messages card. Nearest in the Kit: `@kit/arlan/holo`. Tags: card, hero, motion, webgl.
- **Line Sidebar** · `no` · dom · lite ✓ · touch ✗ · motion none — A sidebar list whose items shift toward the cursor; there is no cursor in Messages and no sidebar in a single column, so skip it. Nearest in the Kit: `@kit/beautiful/glide-menu`. Tags: nav, menu, cursor, list.
- **Model Viewer** · `no` · three · lite ✗ · touch ✓ · motion static — Orbitable Three.js viewer for a 3D model file; not for Messages, where the model is a remote asset, WebGL is off under lite and the card is 390px wide. Tags: media, webgl, hero.
- **Reflective Card** · `no` · mixed · lite ✗ · touch ✗ · motion none — Card that mirrors the viewer's webcam and glares with the cursor; needs camera permission and a mouse, neither of which Messages gives you. Nearest in the Kit: `@kit/arlan/color-depth`, `@kit/libraries/metal-fx`. Tags: card, cursor, media, material.
- **Spotlight Card** · `no` · css · lite ✓ · touch ✗ · motion static — Card lit by a gradient spotlight under the cursor; there is no cursor in Messages, and a still highlight is a two-line CSS glow the Builder writes inline. Nearest in the Kit: `@kit/libraries/border-beam`. Tags: card, cursor, decor, style.

### Backgrounds and fields

57 effects: 8 already in the Kit, 13 packed Tier B, 6 to pack, 24 to build, 6 not for a mini-app.

- **Aurora** · `@kit/fancy/animated-gradient-with-svg` · webgl · lite ✗ · touch ✓ · motion static — Slow ribbons of color flowing behind a headline; use fancy/animated-gradient-with-svg in aurora tones instead of a shader. Tags: background, ambient, hero, motion. Fits: landing-funnel, countdown-ticker-hero, rsvp-guest-list, landing, page.
- **Color Bends** · `@kit/fancy/animated-gradient-with-svg` · webgl · lite ✗ · touch ✓ · motion static — Bands of vivid color bending and flowing behind a panel; use fancy/animated-gradient-with-svg, or Grainient from the restricted set for the richer look. Tags: background, ambient, hero, motion. Fits: landing-funnel, form-confirmation, landing, page.
- **Dark Veil** · `@kit/fancy/animated-gradient-with-svg` · webgl · lite ✗ · touch ✓ · motion static — A barely moving dark haze behind a chat or tool screen; the atmosphere canvas with fancy/animated-gradient-with-svg in dark tones already gives it. Tags: background, ambient, motion. Fits: chat-with-agent, status-thinking, page, tool, landing.
- **Dot Field** · `@kit/fancy/pixel-trail` · canvas2d · lite ✗ · touch ✗ · motion static — A dot grid that bulges and sparkles under the cursor; on touch use fancy/pixel-trail, and a plain dot grid is one CSS radial-gradient. Tags: background, cursor, decor, motion. Fits: landing, game-2d.
- **Dot Grid** · `@kit/fancy/pixel-trail` · canvas2d · lite ✗ · touch ✗ · motion static — A dot grid that shoves away from the cursor; on touch use fancy/pixel-trail, and a still dot grid is one CSS radial-gradient. Tags: background, cursor, decor, layout. Fits: landing, tool.
- **Orb** · `@kit/aicss/orbs` · webgl · lite ✗ · touch ✗ · motion static — A glowing energy orb that warps under the pointer; the Kit already has aicss/orbs for an ambient presence and libraries/thinking-orbs for named states, so use those. Tags: status, ambient, thinking, webgl, decor. Fits: status-thinking, chat-with-agent, tool, page.
- **Plasma** · `@kit/fancy/animated-gradient-with-svg` · webgl · lite ✗ · touch ✓ · motion static — Soft plasma gradients that swirl and morph; fancy/animated-gradient-with-svg gives the same ambient wash in lite CSS, and packed Grainient or Silk give the shader look. Tags: background, ambient, motion, webgl. Fits: landing-funnel, landing, page.
- **Soft Aurora** · `@kit/fancy/animated-gradient-with-svg` · webgl · lite ✗ · touch ✓ · motion static — Slow aurora-like color drift behind a panel; reach for fancy/animated-gradient-with-svg, or a packed shader gradient (Grainient, Silk) when non-lite. Tags: background, ambient, hero, webgl, motion. Fits: landing-funnel, form-confirmation, landing, page.
- **Beams** · `@kit/restricted/beams` · three · lite ✗ · touch ✓ · motion static — Crossing light ribbons behind a landing or product hero; compiled from the restricted artifact, non-lite, one per app. Tags: background, webgl, hero, ambient. Fits: landing-funnel, product-page, countdown-ticker-hero, landing, store.
- **Dither** · `@kit/restricted/dither` · three · lite ✗ · touch ✓ · motion static — Retro dithered noise drifting behind a pixel-styled hero or 2D game; compiled from the restricted artifact, non-lite, one per app. Tags: background, webgl, ambient, filter, game. Fits: game-2d-canvas, landing-funnel, game-2d, landing.
- **Faulty Terminal** · `@kit/restricted/faulty-terminal` · webgl · lite ✗ · touch ✓ · motion static — Flickering CRT terminal squares behind a status or retro game screen; compiled from the restricted artifact, non-lite, one per app. Tags: background, webgl, ambient, status, game. Fits: status-thinking, game-2d-canvas, game-2d, tool, landing.
- **Galaxy** · `@kit/restricted/galaxy` · webgl · lite ✗ · touch ✓ · motion static — Twinkling starfield behind a launch countdown or night-event hero; one per app, aria-hidden, never under lite, with pointer parallax switched off. Tags: background, webgl, ambient, hero, motion. Fits: countdown-ticker-hero, landing-funnel, landing, game-3d, page.
- **Grainient** · `@kit/restricted/grainient` · webgl · lite ✗ · touch ✓ · motion static — Grainy swirling gradient with soft wave distortion; the packed painterly backdrop for a landing or store hero with one display line over it. Tags: background, webgl, ambient, hero, material. Fits: landing-funnel, product-page, landing, store, page.
- **Iridescence** · `@kit/restricted/iridescence` · webgl · lite ✗ · touch ✓ · motion static — Pearlescent shader with slow shifting waves; the packed glossy backdrop for a product or store hero that wants a material feel behind a single display line. Tags: background, webgl, material, hero, ambient. Fits: product-page, landing-funnel, store, landing.
- **Light Rays** · `@kit/restricted/light-rays` · webgl · lite ✗ · touch ✓ · motion static — Volumetric rays falling from a chosen edge; the packed backdrop for a confirmation or reveal hero where light from above is the only motion on the screen. Tags: background, webgl, ambient, hero, decor. Fits: form-confirmation, landing-funnel, countdown-ticker-hero, landing, page.
- **Liquid Chrome** · `@kit/restricted/liquid-chrome` · webgl · lite ✗ · touch ✓ · motion static — A flowing chrome surface behind a hero word or product; import @kit/restricted/liquid-chrome for one non-lite hero, or reach for libraries/metal-fx when you need Tier A. Tags: background, material, webgl, hero, ambient. Fits: landing-funnel, product-page, landing, store.
- **Liquid Ether** · `@kit/restricted/liquid-ether` · three · lite ✗ · touch ✓ · motion static — Fluid color that swirls under a finger and drifts on its own; import @kit/restricted/liquid-ether for a non-lite landing hero that invites a touch. Tags: background, webgl, ambient, hero, motion. Fits: landing-funnel, landing, game-3d.
- **Molten Metal** · `@kit/restricted/molten-metal` · webgl · lite ✗ · touch ✓ · motion static — Molten plasma filaments with white-hot cores; import @kit/restricted/molten-metal for a dramatic non-lite game or launch hero, one per app. Tags: background, webgl, hero, ambient, material. Fits: game-3d-three, landing-funnel, game-3d, landing.
- **Prism** · `@kit/restricted/prism` · webgl · lite ✗ · touch ✓ · motion static — A rotating glass prism throwing colored light; import @kit/restricted/prism as the one non-lite hero of a landing or store screen. Tags: background, webgl, hero, ambient. Fits: landing-funnel, product-page, landing, store.
- **Side Rays** · `@kit/restricted/side-rays` · webgl · lite ✗ · touch ✓ · motion static — Light rays fanning in from one edge behind a hero word or product shot; already packed as @kit/restricted/side-rays, non-lite, flat plate when off. Tags: background, hero, webgl, ambient, decor. Fits: landing-funnel, product-page, countdown-ticker-hero, landing, store.
- **Silk** · `@kit/restricted/silk` · three · lite ✗ · touch ✓ · motion static — Slow silk-like waves with soft lighting behind a hero; already packed as @kit/restricted/silk, non-lite, renders a flat gradient when WebGL is off. Tags: background, hero, webgl, material, ambient. Fits: landing-funnel, product-page, form-confirmation, landing, store, page.
- **Aero Shards** · `pack` · three · lite ✗ · touch ✓ · motion static — A 3D field of folded foil shards drifting behind a landing or product hero; non-lite, one per app, flat plate under lite and reduced motion. Worth packing: lit, folded 3D geometry moved by wind cannot be rebuilt honestly in CSS or Canvas 2D, and 'content-safe placements' means it is designed to sit behind cop… Tags: background, webgl, hero, material, ambient. Fits: landing-funnel, product-page, landing, store.
- **CRT Warp** · `pack` · webgl · lite ✗ · touch ✓ · motion static — A curved CRT plasma with scanlines and bloom for a retro game title screen; non-lite, one per app, flat frame under reduced motion, pointer part off. Low-priority candidate: plasma plus barrel warp plus bloom genuinely needs a shader, and a retro game-2d title screen is a legitimate use. Tags: background, webgl, ambient, hero, game. Fits: game-2d-canvas, game-2d, landing.
- **Ferrofluid** · `pack` · webgl · lite ✗ · touch ✓ · motion static — Churning fluid drawn as glowing contour lines behind a landing hero; non-lite, one per app, cursor magnet off or bound to a drag. Low-priority candidate: a fluid field with marching contour lines needs a shader, and the topographic look is distinct from Liquid Ether, Liquid Chrome and Molten Metal. Tags: background, webgl, ambient, material, cursor. Fits: landing-funnel, landing.
- **Ghost Fibers** · `pack` · webgl · lite ✗ · touch ✓ · motion static — Deep-blue twisting fiber field with glowing bands; a calm atmospheric backdrop for a landing hero or evening invite when the packed Silk and Beams feel too plain. Genuinely needs a shader (recursive noise, radial twist, bloom), so not a build-original. Tags: background, webgl, ambient, hero, motion. Fits: landing-funnel, rsvp-guest-list, landing, page.
- **Lightfall** · `pack` · webgl · lite ✗ · touch ✗ · motion static — A tunnel of colored light streaks raining toward the viewer; use once as the splash hero of a game-3d or launch screen when Beams and Light Rays feel too calm. Overlaps packed Beams, Light Rays and Side Rays, so pack only if a template asks for a hyperspace look. Tags: background, webgl, hero, ambient, motion. Fits: game-3d-three, landing-funnel, game-3d, landing.
- **Plasma Wave** · `pack` · webgl · lite ✗ · touch ✓ · motion static — Raymarched plasma sheets with two interfering waves; a non-lite hero for a game-3d or launch screen when Molten Metal and Silk are already spoken for. Needs a shader, so it cannot be rebuilt, but it sits close to packed Molten Metal, Silk and Liquid Ether. Tags: background, webgl, hero, ambient, motion. Fits: game-3d-three, landing-funnel, game-3d, landing.
- **Acid Squares** · `build` · webgl · lite ✗ · touch ✓ · motion static — A square tunnel receding into depth behind a title; build it as concentric squares scaling in Canvas 2D for a game title screen, not as a page-wide shader. The React Bits version is a refractive 'crystalline' shader; the idea (nested squares zooming toward the viewer) is about 1 KiB of Canvas 2D or CSS p… Tags: background, ambient, hero, webgl. Fits: countdown-ticker-hero, game-2d-canvas, game-2d, game-3d, landing.
- **Evil Eye** · `build` · webgl · lite ✗ · touch ✓ · motion static — A single animated eye with a slit pupil and fiery glow as a hero mark; draw it in SVG with a dilating pupil and one glow, one per screen. A fixed-size mark, not a background, so it does not need a shader: radial-gradient iris, an ellipse pupil animated on transform (scaleX) for the slit… Tags: hero, decor, ambient, webgl. Fits: countdown-ticker-hero, landing, game-2d.
- **Gradient Blinds** · `build` · webgl · lite ✗ · touch ✗ · motion static — Bold diagonal gradient stripes behind a store or landing hero when a flat token gradient feels too quiet; the pointer spotlight means nothing on touch, so leave it out. Original approach: repeating-linear-gradient stripes over a two-stop token gradient, a fixed radial highlight in place of the cursor spotlight, optio… Tags: background, style, decor, hero. Fits: landing-funnel, product-page, landing, store.
- **Gradient Waves** · `build` · webgl · lite ✗ · touch ✓ · motion static — Stacked sine waves rolling toward a hazy horizon; a calm hero backdrop for a landing or countdown screen where the slow roll is the only motion, flat and still under lite. The look (stacked waves fading to a horizon) does not need raymarching: three to five SVG sine paths with shrinking amplitude and spacing, a token gr… Tags: background, ambient, hero, motion, decor. Fits: landing-funnel, countdown-ticker-hero, landing, page.
- **Grid Scan** · `build` · three · lite ✗ · touch ✓ · motion static — A perspective grid room with one light plane sweeping through it; the hero backdrop for a retro game or tool screen, with the sweep as the screen's single hero motion. Original approach: a preserve-3d container with floor and wall faces filled by repeating-linear-gradient grids in var(--accent), and a single transla… Tags: background, game, hero, motion, decor. Fits: game-2d-canvas, game-3d-three, game-2d, game-3d, tool.
- **Letter Glitch** · `build` · canvas2d · lite ✗ · touch ✓ · motion static — Matrix-style grid of glyphs flickering on a 2D canvas; build an original for a terminal-themed game or tool backdrop, in the system mono font, finite and aria-hidden. Simple to rebuild: a canvas 2D grid of glyphs in var(--font-ui) or system monospace (no web font), random cell swaps at a fixed interval, and a CSS r… Tags: background, text, ambient, game, motion. Fits: game-2d-canvas, game-2d, tool.
- **Light Pillar** · `build` · three · lite ✗ · touch ✓ · motion static — One glowing vertical column of light behind a headline or countdown number; use it when a hero needs a focal glow without a shader, flat and unblurred under lite. Original approach: a narrow linear-gradient column from transparent through var(--accent) and back, with two wider low-opacity copies for the glow; a… Tags: background, hero, decor, ambient, motion. Fits: countdown-ticker-hero, landing-funnel, landing, page, store.
- **Light Tunnel** · `build` · webgl · lite ✗ · touch ✓ · motion static — Radial strands converging to a vanishing point with pulses racing inward; a loud launch or game hero backdrop, one pulse cycle as the hero motion, still under reduced motion. The fibre-optic read is many thin bright lines from a center point with brighter segments moving along them; canvas 2D draws that directly, with a ra… Tags: background, game, hero, motion, ambient. Fits: game-3d-three, countdown-ticker-hero, game-3d, game-2d, landing.
- **Lightning** · `build` · webgl · lite ✗ · touch ✓ · motion static — A branching lightning bolt with glow flicker; strike once as the hero moment of a storm, power-up or countdown-hits-zero screen, never as a permanent flashing loop. Rebuild on Canvas 2D: midpoint-displacement polyline with two or three branches, shadowBlur glow, one-shot flicker under 300ms. Tags: background, hero, motion, decor, game. Fits: countdown-ticker-hero, game-2d-canvas, game-2d, landing.
- **Line Waves** · `build` · webgl · lite ✗ · touch ✓ · motion static — Stacked sine lines that ripple across a panel with a slow color shift; use as a calm ambient header behind a title when a gradient blob feels too soft. Rebuild as 8-12 SVG polylines or Canvas 2D paths displaced by a sine function; pure lite, under 2 KiB. Tags: background, ambient, motion, decor. Fits: landing-funnel, landing, page.
- **Particles** · `build` · webgl · lite ✗ · touch ✓ · motion static — A field of drifting dots behind a hero or empty state; draw a few dozen on Canvas 2D at DPR 1, aria-hidden, and stop them under reduced motion. The same read comes from 40-80 Canvas 2D circles with slow velocity and soft alpha; no library. Tags: background, ambient, motion, decor. Fits: landing-funnel, landing, game-2d, page.
- **Pixel Blast** · `build` · three · lite ✗ · touch ✓ · motion static — Chunky pixel bursts that ripple out from a tap; rebuild on Canvas 2D as tap feedback in a game or a one-shot celebration when a form confirms. Three plus postprocessing exist only for the optional liquid pass. Tags: background, game, motion, action, decor. Fits: game-2d-canvas, form-confirmation, game-2d, landing.
- **Pixel Snow** · `build` · three · lite ✗ · touch ✓ · motion static — Pixelated snow drifting down behind a card; rebuild on Canvas 2D for a seasonal invite or countdown, aria-hidden, frozen to a still scatter under reduced motion. Three is overkill for falling squares; 60-120 rects with a slow y velocity and a sideways wobble on Canvas 2D reads the same and is lite. Tags: background, ambient, motion, decor. Fits: rsvp-guest-list, countdown-ticker-hero, landing, page.
- **Prismatic Burst** · `build` · webgl · lite ✗ · touch ✓ · motion static — Rainbow rays bursting from a center point; rebuild as a slowly rotating CSS conic-gradient sunburst under a radial mask for a celebration or reveal hero. The shader adds distortion and a texture lookup that a CSS sunburst loses; when a shader look is wanted, packed Light Rays and Side Rays already exis… Tags: background, hero, motion, decor. Fits: form-confirmation, countdown-ticker-hero, landing-funnel, landing, store.
- **Radar** · `build` · webgl · lite ✗ · touch ✓ · motion static — Concentric rings, spokes and a sweeping beam; rebuild in SVG with a rotating conic-gradient wedge as the searching state of a status or chat screen. Pure geometry: circles, lines and one conic-gradient wedge rotating with transform; under 1 KiB and lite. Tags: status, thinking, loading, motion, decor. Fits: status-thinking, chat-with-agent, tool, game-2d.
- **Ripple Grid** · `build` · webgl · lite ✗ · touch ✓ · motion static — A grid whose lines ripple outward in slow rings; an ambient backdrop for one hero screen of a tool or game lobby, never behind body copy. Concept is a grid of lines displaced by a radial sine; Canvas 2D or an SVG with stroke offsets rebuilds it in under 3 KiB with no shader. Tags: background, ambient, motion, webgl, decor. Fits: landing-funnel, game-2d-canvas, landing, game-2d, tool.
- **Scanner** · `build` · webgl · lite ✗ · touch ✓ · motion static — Soft interference bands sweep the panel like an oscilloscope trace; use as the ambient plate behind a working or scanning status, one per screen. Two or three repeating-linear-gradient layers moved with transform and a slow opacity pulse give the same calm scan-line read in pure CSS, which woul… Tags: background, ambient, status, motion, thinking. Fits: status-thinking, chat-with-agent, tool, page.
- **Shape Grid** · `build` · canvas2d · lite ✗ · touch ✓ · motion static — A field of small squares, hexagons, circles or triangles drifting in one direction; a quiet decorative plate behind a hero card or lobby. A tiled SVG pattern (one shape symbol, repeated) with a single translate keyframe is the whole effect; Canvas 2D if per-shape variation is wanted. Tags: background, decor, ambient, motion, game. Fits: game-2d-canvas, landing-funnel, game-2d, landing, page.
- **Shape Waves** · `build` · webgl · lite ✗ · touch ✓ · motion static — Rows of tiny shapes brighten and swell as waves roll across them, optionally parting around a word; a hero plate for a landing or launch card. WebGPU is not in the contract and cannot be packed as-is; do not add to the restricted allowlist. Tags: background, hero, motion, webgl, text. Fits: landing-funnel, countdown-ticker-hero, landing, store.
- **Sliced Waves** · `build` · webgl · lite ✗ · touch ✓ · motion static — A row of soft glowing bars that ripple like an equalizer; an ambient plate for a music, playlist or listening-party card, one per screen. A dozen div bars with transform: scaleY driven by staggered keyframes and a box-shadow glow is the entire effect in CSS, and that version is lite-saf… Tags: background, ambient, data, motion, media. Fits: gallery, page, landing.
- **Threads** · `build` · webgl · lite ✗ · touch ✓ · motion static — Fine wavy lines that sway like woven fabric; an ambient plate behind a hero headline, drawn once as a still under reduced motion. Twenty to forty polylines with a sine offset per vertex in Canvas 2D reproduce the fabric read with no dependency. Tags: background, ambient, motion, webgl, decor. Fits: landing-funnel, chat-with-agent, landing, page, tool.
- **Topography** · `build` · webgl · lite ✗ · touch ✓ · motion static — Glowing contour lines that slowly breathe like a living elevation map; a hero plate for a trip, hike, route or map-flavored card. Build it original: a coarse value-noise grid (about 40x80 cells at 390px) run through marching squares on Canvas 2D, contour levels tinted by elevati… Tags: background, hero, webgl, data, ambient. Fits: landing-funnel, rsvp-guest-list, landing, page, tool.
- **Waves** · `build` · canvas2d · lite ✗ · touch ✓ · motion static — Layered flowing lines with a gentle swell; a calm ambient plate behind a hero card, one per screen and still under reduced motion. Same family as Threads and Web Threads; one original Canvas 2D 'flowing lines' plate with a density and swell prop covers all three. Tags: background, ambient, motion, decor, hero. Fits: landing-funnel, form-confirmation, landing, page.
- **Web Threads** · `build` · webgl · lite ✗ · touch ✓ · motion static — Glowing sine threads converging on a bright focal point; a hero plate for a launch or landing card that wants one luminous center. A variant of the flowing-lines plate: sine polylines whose amplitude tapers toward a shared point, glow via layered low-alpha strokes rather than sha… Tags: background, hero, motion, webgl, ambient. Fits: landing-funnel, countdown-ticker-hero, landing, store.
- **Balatro** · `no` · webgl · lite ✗ · touch ✓ · motion static — The swirling paint shader from the game Balatro; it reproduces another product's identity, so it stays out of every mini-app. Tags: background, webgl, ambient.
- **Ballpit** · `no` · three · lite ✗ · touch ✓ · motion static — A 3D physics ball pit filling the page; too heavy and too playful for a 390px card in Messages, so it stays out. Tags: background, webgl, game, motion.
- **Floating Lines** · `no` · three · lite ✗ · touch ✗ · motion static — Three.js lines that drift and bend toward the cursor; skip it in a mini-app, since Messages has no cursor and the packed Beams background already gives ambient light lines. Tags: background, webgl, cursor, ambient, motion.
- **Grid Distortion** · `no` · three · lite ✗ · touch ✗ · motion static — An image whose mesh warps under the cursor; skip it in a mini-app, since the distortion exists only while a pointer moves and Messages has no pointer. Nearest in the Kit: `@kit/fancy/pixelate-svg-filter`. Tags: media, cursor, webgl, filter, motion.
- **Grid Motion** · `no` · dom · lite ✗ · touch ✗ · motion static — Rows of tiles that slide with the pointer's position; skip it in a mini-app, since there is no pointer on touch and a drifting wall of tiles fights the single column. Nearest in the Kit: `@kit/fancy/simple-marquee`, `@kit/fancy/simple-carousel`. Tags: background, cursor, gallery, layout, motion.
- **Hyperspeed** · `no` · three · lite ✗ · touch ✓ · motion static — Light streaks racing at the viewer, faster while held; skip it in a mini-app, since it is a heavy full-page Three scene with bloom and the hold gesture collides with iOS long-press. Tags: background, webgl, motion, game, hero.

### arlan.me vault studies

18 effects: 6 already in the Kit, 0 packed Tier B, 0 to pack, 2 to build, 10 not for a mini-app.

- **Apple's corners** · `@kit/arlan/squircle` · svg · lite ✓ · touch ✓ · motion n/a — True superellipse corners for a button or card via clip-path; use where the Air pill radius still shows a kink, one shape per element. Tags: button, card, style, layout. Fits: approval-card, form-confirmation, product-page, landing, store, tool, page.
- **Ghosty reveal** · `@kit/arlan/ghosty-reveal` · css · lite ✓ · touch ✓ · motion static — Mask-image wipe that lets a photo bleed in through a soft cloudy edge; use for one hero image or gallery reveal per screen, mask asset bundled. Tags: media, motion, gallery, hero. Fits: gallery, product-page, landing, store, page.
- **Holo** · `@kit/arlan/holo` · css · lite ✓ · touch ✓ · motion static — Holographic foil card that tilts with a finger drag or device orientation; use once per screen for a ticket, pass or profile hero. Tags: card, motion, hero, material. Fits: rsvp-guest-list, product-page, landing, store, page.
- **Liquid UI** · `@kit/arlan/liquid-ui` · svg · lite ✓ · touch ✓ · motion static — SDF-driven liquid silhouettes that fuse neighbouring cards with an inward bend; a decorative group background behind a card cluster, not layout. Tags: card, decor, motion, layout. Fits: gallery, product-page, landing, store, page.
- **The art of color depth** · `@kit/arlan/color-depth` · css · lite ✓ · touch ✓ · motion n/a — Ten layered-light CSS materials (glossy, glass, metal, cushion) for buttons; use on the one primary action of a screen, never on every control. Tags: button, material, style, action. Fits: approval-card, form-confirmation, landing-funnel, landing, store, tool, page.
- **The typer** · `@kit/arlan/typer` · dom · lite ✓ · touch ✓ · motion static — Eased typing where a wave flickers each letter through pill, highlight and outline states before landing; for one headline or status line. Tags: text, motion, hero, status. Fits: status-thinking, countdown-ticker-hero, chat-with-agent, landing-funnel, landing, page, tool.
- **Kinetic typography** · `build` · dom · lite ✓ · touch ✓ · motion static — A hero letter or short word sliced into tiles that ripple on independent sine waves; the single hero motion on a title screen, never body copy. Kit status: not reviewed for Kit 2026.09. Tags: text, motion, hero, decor. Fits: countdown-ticker-hero, landing-funnel, landing, page, game-2d.
- **Pixel brushes** · `build` · canvas2d · lite ✓ · touch ✓ · motion n/a — Canvas 2D brush that stamps a small shape along the finger's path, from crisp line to loose spray; use for a doodle, signature or guestbook sketch. Kit status: not reviewed for Kit 2026.09. Tags: input, media, game, action. Fits: game-2d-canvas, form-confirmation, tool, game-2d.
- **Amo hover button** · `no` · video · lite ✓ · touch ✗ · motion static — A pill button that plays a short bundled clip of its label puffing into glossy 3D letters on hover; not for mini-apps: it is amo's trade dress and hover-only. Tags: button, pill, media, cursor.
- **Arcade pixel** · `no` · webgl · lite ✗ · touch ✓ · motion static — A word drawn tiny and blown up so each pixel becomes a big square, rendered in WebGL; not for mini-apps, WebGL is non-lite and the study is excluded. Tags: text, webgl, style, hero.
- **Chromatic glow** · `no` · webgl · lite ✗ · touch ✓ · motion static — Soft light bloom that splits into colour fringes at the edges, a WebGL post effect; not for mini-apps, WebGL is non-lite and the study is excluded. Tags: text, decor, webgl, ambient.
- **Dia Browser's gradient** · `no` · css · lite ✗ · touch ✓ · motion static — A bottom-of-screen glow of blurred colour bars that grows up on load; not for mini-apps, it reproduces Dia Browser's identity and is a full-screen background. Nearest in the Kit: `@kit/fancy/animated-gradient-with-svg`. Tags: background, ambient, decor, hero.
- **Fade motion** · `no` · webgl · lite ✗ · touch ✓ · motion static — A headline motion-blurred by stacking hundreds of faint copies on the GPU; not for mini-apps, WebGL is non-lite and the study is excluded. Nearest in the Kit: `@kit/fancy/vertical-cut-reveal`, `@kit/arlan/typer`. Tags: text, motion, webgl, hero.
- **Figma vector editor** · `no` · svg · lite ✓ · touch ✓ · motion n/a — Figma-style selection chrome: thin blue box, corner handles, size tag and draggable bezier points; not for mini-apps, it is Figma's trade dress. Tags: input, layout, style.
- **Midjourney Medical's ASCII** · `no` · dom · lite ✓ · touch ✓ · motion static — Loose letters spin and settle into a wordmark as live text on load; not for mini-apps, it reproduces the Midjourney Medical identity. Nearest in the Kit: `@kit/fancy/scramble-in`. Tags: text, motion, hero, loading.
- **Ransom note** · `no` · dom · lite ✓ · touch ✓ · motion n/a — Type with real cut-out magazine letters taped down crooked; not for mini-apps as published, the letter imagery is site-hosted and was never captured. Tags: text, input, style, decor.
- **Realistic emboss** · `no` · webgl · lite ✗ · touch ✓ · motion static — Realistic embossed lettering with directional light, rendered in WebGL; not for mini-apps, WebGL is non-lite and the study is excluded. Nearest in the Kit: `@kit/arlan/color-depth`. Tags: text, material, style, webgl.
- **Symbols effect** · `no` · webgl · lite ✗ · touch ✓ · motion static — GPU filter that cuts an image or live video into four brightness bands stamped with tinted symbols; not for mini-apps, GPU-only makes it non-lite. Tags: media, filter, webgl, style.

Three further vault pieces were supplied by the owner as full briefs with source (`prompts/src/briefs/`); they are not in the 2026-09-17 sitemap crawl and are harvest candidates, verified at harvest:

- **Shutter type** · `build` · canvas2d · lite ✓ · touch ✓ · motion static — Two lines of type seen through a rolling shutter: stripes lag the ink and a word tears into bands only while it moves, whole at rest. One hero motion for a title card; brief in `briefs/shutter-type.md`. Tags: text, motion, hero. Fits: landing-funnel; landing, page.
- **Swing type** · `build` · canvas2d · lite ✓ · touch ✓ · motion static — Enormous coloured letters on a pendulum, each turning edge-on as it passes so only two or three read at once and the word never assembles; the word changes by turning. Brief in `briefs/swing-type.md`. Tags: text, motion, hero. Fits: landing-funnel; landing, page.
- **Rush type** · `build` · webgl · lite ✗ · touch ✓ · motion static — One lowercase word rests, then blasts twelve times taller and tears into green and violet streaks from speed alone, resolving to plain white at rest. Non-lite (WebGL1); the resting word is the still frame. Brief in `briefs/rush-type.md`. Tags: text, motion, hero, webgl. Fits: landing.

## 5. Exclusions

Not in the Kit, so stop looking:

- **ReactBits** (`DavidHDev/react-bits`) — "MIT + Commons Clause": may compile into an app, may not be redistributed as source. Tier B: it never lives in Git or in this Kit; the Build Service resolves it from the restricted artifact (`restricted/README.md`). The 13 files under `apps/web/lib/miniapps/client/backgrounds/vendor/` are ReactBits ports and are governed by the same terms.
- **CanvasUI** (`DavidHDev/canvas-ui`) — same Commons Clause, and it needs WebGL2 + Three + experimental HTML-in-canvas. Excluded outright.
- **WebGL under lite** — `libraries/metal-fx` is the only WebGL component and is `lite="false"`; `AirMetalFx` renders its child on a flat plate when lite, reduced motion, or no WebGL. arlan's `arcade-pixel`, `fade-motion`, `chroma-glow`, `emboss` and `sandbox` (the Symbols effect; all WebGL) and Beautiful UI's `prompt-bar` (WebGL via `glimm`) are not harvested.
- **Pro / paid components** — AI CSS `file-diff`, `image-generation`, `inline-citations`, `comparison-table` are Pro (private, licensed). Only the ten free components are here.
- **Trade dress** — arlan's `amo`, `midjourney`, `figma` (URL slug `vector-editor`), `dia-gradient` reproduce other products' identities. Not harvested.
- **Proprietary icon fonts** — Beautiful UI `sidebar-nav` depends on `@central-icons-react`; every other icon import is rewritten to `lucide-react`.
- **Heavy** — Beautiful UI `insight-cards` (`liveline` charts), libraries.dev `img-fx`, Fancy's variable-font components (need the font), physics components other than `elastic-line` (`matter-js`).
- **Not available at harvest** — recorded as gaps in `kit.sources.json`, never fabricated: Beautiful UI `agent-screen` (registry lists it, `r/agent-screen.json` is 404), arlan `ransom-note` (depends on site-hosted imagery and a manifest that is not published).

## 6. Budgets

The Build Service enforces these; `scripts/verify.ts` enforces them on the Kit itself, so a component listed above already fits.

| Budget | Limit | Where it is enforced |
| --- | --- | --- |
| JS, `lite` set (everything marked `lite="true"` bundled together, gzip) | 300 KiB | verify.ts, Build Service |
| JS, hard (any app, gzip) | 1 MiB | Build Service |
| CSS (gzip) | 200 KiB | verify.ts, Build Service |
| One image | 2 MiB | Build Service (`validateBundle`) |
| Apps API state, one resource | 256 KiB | `PUT /api/apps/v1/state` (413) |
| Functions module | 1 MiB | `lib/functions/deploy.ts` |
| Functions request | 50 CPU-ms, 20 subrequests | Dispatcher |

Weights in the catalog are gzipped KiB after esbuild with react/react-dom and the shared vendor packages external, so they add. `jsFull` in `meta.json` is the cost when the app has nothing but React.

Measured for Kit 2026.09: the `lite` set (67 renderable components, bundled together with React) is 181.8 KiB JS and 19.9 KiB CSS gzipped against budgets of 300 KiB and 200 KiB.
