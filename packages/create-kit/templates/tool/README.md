An owner utility: a checklist in the owner's useAirState document with an add row, checkboxes, a clear-done chip and a count pill; guests read and are told "Guests are read-only." on a write.
The Planner replaces the copy and the item shape (fields, count rule); rows, hooks and locked tests stay.
