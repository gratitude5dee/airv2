A one-tap Canvas 2D runner: a canvas, a score in tenths of a second, an owner-kept best via useAirState, Play/Jump buttons.
The Planner replaces the copy and the RULES tuning (and, if asked, the sprite drawing in draw()); the loop, hooks and locked tests stay.
